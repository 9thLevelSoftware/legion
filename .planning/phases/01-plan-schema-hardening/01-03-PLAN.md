---
phase: 01-plan-schema-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - tests/plan-schema-conformance.test.js
  - tests/fixtures/plan-valid-v6.md
  - tests/fixtures/plan-missing-verification.md
  - tests/fixtures/plan-overlap-forbidden.md
files_forbidden:
  - skills/phase-decomposer/SKILL.md
  - skills/plan-critique/SKILL.md
  - agents/
  - commands/
  - adapters/
expected_artifacts:
  - path: tests/plan-schema-conformance.test.js
    provides: Comprehensive test suite for v6.0 plan schema fields
    required: true
  - path: tests/fixtures/plan-valid-v6.md
    provides: Valid plan fixture with all three new schema fields
    required: true
  - path: tests/fixtures/plan-missing-verification.md
    provides: Invalid plan fixture missing verification_commands for negative testing
    required: true
  - path: tests/fixtures/plan-overlap-forbidden.md
    provides: Invalid plan fixture with files_modified/files_forbidden overlap
    required: true
autonomous: false
agents:
  - testing-qa-verification-specialist
requirements:
  - DSC-01
  - DSC-02
  - DSC-03
user_setup: []
verification_commands:
  - test -f tests/plan-schema-conformance.test.js
  - test -f tests/fixtures/plan-valid-v6.md
  - test -f tests/fixtures/plan-missing-verification.md
  - test -f tests/fixtures/plan-overlap-forbidden.md
  - node --test tests/plan-schema-conformance.test.js

must_haves:
  truths:
    - "Test file validates presence and format of files_forbidden, expected_artifacts, and verification_commands"
    - "Test file includes both positive tests (valid plan passes) and negative tests (missing fields fail)"
    - "Test detects files_modified/files_forbidden overlap as a schema violation"
    - "Test fixtures demonstrate the new schema fields in realistic plan frontmatter"
    - "All tests pass when run via node --test runner (project standard)"
  artifacts:
    - path: "tests/plan-schema-conformance.test.js"
      provides: "Schema conformance test suite"
      min_lines: 80
      contains: "files_forbidden"
  key_links:
    - from: "tests/plan-schema-conformance.test.js"
      to: "skills/phase-decomposer/SKILL.md"
      via: "Tests validate the schema defined in the phase-decomposer template"
      pattern: "verification_commands"
    - from: "tests/plan-schema-conformance.test.js"
      to: "skills/plan-critique/SKILL.md"
      via: "Tests mirror the validation rules from plan-critique Section 5"
      pattern: "BLOCKER"
---

<objective>
Create a comprehensive test suite for v6.0 plan schema conformance, including test fixtures demonstrating valid and invalid plans. Tests validate the three new schema fields (files_forbidden, expected_artifacts, verification_commands) and detect violations matching plan-critique Section 5 rules.

Purpose: Ensure schema enforcement is testable and regressions are caught automatically.
Output: `tests/plan-schema-conformance.test.js` + 3 fixture files
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-plan-schema-hardening/01-CONTEXT.md
@.planning/phases/01-plan-schema-hardening/01-01-SUMMARY.md
@.planning/phases/01-plan-schema-hardening/01-02-SUMMARY.md

Relevant source files:
@skills/phase-decomposer/SKILL.md (schema field definitions — read after Plan 01-01 updates)
@skills/plan-critique/SKILL.md (validation rules — read after Plan 01-02 updates)
@tests/agent-contract.test.js (reference for existing test patterns and conventions)
@tests/authority-matrix.test.js (reference for existing test patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixtures</name>
  <files>tests/fixtures/plan-valid-v6.md, tests/fixtures/plan-missing-verification.md, tests/fixtures/plan-overlap-forbidden.md</files>
  <action>
Create a `tests/fixtures/` directory if it doesn't exist. Then create three fixture files:

**Fixture 1: `tests/fixtures/plan-valid-v6.md`**
A complete, valid v6.0 plan with all three new schema fields properly filled:
```yaml
---
phase: 99-test-phase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/test-skill/SKILL.md
  - tests/test-skill.test.js
files_forbidden:
  - agents/
  - commands/
  - adapters/
expected_artifacts:
  - path: skills/test-skill/SKILL.md
    provides: Test skill definition
    required: true
  - path: tests/test-skill.test.js
    provides: Test coverage for the skill
    required: true
autonomous: false
agents:
  - engineering-senior-developer
requirements:
  - TEST-01
user_setup: []
verification_commands:
  - test -f skills/test-skill/SKILL.md
  - test -f tests/test-skill.test.js
  - grep -q "Section 1" skills/test-skill/SKILL.md
must_haves:
  truths:
    - "Test skill has Section 1"
  artifacts:
    - path: skills/test-skill/SKILL.md
      provides: Test skill
      min_lines: 50
      contains: "Section 1"
  key_links: []
---
```

**Fixture 2: `tests/fixtures/plan-missing-verification.md`**
A plan missing the mandatory `verification_commands` field:
```yaml
---
phase: 99-test-phase
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/test-skill/SKILL.md
files_forbidden:
  - agents/
expected_artifacts:
  - path: skills/test-skill/SKILL.md
    provides: Test skill definition
    required: true
autonomous: false
agents:
  - engineering-senior-developer
requirements:
  - TEST-01
user_setup: []
must_haves:
  truths:
    - "Test"
  artifacts: []
  key_links: []
---
```

**Fixture 3: `tests/fixtures/plan-overlap-forbidden.md`**
A plan with overlap between `files_modified` and `files_forbidden`:
```yaml
---
phase: 99-test-phase
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/test-skill/SKILL.md
  - skills/other-skill/SKILL.md
files_forbidden:
  - skills/other-skill/SKILL.md
  - agents/
expected_artifacts:
  - path: skills/test-skill/SKILL.md
    provides: Test skill definition
    required: true
autonomous: false
agents:
  - engineering-senior-developer
requirements:
  - TEST-01
user_setup: []
verification_commands:
  - test -f skills/test-skill/SKILL.md
must_haves:
  truths:
    - "Test"
  artifacts: []
  key_links: []
---
```

> verification: test -f tests/fixtures/plan-valid-v6.md
> verification: test -f tests/fixtures/plan-missing-verification.md
> verification: test -f tests/fixtures/plan-overlap-forbidden.md
  </action>
  <verify>
test -f tests/fixtures/plan-valid-v6.md
test -f tests/fixtures/plan-missing-verification.md
test -f tests/fixtures/plan-overlap-forbidden.md
  </verify>
  <done>Three test fixture files created: valid plan, missing verification_commands, and files_modified/files_forbidden overlap.</done>
</task>

<task type="auto">
  <name>Task 2: Create schema conformance test suite</name>
  <files>tests/plan-schema-conformance.test.js</files>
  <action>
Create `tests/plan-schema-conformance.test.js` following the patterns used in existing test files (reference `tests/agent-contract.test.js` and `tests/authority-matrix.test.js` for conventions).

The test file should:

1. **Import/setup**: Use `node:test` (`describe`, `test`/`it`) and `node:assert/strict` — matching the project's existing test convention (see `tests/agent-contract.test.js` for the canonical pattern). Use `node:fs` and `node:path` for file I/O.

2. **YAML frontmatter parsing**: Use a line-by-line parser matching the project's existing approach (see `tests/path-enforcement.test.js` or `tests/authority-matrix.test.js` for reference). Extract text between `---` delimiters, then parse key-value pairs and simple arrays/objects. The project has **zero npm dependencies** — do NOT install `js-yaml` or any other library. For nested structures like `expected_artifacts` (array of objects), use indentation-based parsing: lines starting with `  - path:` begin a new artifact entry, subsequent indented lines (`    provides:`, `    required:`) are properties of that entry.

3. **Helper function**: `parsePlanFrontmatter(filePath)` — reads a plan .md file, extracts YAML frontmatter between `---` delimiters, returns parsed object with arrays and nested objects.

4. **Test groups** (use `node:test` syntax):

```javascript
const { describe, test } = require('node:test');
const assert = require('node:assert/strict');

describe('Plan Schema Conformance (v6.0)', () => {
  describe('verification_commands field', () => {
    test('valid plan has verification_commands array', ...);
    test('missing verification_commands triggers BLOCKER', ...);
    test('empty verification_commands array triggers BLOCKER', ...);
    test('verification_commands entries are non-empty strings', ...);
  });

  describe('files_forbidden field', () => {
    test('valid plan has files_forbidden array', ...);
    test('missing files_forbidden for code plan triggers WARNING', ...);
    test('files_modified and files_forbidden have no overlap (BLOCKER if overlap)', ...);
    test('empty files_forbidden array is valid', ...);
  });

  describe('expected_artifacts field', () => {
    test('valid plan has expected_artifacts with path and provides', ...);
    test('missing expected_artifacts triggers WARNING', ...);
    test('required artifacts appear in files_modified', ...);
    test('artifact entries have required structure (path, provides)', ...);
  });

  describe('Phase 1 plan files conform to v6.0 schema', () => {
    test('01-01-PLAN.md passes schema validation', ...);
    test('01-02-PLAN.md passes schema validation', ...);
    test('01-03-PLAN.md passes schema validation', ...);
  });
});
```

4. **Validation functions** (reusable):
- `validateVerificationCommands(frontmatter)` — returns `{ valid, severity, message }`
- `validateFilesForbidden(frontmatter)` — returns `{ valid, severity, message }`
- `validateExpectedArtifacts(frontmatter)` — returns `{ valid, severity, message }`
- `detectFileOverlap(frontmatter)` — returns `{ hasOverlap, overlapping }` for files_modified vs files_forbidden

> verification: test -f tests/plan-schema-conformance.test.js
> verification: grep -q "verification_commands" tests/plan-schema-conformance.test.js
> verification: grep -q "files_forbidden" tests/plan-schema-conformance.test.js
> verification: grep -q "expected_artifacts" tests/plan-schema-conformance.test.js
  </action>
  <verify>
test -f tests/plan-schema-conformance.test.js
grep -q "verification_commands" tests/plan-schema-conformance.test.js
grep -q "files_forbidden" tests/plan-schema-conformance.test.js
grep -q "expected_artifacts" tests/plan-schema-conformance.test.js
  </verify>
  <done>Schema conformance test suite created with validation functions and test groups covering all three new fields, plus self-validation against Phase 1 plan files.</done>
</task>

<task type="auto">
  <name>Task 3: Run tests and verify all pass</name>
  <files>tests/plan-schema-conformance.test.js</files>
  <action>
Run the full test suite to ensure:
1. All new schema conformance tests pass
2. No existing tests have regressed

Execute:
```bash
node --test tests/plan-schema-conformance.test.js
node --test  # full suite
```

Fix any test failures. Common issues to watch for:
- YAML parsing differences (ensure line-by-line parser handles nested arrays-of-objects)
- Path resolution (tests may need to resolve paths relative to repo root)
- Fixture file format (ensure `---` delimiters are correct)
- Do NOT install any npm dependencies — the project has zero runtime/dev dependencies

> verification: node --test tests/plan-schema-conformance.test.js
  </action>
  <verify>
node --test tests/plan-schema-conformance.test.js
  </verify>
  <done>All schema conformance tests pass, no regressions in existing test suite.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Test fixtures exist in tests/fixtures/ with valid and invalid plan examples
- [ ] tests/plan-schema-conformance.test.js exists with all test groups
- [ ] Tests cover verification_commands (BLOCKER), files_forbidden (WARNING/BLOCKER), expected_artifacts (WARNING)
- [ ] Tests detect files_modified/files_forbidden overlap
- [ ] Phase 1 plan files (01-01, 01-02, 01-03) pass schema validation
- [ ] All tests pass via `node --test`
- [ ] No changes to skill files (those are owned by Plans 01-01 and 01-02)
</verification>

<success_criteria>
- test suite has >= 12 individual test cases covering all three schema fields
- All fixture-based tests pass (positive and negative)
- Phase 1 plan files self-validate against the new schema
- No regressions in existing test suite
- `node --test tests/plan-schema-conformance.test.js` exits with code 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-plan-schema-hardening/01-03-SUMMARY.md`

Update STATE.md and ROADMAP.md to mark Phase 1 complete.
</output>
