---
phase: 01-plan-schema-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/plan-critique/SKILL.md
files_forbidden:
  - skills/phase-decomposer/SKILL.md
  - agents/
  - commands/
  - adapters/
  - tests/
expected_artifacts:
  - path: skills/plan-critique/SKILL.md
    provides: Updated plan-critique skill with new Section 5 for schema conformance validation
    required: true
autonomous: false
agents:
  - testing-reality-checker
requirements:
  - DSC-03
user_setup: []
verification_commands:
  - grep -q "Section 5" skills/plan-critique/SKILL.md
  - grep -q "Schema" skills/plan-critique/SKILL.md
  - grep -q "BLOCKER" skills/plan-critique/SKILL.md
  - grep -q "verification_commands" skills/plan-critique/SKILL.md
  - grep -q "files_forbidden" skills/plan-critique/SKILL.md

must_haves:
  truths:
    - "Plan-critique has a new Section 5 titled 'Schema Conformance Check' or similar"
    - "Missing verification_commands triggers BLOCKER severity in the critique verdict"
    - "Schema validation checks for files_forbidden, expected_artifacts, and verification_commands presence"
    - "Section 5 integrates with the existing Section 3 verdict logic (PASS/CAUTION/REWORK)"
    - "Overlap between files_modified and files_forbidden triggers BLOCKER"
  artifacts:
    - path: "skills/plan-critique/SKILL.md"
      provides: "Schema validation section with BLOCKER enforcement"
      min_lines: 350
      contains: "Schema Conformance"
  key_links:
    - from: "skills/plan-critique/SKILL.md"
      to: "skills/phase-decomposer/SKILL.md"
      via: "Critique validates schema fields defined in decomposer template"
      pattern: "verification_commands"
---

<objective>
Add a new Section 5 "Schema Conformance Check" to the plan-critique skill that validates plan frontmatter against the v6.0 schema. Flag missing `verification_commands` as BLOCKER severity, validate `files_forbidden`/`files_modified` non-overlap, and check `expected_artifacts` completeness.

Purpose: Enforce the schema defined in Plan 01-01 at planning time, shifting quality checks left before execution begins.
Output: Updated `skills/plan-critique/SKILL.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-plan-schema-hardening/01-CONTEXT.md
@skills/plan-critique/SKILL.md

Relevant source files:
@skills/phase-decomposer/SKILL.md (read-only reference for schema field definitions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Section 5 — Schema Conformance Check</name>
  <files>skills/plan-critique/SKILL.md</files>
  <action>
In `skills/plan-critique/SKILL.md`, after Section 4 (Agent Selection for Critique) and before the References section, add a new section:

```markdown
## Section 5: Schema Conformance Check (DSC-01, DSC-02, DSC-03)

Validates plan frontmatter against the v6.0 schema before execution. Runs automatically as part of plan-critique (before pre-mortem and assumption hunting). Schema violations are reported alongside critique findings in the consolidated report.

### When to Run

Schema conformance check runs:
- Automatically when plan-critique is invoked (always, before Sections 1-2)
- Standalone via direct invocation on any plan file
- During wave-executor pre-flight validation

### Validation Rules

For each plan file in the phase:

#### Rule 1: verification_commands (BLOCKER if missing)
```
Check: Plan frontmatter contains `verification_commands` field
If missing: BLOCKER — "Plan {NN}-{PP} missing mandatory verification_commands field"
If present but empty array: BLOCKER — "Plan {NN}-{PP} has empty verification_commands — must contain at least one bash command"
If present with entries: PASS
Additional: Each command must be a valid-looking bash command (not empty string, not a comment)
```

#### Rule 2: files_forbidden (WARNING if missing for code-modifying plans)
```
Check: Plan frontmatter contains `files_forbidden` field
If missing AND plan has code files in files_modified: WARNING — "Plan {NN}-{PP} modifies code but declares no files_forbidden"
If missing AND plan is markdown-only: PASS (no warning)
If present: Validate no overlap with files_modified using PREFIX MATCHING:
  - A `files_forbidden` entry ending with `/` (e.g., `agents/`) is a directory prefix — it matches any `files_modified` entry starting with that prefix
  - A `files_forbidden` entry without trailing `/` is an exact path — it matches only identical entries in `files_modified`
  - If overlap found: BLOCKER — "Plan {NN}-{PP} has {file} in both files_modified and files_forbidden"
  - Document this matching algorithm explicitly in the section so implementations are consistent
```

#### Rule 3: expected_artifacts (WARNING if missing)
```
Check: Plan frontmatter contains `expected_artifacts` field
If missing: WARNING — "Plan {NN}-{PP} missing expected_artifacts — consider declaring outputs"
If present: Validate structure
  - Each entry must have `path` (string) and `provides` (string)
  - Each `required: true` artifact must appear in `files_modified`
  - If required artifact not in files_modified: WARNING — "Artifact {path} marked required but not in files_modified"
```
```

This section should follow the same formatting conventions as Sections 1-4 (step-by-step process blocks with clear inputs/outputs).

> verification: grep -q "Section 5" skills/plan-critique/SKILL.md
> verification: grep -q "Schema Conformance" skills/plan-critique/SKILL.md
> verification: grep -q "BLOCKER" skills/plan-critique/SKILL.md
  </action>
  <verify>
grep -q "Section 5" skills/plan-critique/SKILL.md
grep -q "Schema Conformance" skills/plan-critique/SKILL.md
grep -q "BLOCKER" skills/plan-critique/SKILL.md
  </verify>
  <done>Plan-critique has a new Section 5 with three validation rules covering verification_commands (BLOCKER), files_forbidden (WARNING/BLOCKER), and expected_artifacts (WARNING).</done>
</task>

<task type="auto">
  <name>Task 2: Integrate schema check into critique verdict logic</name>
  <files>skills/plan-critique/SKILL.md</files>
  <action>
In `skills/plan-critique/SKILL.md` Section 3 "Critique Report and Routing", update Step 1 (Merge findings) to include schema conformance findings:

Update the merge step to read:
```
Step 1: Merge findings
  Combine:
  - Schema conformance violations (Section 5)
  - Pre-mortem critical risks (Section 1)
  - Assumption critical/warning items (Section 2)
  Deduplicate: if a pre-mortem finding and an assumption point to the same
  plan section with the same root issue, merge into one entry.
  Schema BLOCKERs are never deduplicated — they always appear individually.
```

Update the verdict computation in Step 2 to account for schema violations:
```
Step 2: Compute critique verdict
  - PASS: No schema BLOCKERs AND no critical risks AND no critical assumptions
  - CAUTION: 1-2 critical items (including schema warnings), all have clear mitigations
  - REWORK: Any schema BLOCKER OR 3+ critical items OR any item without clear mitigation
```

Add a schema-specific section to the consolidated critique output in Step 3:
```
### Schema Conformance
| Plan | verification_commands | files_forbidden | expected_artifacts | Status |
|------|----------------------|----------------|--------------------|--------|
| {NN}-{PP} | {PASS/BLOCKER} | {PASS/WARNING/BLOCKER} | {PASS/WARNING} | {overall} |
```

> verification: grep -q "Schema conformance" skills/plan-critique/SKILL.md
> verification: grep -q "Schema BLOCKERs" skills/plan-critique/SKILL.md
  </action>
  <verify>
grep -q "Schema conformance" skills/plan-critique/SKILL.md
grep -q "Schema BLOCKERs" skills/plan-critique/SKILL.md
  </verify>
  <done>Section 3 verdict logic includes schema conformance findings, with BLOCKERs triggering automatic REWORK verdict.</done>
</task>

<task type="auto">
  <name>Task 3: Add schema validation examples and edge cases</name>
  <files>skills/plan-critique/SKILL.md</files>
  <action>
In `skills/plan-critique/SKILL.md` Section 5, after the validation rules, add a subsection:

```markdown
### Schema Validation Examples

**Example 1: Plan passes all checks**
```yaml
files_modified: [skills/new-skill/SKILL.md]
files_forbidden: [agents/, commands/]
expected_artifacts:
  - path: skills/new-skill/SKILL.md
    provides: New skill definition
    required: true
verification_commands:
  - test -f skills/new-skill/SKILL.md
  - grep -q "Section 1" skills/new-skill/SKILL.md
```
Result: All rules PASS.

**Example 2: Missing verification_commands**
```yaml
files_modified: [skills/new-skill/SKILL.md]
# no verification_commands field
```
Result: Rule 1 BLOCKER — "missing mandatory verification_commands"

**Example 3: files_modified / files_forbidden overlap**
```yaml
files_modified: [skills/decomposer/SKILL.md, skills/critique/SKILL.md]
files_forbidden: [skills/critique/SKILL.md]
```
Result: Rule 2 BLOCKER — "skills/critique/SKILL.md in both files_modified and files_forbidden"

### Edge Cases

- **Autonomous plans**: Schema checks still apply. Even autonomous plans must declare verification_commands.
- **Wave 2+ plans**: May reference Wave 1 outputs in expected_artifacts. This is valid.
- **Empty files_forbidden**: `files_forbidden: []` is valid and passes. Only missing field triggers warning for code plans.
- **Legacy plans (pre-v6.0)**: Schema check reports findings but does NOT block execution of archived plans. Apply exemption: if plan's phase number predates v6.0, downgrade BLOCKERs to WARNINGs.
```

> verification: grep -q "Schema Validation Examples" skills/plan-critique/SKILL.md
> verification: grep -q "Edge Cases" skills/plan-critique/SKILL.md
> verification: grep -q "Legacy plans" skills/plan-critique/SKILL.md
  </action>
  <verify>
grep -q "Schema Validation Examples" skills/plan-critique/SKILL.md
grep -q "Edge Cases" skills/plan-critique/SKILL.md
grep -q "Legacy plans" skills/plan-critique/SKILL.md
  </verify>
  <done>Section 5 includes concrete examples of passing/failing plans and edge case handling for autonomous plans, wave dependencies, empty arrays, and legacy plan exemptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Section 5 exists in plan-critique with title "Schema Conformance Check"
- [ ] Rule 1 (verification_commands) triggers BLOCKER when missing or empty
- [ ] Rule 2 (files_forbidden) triggers BLOCKER on overlap with files_modified
- [ ] Rule 3 (expected_artifacts) triggers WARNING when missing
- [ ] Section 3 verdict logic updated to include schema findings
- [ ] Schema Conformance table added to critique output format
- [ ] Examples show passing and failing plans
- [ ] Edge cases documented (autonomous, wave 2+, empty arrays, legacy)
- [ ] No changes made to files outside `skills/plan-critique/SKILL.md`
</verification>

<success_criteria>
- Plan-critique Section 5 validates all three schema fields
- Missing verification_commands triggers BLOCKER severity
- files_modified/files_forbidden overlap triggers BLOCKER severity
- Section 3 verdict integrates schema findings into PASS/CAUTION/REWORK logic
- Examples demonstrate clear pass/fail scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/01-plan-schema-hardening/01-02-SUMMARY.md`

Update STATE.md to note Plan 01-02 completion.
</output>
