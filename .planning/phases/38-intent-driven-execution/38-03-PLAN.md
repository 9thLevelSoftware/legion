---
phase: 38-intent-driven-execution
plan: 38-03
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - commands/review.md
  - skills/review-panel/SKILL.md
autonomous: true
requirements:
  - INTENT-04
must_haves:
  truths:
    - '/legion:review --just-security runs security-only audit'
    - Review panel filters to security domain findings only
    - Non-security agents excluded from security-only review
    - Authority matrix domains respected in filtering
  artifacts:
    - path: commands/review.md
      provides: Review command with --just-security flag support
      contains:
        - '--just-security'
        - security-only
        - INTENT DETECTION
    - path: skills/review-panel/SKILL.md
      provides: Review panel with intent-based filtering
      contains:
        - filterByIntent
        - securityOnlyMode
  key_links:
    - from: commands/review.md
      to: skills/intent-router/SKILL.md
      via: 'parseIntentFlags(), validateFlagCombinations()'
    - from: commands/review.md
      to: skills/review-panel/SKILL.md
      via: composePanel(intent) with domain filtering
---

<objective>
Integrate intent-driven execution into /legion:review command. Support --just-security flag for security-only audits using review panel filtering.

Purpose: Enable targeted security reviews without running full review panels
Output: Enhanced review command with intent-based panel composition
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-intent-driven-execution/38-RESEARCH.md
@commands/review.md
@skills/review-panel/SKILL.md
@skills/intent-router/SKILL.md
@.planning/config/intent-teams.yaml
@.planning/config/authority-matrix.yaml
@tests/intent-review.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add intent detection to review command</name>
  <files>commands/review.md</files>
  <action>
Update `commands/review.md` to add intent flag support for --just-security:

**Add after Step 0 (Initialization):**

```markdown
## Step 0.5: INTENT DETECTION AND VALIDATION

If $ARGUMENTS contains intent flags (--just-*):

1. **Parse Intent Flags**
   - Load skill: intent-router
   - Call: parseIntentFlags($ARGUMENTS)
   - Expected for review: [{name: "security-only"}] (--just-security)

2. **Validate for Review Command**
   - Call: validateFlagCombination(intents, "review")
   - Only "security-only" intent is valid for review command
   - If other intents detected (harden, document, etc.):
     - ERROR: "{intent} is only valid for /legion:build"
     - EXIT

3. **Determine Review Mode**
   - Normal mode: No intent flags → Full review panel (all domains)
   - Intent mode: --just-security → Filtered panel (security domains only)
   - Set: REVIEW_MODE = "full" | "security-only"

4. **Load Intent Configuration**
   - Load: intent-teams.yaml
   - Get: security-only template (agents, domains)
   - Domains: ["security", "owasp", "stride", "authentication", "authorization"]
```

**Update Step 1 (Agent Selection):**

```markdown
## Step 1: AGENT SELECTION

**If REVIEW_MODE === "security-only":**
1. Use intent template agents:
   - Primary: engineering-security-engineer
   - Secondary: testing-api-tester (for API security)
2. Skip normal agent registry recommendation
3. Set: agents = [security-engineer, api-tester]

**If REVIEW_MODE === "full":**
[Existing Step 1 logic unchanged]
```

**Add to USAGE section:**

```markdown
## Intent-Driven Review

| Flag | Description |
|------|-------------|
| `--just-security` | Security-only audit (OWASP + STRIDE) |

Examples:
- `/legion:review` — Full review panel (all domains)
- `/legion:review --just-security` — Security-only review
```

Insert intent detection without breaking existing review logic. Normal reviews (no flags) should work unchanged.
  </action>
  <verify>
    <automated>grep -q "INTENT DETECTION" commands/review.md && echo "Intent detection section added"</automated>
    <manual>Review commands/review.md: Step 0.5 exists, Step 1 has security-only branch</manual>
    <sampling_rate>run after task commits, before next task begins</sampling_rate>
  </verify>
  <done>review.md has intent detection, security-only mode in agent selection</done>
</task>

<task type="auto">
  <name>Task 2: Add intent filtering to review panel</name>
  <files>skills/review-panel/SKILL.md</files>
  <action>
Update `skills/review-panel/SKILL.md` to add intent-based panel filtering:

**Add to Section 1 (Panel Composition):**

```markdown
### 1.2 Intent-Based Panel Filtering

When review command specifies intent (e.g., --just-security):

1. **Load Intent Domains**
   - From intent-teams.yaml security-only template
   - Domains: security, owasp, stride, authentication, authorization

2. **Filter Agents by Domain**
   - For each candidate agent:
     - Check agent's primary domains in authority-matrix.yaml
     - Include only if domains overlap with intent domains
   - Example: engineering-security-engineer (security domain) → INCLUDE
   - Example: engineering-frontend-developer (ui domain) → EXCLUDE

3. **Override Recommendations**
   - If intent mode: Use intent template agents, skip registry recommendation
   - If full mode: Use existing recommendation algorithm

4. **Domain-Only Review Scope**
   - Agents review ONLY their intent-matching domains
   - Non-security findings deferred or excluded
   - Prevents out-of-domain opinions (already filtered by authority in Phase 37)
```

**Add new Step 2.5: INTENT FILTERING (after Step 2):**

```markdown
## Step 2.5: INTENT FILTERING (conditional)

If REVIEW_MODE === "security-only":

1. **Filter Findings by Domain**
   - For each finding from all agents:
     - Check finding.category or finding.domain
     - Include only if matches security domains:
       - security, owasp, stride, authentication, authorization
       - vulnerability, injection, xss, csrf, etc.
   - Exclude: performance, accessibility, code-style, ui-ux findings

2. **Apply Authority Filtering**
   - Per AUTH-04 from Phase 37
   - If engineering-security-engineer provided finding → KEEP
   - If testing-api-tester provided security finding → KEEP
   - If design-ui-designer provided security opinion → DISCARD (out-of-domain)

3. **Generate Intent Filter Report**
   ```markdown
   ## Intent Filtering Report
   
   **Mode:** security-only
   **Domains:** security, owasp, stride, authentication, authorization
   
   **Filtering Applied:**
   - Raw findings: 47
   - Security domain findings: 12
   - After authority filtering: 10
   - Excluded: 37 (non-security domains)
   ```

4. **Proceed with filtered findings to Step 3 (Deduplication)**
```

**Update Section 5 (Output):**

```markdown
If REVIEW_MODE === "security-only":
- Write: .planning/security-review-{timestamp}.md
- Include: Security findings only, prioritized by severity
- Format: Standard finding blocks with OWASP/STRIDE categorization
```

This implements INTENT-04: --just-security runs security-only audit with domain-filtered review panel.
  </action>
  <verify>
    <automated>grep -q "Intent-Based Panel Filtering" skills/review-panel/SKILL.md && echo "Intent filtering section added"</automated>
    <manual>Review review-panel skill: Section 1.2 present, Step 2.5 exists with filtering logic</manual>
    <sampling_rate>run after task commits, before next task begins</sampling_rate>
  </verify>
  <done>review-panel skill has intent filtering with domain matching and authority enforcement</done>
</task>

<task type="auto">
  <name>Task 3: Update review command output for intent mode</name>
  <files>commands/review.md</files>
  <action>
Add intent-specific output handling to `commands/review.md`:

**Add new Step 6-INTENT (parallel to Step 6):**

```markdown
## Step 6-INTENT: SECURITY-ONLY OUTPUT

If REVIEW_MODE === "security-only":

1. **Generate Security Report**
   Write to: `.planning/security-review-{timestamp}.md`
   
   Template:
   ```markdown
   # Security Review Report
   
   **Generated:** {timestamp}
   **Mode:** --just-security (security-only audit)
   **Agents:** engineering-security-engineer, testing-api-tester
   
   ## Executive Summary
   - Total findings: {count}
   - Critical (BLOCKER): {count}
   - High (WARNING): {count}
   - Low (SUGGESTION): {count}
   
   ## OWASP Top 10 Coverage
   - [ ] A01: Broken Access Control — {findings}
   - [ ] A02: Cryptographic Failures — {findings}
   - [ ] A03: Injection — {findings}
   - [ ] A05: Security Misconfiguration — {findings}
   - [ ] A07: Auth Failures — {findings}
   - [ ] ...
   
   ## STRIDE Threats Identified
   - Spoofing: {findings}
   - Tampering: {findings}
   - Repudiation: {findings}
   - Information Disclosure: {findings}
   - Denial of Service: {findings}
   - Elevation of Privilege: {findings}
   
   ## Findings
   {Finding blocks from review panel}
   
   ## Remediation Priority
   1. [BLOCKER] {highest severity finding}
   2. [WARNING] {next priority}
   ...
   ```

2. **Display Summary**
   ```
   Security-only review complete.
   
   Findings: {total} (BLOCKER: {blocker}, WARNING: {warning}, SUGGESTION: {suggestion})
   Report: .planning/security-review-{timestamp}.md
   
   Next steps:
   - Review BLOCKER findings immediately
   - Run /legion:build --just-harden for detailed remediation
   - Run full /legion:review for complete audit
   ```

3. **EXIT** (security-only review complete)
```

**Update main Step 6 (Normal Output):**

Add condition:
```markdown
## Step 6: OUTPUT GENERATION

If REVIEW_MODE === "full":
   [Existing Step 6 logic unchanged]

If REVIEW_MODE === "security-only":
   → Use Step 6-INTENT above
```

This provides clear, actionable output for security-only reviews with OWASP/STRIDE categorization.
  </action>
  <verify>
    <automated>grep -q "Step 6-INTENT" commands/review.md && echo "Intent output section added"</automated>
    <manual>Review review.md: Step 6-INTENT present with security report template</manual>
    <sampling_rate>run after task commits, before next task begins</sampling_rate>
  </verify>
  <done>review.md has Step 6-INTENT for security-only output with OWASP/STRIDE format</done>
</task>

</tasks>

<verification>
Review command intent integration validation:
1. All tests from Wave 0 pass
2. review.md has intent detection (Step 0.5)
3. review.md has security-only agent selection (Step 1)
4. review.md has security-only output (Step 6-INTENT)
5. review-panel skill has intent filtering (Section 1.2, Step 2.5)
6. INTENT-04 satisfied: --just-security runs security-only audit
</verification>

<success_criteria>
- review.md updated with intent flag detection and validation
- review.md has security-only mode in agent selection
- review-panel skill has intent-based filtering
- review.md generates security-specific output report
- INTENT-04 satisfied: --just-security runs security-only review
- Normal review mode (no flags) works unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/38-intent-driven-execution/38-03-SUMMARY.md`
</output>
