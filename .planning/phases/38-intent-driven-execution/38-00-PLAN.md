---
phase: 38-intent-driven-execution
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/intent-flag-parsing.test.js
  - tests/intent-filtering.test.js
  - tests/intent-review.test.js
  - tests/intent-teams.test.js
  - tests/intent-validation.test.js
  - tests/fixtures/intent-teams.yaml
autonomous: true
requirements:
  - INTENT-01
  - INTENT-02
  - INTENT-03
  - INTENT-04
  - INTENT-05
  - INTENT-06
must_haves:
  truths:
    - "Test suite validates all intent flag parsing scenarios"
    - "Test suite validates plan filtering by intent"
    - "Test suite validates review panel intent filtering"
    - "Test suite validates team template loading"
    - "Test suite validates flag combination validation"
  artifacts:
    - path: "tests/intent-flag-parsing.test.js"
      provides: "Flag parsing test coverage for --just-harden, --just-document, etc."
      min_tests: 15
    - path: "tests/intent-filtering.test.js"
      provides: "Plan filtering tests for --skip-frontend and --just-document"
      min_tests: 20
    - path: "tests/intent-review.test.js"
      provides: "Review filtering tests for --just-security"
      min_tests: 12
    - path: "tests/intent-teams.test.js"
      provides: "Team template loading and resolution tests"
      min_tests: 15
    - path: "tests/intent-validation.test.js"
      provides: "Flag combination validation tests"
      min_tests: 18
  key_links:
    - from: "tests/*.test.js"
      to: "commands/build.md, commands/review.md"
      via: "behavioral contracts"
---

<objective>
Create comprehensive test scaffolding for intent-driven execution features. Tests define behavioral contracts that implementation plans must satisfy.

Purpose: Establish test-first development pattern ensuring all intent flags work correctly
Output: 5 test files with 80+ tests covering all INTENT requirements
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-intent-driven-execution/38-RESEARCH.md
@tests/authority-matrix.test.js
@tests/deduplication.test.js
@tests/two-wave-detection.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create intent flag parsing tests</name>
  <files>tests/intent-flag-parsing.test.js, tests/fixtures/intent-teams.yaml</files>
  <action>
Create `tests/intent-flag-parsing.test.js` with 15+ tests for flag detection:

1. Test cases for each intent flag:
   - --just-harden → intent: harden
   - --just-document → intent: document  
   - --skip-frontend → intent: skip-frontend
   - --skip-backend → intent: skip-backend
   - --just-security → intent: security-only

2. Test flag combination parsing:
   - Multiple flags in single command
   - Flag order independence
   - Flag with values (--skip-pattern="*.test.js")

3. Test edge cases:
   - Unknown flags handled gracefully
   - Case sensitivity
   - Flags with dashes vs underscores

4. Create minimal fixture `tests/fixtures/intent-teams.yaml` with 2-3 sample intents for testing

Follow test patterns from `tests/authority-matrix.test.js`: use describe/it blocks, assert/strict, and clear test descriptions. Each test must be executable via `node --test tests/intent-flag-parsing.test.js`.
  </action>
  <verify>
    <automated>node --test tests/intent-flag-parsing.test.js</automated>
    <manual>Review test output: all tests should pass, covering flag parsing scenarios</manual>
    <sampling_rate>run after task commits, before next task begins</sampling_rate>
  </verify>
  <done>All 15 flag parsing tests pass, fixture file exists with sample intents</done>
</task>

<task type="auto">
  <name>Task 2: Create intent filtering and validation tests</name>
  <files>tests/intent-filtering.test.js, tests/intent-validation.test.js</files>
  <action>
Create two test files covering filtering and validation:

**tests/intent-filtering.test.js** (20 tests):
1. Agent-based filtering:
   - Exclude plans by agent ID (frontend-developer excluded by --skip-frontend)
   - Include only specific agent types (--just-document includes only documenter)

2. File-based filtering:
   - Pattern matching for file paths ("*.tsx" excluded by --skip-frontend)
   - Glob-style patterns work correctly
   - Multiple patterns combine with OR logic

3. Task-based filtering:
   - Parse plan content to detect task types
   - Filter by include_task_types
   - Filter by exclude_task_types

4. Content-based filtering:
   - Detect "documentation" vs "implementation" intent from plan objective
   - Filter plans with zero matching tasks remaining

**tests/intent-validation.test.js** (18 tests):
1. Mutual exclusion rules:
   - --just-harden + --just-document rejected
   - --just-document + --skip-frontend rejected (redundant)
   - --skip-frontend + --skip-backend rejected (nothing to build)

2. Command context requirements:
   - --just-harden only valid for build command
   - --just-security only valid for review command

3. Combination validation:
   - Valid combinations allowed (--skip-frontend alone)
   - Invalid combinations rejected with specific error messages
   - Error messages include helpful suggestions

4. Edge cases:
   - Empty intent flags handled
   - Duplicate flags deduplicated or rejected

Follow patterns from Phase 37 tests. Create test fixtures as needed in tests/fixtures/.
  </action>
  <verify>
    <automated>node --test tests/intent-filtering.test.js tests/intent-validation.test.js</automated>
    <manual>Review test output: all 38 tests pass, covering filtering and validation</manual>
    <sampling_rate>run after task commits, before next task begins</sampling_rate>
  </verify>
  <done>38 tests pass (20 filtering + 18 validation), fixture files created</done>
</task>

<task type="auto">
  <name>Task 3: Create team template and review filtering tests</name>
  <files>tests/intent-teams.test.js, tests/intent-review.test.js</files>
  <action>
Create final two test files covering team assembly and review filtering:

**tests/intent-teams.test.js** (15 tests):
1. Template loading:
   - Load intent-teams.yaml correctly
   - Parse intents with agents, domains, mode
   - Handle missing templates gracefully

2. Agent resolution:
   - Resolve primary agents from template
   - Resolve secondary/supporting agents
   - Map intent domains to authority matrix

3. Team assembly:
   - Build team for harden intent (testing + security)
   - Build team for document intent (technical-writer)
   - Build team for security-only (security-engineer only)

4. Mode detection:
   - ad_hoc mode recognized
   - filter_plans mode recognized
   - filter_review mode recognized

**tests/intent-review.test.js** (12 tests):
1. Review panel intent filtering:
   - --just-security filters to security domains only
   - Non-security findings excluded from review
   - Domain-based filtering per authority matrix

2. Ad-hoc review teams:
   - Spawn security-only review team
   - Collect findings from ad-hoc agents
   - Synthesize security audit report

3. Integration with review-panel skill:
   - Intent filtering applied before deduplication
   - Authority filtering still applies after intent filter

Create comprehensive test fixtures: sample plans, intent configurations, authority matrix subsets.
  </action>
  <verify>
    <automated>node --test tests/intent-teams.test.js tests/intent-review.test.js</automated>
    <manual>Review test output: all 27 tests pass, covering teams and review</manual>
    <sampling_rate>run after task commits, before next task begins</sampling_rate>
  </verify>
  <done>27 tests pass (15 teams + 12 review), all 5 test files complete</done>
</task>

</tasks>

<verification>
Full test suite validation:
1. Run all 5 test files together: `node --test tests/intent-*.test.js`
2. Expected: 80+ tests passing
3. All tests cover INTENT-01 through INTENT-06 requirements
4. Test fixtures are complete and reusable for implementation plans
</verification>

<success_criteria>
- 5 test files exist in tests/ directory
- 80+ tests total, all passing
- Tests cover: flag parsing, filtering, validation, teams, review
- Test fixtures created for intent teams, plans, authority matrix
- Tests establish behavioral contracts for implementation
</success_criteria>

<output>
After completion, create `.planning/phases/38-intent-driven-execution/38-00-SUMMARY.md`
</output>
