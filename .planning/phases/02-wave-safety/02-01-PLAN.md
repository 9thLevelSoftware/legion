---
phase: 02-wave-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/plan-critique/SKILL.md
  - tests/plan-critique-overlap.test.js
  - tests/fixtures/plans-wave-overlap/wave1-plan-a.md
  - tests/fixtures/plans-wave-overlap/wave1-plan-b.md
  - tests/fixtures/plans-wave-overlap/wave1-plan-c-no-overlap.md
files_forbidden:
  - skills/phase-decomposer/SKILL.md
  - skills/wave-executor/SKILL.md
  - agents/
  - commands/
  - adapters/
expected_artifacts:
  - path: skills/plan-critique/SKILL.md
    provides: Updated plan-critique with Section 6 cross-plan overlap detection
    required: true
  - path: tests/plan-critique-overlap.test.js
    provides: Test suite for file overlap detection across same-wave plans
    required: true
autonomous: false
agents:
  - engineering-senior-developer
requirements:
  - DSC-04
user_setup: []
verification_commands:
  - grep -q "Section 6" skills/plan-critique/SKILL.md
  - grep -q "Wave File Overlap" skills/plan-critique/SKILL.md
  - grep -q "BLOCKER" skills/plan-critique/SKILL.md
  - test -f tests/plan-critique-overlap.test.js
  - node --experimental-vm-modules node_modules/.bin/jest tests/plan-critique-overlap.test.js --no-coverage 2>&1 | grep -q "passed"

must_haves:
  truths:
    - "Plan-critique has a new Section 6 that reads all plans in a phase, groups by wave, and detects files_modified overlaps"
    - "Overlapping files_modified between same-wave plans triggers BLOCKER severity"
    - "Section 3 (Critique Report) is updated to include wave overlap findings"
    - "Test suite covers: overlap detected, no overlap, directory prefix matching, multi-wave isolation"
    - "Existing Section 5 schema checks are not altered"
  artifacts:
    - path: "skills/plan-critique/SKILL.md"
      provides: "Updated plan-critique with cross-plan overlap detection"
      min_lines: 450
      contains: "Wave File Overlap"
    - path: "tests/plan-critique-overlap.test.js"
      provides: "Overlap detection test suite"
      min_lines: 80
      contains: "BLOCKER"
  key_links:
    - from: "skills/plan-critique/SKILL.md"
      to: "skills/wave-executor/SKILL.md"
      via: "Plan-critique overlap detection replaces wave-executor's runtime warning with planning-time BLOCKER"
      pattern: "files_modified"
---

<objective>
Add a new Section 6 "Wave File Overlap Detection" to the plan-critique skill that reads all plans in a phase, groups them by wave number, and checks for overlapping `files_modified` entries between plans in the same wave. Overlaps are flagged as BLOCKER severity. Create a test suite validating the detection logic.

Purpose: Shift file overlap detection from a runtime warning in wave-executor to a planning-time BLOCKER in plan-critique, catching conflicts before execution begins (DSC-04).
Output: Updated `skills/plan-critique/SKILL.md`, test suite, and fixtures.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-wave-safety/02-CONTEXT.md

Relevant source files:
@skills/plan-critique/SKILL.md (modify — add Section 6)
@skills/wave-executor/SKILL.md (read-only — reference Section 1 Principle 8 and Section 2 Step 5d for existing overlap warning)
@tests/plan-schema-conformance.test.js (read-only — reference for test patterns and fixture approach)
@tests/fixtures/plan-valid-v6.md (read-only — reference for plan fixture format)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Section 6 to plan-critique skill</name>
  <files>skills/plan-critique/SKILL.md</files>
  <action>
In `skills/plan-critique/SKILL.md`, add a new **Section 6: Wave File Overlap Detection (DSC-04)** between Section 5 (Schema Conformance Check) and the References section. This section defines a cross-plan validation rule that detects when two or more plans in the same wave have overlapping `files_modified` entries.

Section 6 content must include:

**When to Run**: Runs as part of plan-critique after Section 5 (schema conformance) and before Section 1 (pre-mortem). Also runnable standalone on a phase directory.

**Algorithm**:
```
Step 1: Collect all plan files in the phase directory
  - Read all {NN}-{PP}-PLAN.md files
  - Parse YAML frontmatter for: wave number, files_modified list

Step 2: Group plans by wave number
  - Build a map: wave_number → [{ plan_id, files_modified }]
  - Only waves with 2+ plans need overlap checking

Step 3: For each wave with multiple plans, check for overlaps
  - For each pair of plans (A, B) in the same wave:
    - Compare files_modified lists using PREFIX MATCHING:
      a. Exact path match: "skills/foo/SKILL.md" == "skills/foo/SKILL.md" → overlap
      b. Directory prefix: if plan A has "skills/foo/" (trailing /) and plan B has
         "skills/foo/SKILL.md", the directory covers the file → overlap
      c. Reverse directory: if plan B has "skills/foo/" and plan A has a file
         under that directory → overlap
    - If any overlap found: record as BLOCKER finding

Step 4: Format findings
  For each overlap:
  - Severity: BLOCKER
  - Message: "Plans {NN}-{PP1} and {NN}-{PP2} both modify {file} in Wave {N}"
  - Mitigation: "Move one plan to a different wave, or merge into a single plan"
```

**Integration with Section 3 (Critique Report)**:
- Wave overlap BLOCKERs are included in the "Schema Conformance" table in Section 3 Step 3
- Add a new row type: "Wave Overlap" with plan pair and file(s)
- Any wave overlap BLOCKER triggers automatic REWORK verdict (same as schema BLOCKERs)

Also update Section 3 Step 1 to include wave overlap findings in the merge:
- Add "Wave file overlap BLOCKERs (Section 6)" to the list of findings merged

Also update Section 3 Step 2 to include wave overlap BLOCKERs in the REWORK trigger:
- "REWORK: Any schema BLOCKER OR any wave overlap BLOCKER OR 3+ critical items..."

**Edge Cases**:
- Single-plan waves: Skip (no overlap possible)
- Plans with empty files_modified: Skip (nothing to overlap)
- Plans across different waves: No overlap check (waves are sequential)
- Directory-level entries in files_modified (e.g., "agents/"): Use prefix matching — any file under that directory overlaps with the directory entry

> verification: grep -q "Section 6" skills/plan-critique/SKILL.md
> verification: grep -q "Wave File Overlap" skills/plan-critique/SKILL.md
> verification: grep -q "PREFIX MATCHING" skills/plan-critique/SKILL.md
> verification: grep -q "wave overlap" skills/plan-critique/SKILL.md
  </action>
  <verify>
grep -q "Section 6" skills/plan-critique/SKILL.md
grep -q "Wave File Overlap" skills/plan-critique/SKILL.md
grep -q "PREFIX MATCHING" skills/plan-critique/SKILL.md
grep -q "wave overlap" skills/plan-critique/SKILL.md
  </verify>
  <done>Plan-critique has Section 6 with wave file overlap detection algorithm, BLOCKER severity, prefix matching, and integration with Section 3 critique report.</done>
</task>

<task type="auto">
  <name>Task 2: Create test fixtures and test suite</name>
  <files>tests/plan-critique-overlap.test.js, tests/fixtures/plans-wave-overlap/wave1-plan-a.md, tests/fixtures/plans-wave-overlap/wave1-plan-b.md, tests/fixtures/plans-wave-overlap/wave1-plan-c-no-overlap.md</files>
  <action>
Create test fixtures and a test suite for the wave file overlap detection logic.

**Fixtures** — create directory `tests/fixtures/plans-wave-overlap/` with three minimal plan files:

1. `wave1-plan-a.md` — Wave 1 plan with `files_modified: [skills/foo/SKILL.md, skills/bar/SKILL.md]`
2. `wave1-plan-b.md` — Wave 1 plan with `files_modified: [skills/foo/SKILL.md, tests/new-test.js]` (overlaps with plan-a on `skills/foo/SKILL.md`)
3. `wave1-plan-c-no-overlap.md` — Wave 1 plan with `files_modified: [commands/new-command.md]` (no overlap with either)

Each fixture needs valid YAML frontmatter with at minimum: `phase`, `plan`, `wave`, `files_modified`, `verification_commands: ["echo ok"]`.

**Test suite** — create `tests/plan-critique-overlap.test.js` following the pattern from `tests/plan-schema-conformance.test.js`. Use `fs.readFileSync` to load fixtures and a `parseYamlFrontmatter` helper to extract frontmatter.

Implement a `detectWaveOverlaps(plans)` function that takes an array of `{ planId, wave, filesModified }` objects and returns an array of overlap findings `{ severity, planA, planB, file, wave, message }`.

Test cases (minimum 8):

1. **Overlap detected** — plans A and B in same wave share a file → returns BLOCKER finding
2. **No overlap** — plans A and C in same wave share no files → returns empty array
3. **Multiple overlaps** — two plans share 2+ files → returns one finding per overlapping file
4. **Directory prefix matching** — plan A has `skills/foo/SKILL.md`, plan B has `skills/foo/` → overlap detected
5. **Cross-wave no overlap** — plan A in wave 1, plan B in wave 2, same file → no overlap (waves are sequential)
6. **Single plan wave** — only one plan in a wave → skip, no overlap possible
7. **Empty files_modified** — plan with empty array → no overlap possible
8. **Three plans, pairwise check** — plans A, B, C all in wave 1; A overlaps B but not C → exactly 1 finding

> verification: test -f tests/plan-critique-overlap.test.js
> verification: test -d tests/fixtures/plans-wave-overlap
> verification: test -f tests/fixtures/plans-wave-overlap/wave1-plan-a.md
> verification: node --experimental-vm-modules node_modules/.bin/jest tests/plan-critique-overlap.test.js --no-coverage 2>&1 | grep -q "passed"
  </action>
  <verify>
test -f tests/plan-critique-overlap.test.js
test -d tests/fixtures/plans-wave-overlap
test -f tests/fixtures/plans-wave-overlap/wave1-plan-a.md
node --experimental-vm-modules node_modules/.bin/jest tests/plan-critique-overlap.test.js --no-coverage 2>&1 | grep -q "passed"
  </verify>
  <done>Test suite with 8+ test cases validates overlap detection including prefix matching, cross-wave isolation, and edge cases. All tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Section 6 exists in plan-critique/SKILL.md with wave overlap detection algorithm
- [ ] Overlap detection uses prefix matching (directory entries match files under them)
- [ ] Overlaps produce BLOCKER severity findings
- [ ] Section 3 Step 1 includes wave overlap findings in merge
- [ ] Section 3 Step 2 includes wave overlap BLOCKERs in REWORK trigger
- [ ] Test fixtures created in tests/fixtures/plans-wave-overlap/
- [ ] Test suite has 8+ test cases covering overlap, no-overlap, prefix matching, cross-wave, edge cases
- [ ] All tests pass
- [ ] No changes made to files outside the plan's files_modified list
</verification>

<success_criteria>
- Plan-critique Section 6 detects overlapping files_modified between same-wave plans as BLOCKER
- Prefix matching handles directory-level entries in files_modified
- Section 3 critique report includes wave overlap findings
- Test suite validates all detection paths with 8+ passing tests
- Existing plan-critique Sections 1-5 are unchanged except for Section 3 merge/verdict updates
</success_criteria>

<output>
After completion, create `.planning/phases/02-wave-safety/02-01-SUMMARY.md`

Update STATE.md to note Plan 02-01 completion.
</output>
