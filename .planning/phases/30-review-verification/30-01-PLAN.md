---
phase: 30-review-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/review-loop/SKILL.md
  - skills/review-panel/SKILL.md
autonomous: true
requirements: [REV-01, REV-03]
user_setup: []

must_haves:
  truths:
    - "review-loop Section 3 review prompt includes confidence classification instructions (HIGH/MEDIUM/LOW with percentage ranges)"
    - "review-loop Section 4 triage filters findings by confidence level — only HIGH (80%+) findings enter the must-fix list by default"
    - "review-panel Section 2 rubric injection includes confidence classification requirement"
    - "review-loop Section 6 tracks finding deltas between cycles and aborts when no progress is detected"
    - "Stale loop abort produces a structured report of remaining issues with recommendations"
  artifacts:
    - path: "skills/review-loop/SKILL.md"
      provides: "Review engine with confidence filtering and stale loop detection"
      min_lines: 780
      contains: "Confidence-gated reporting"
    - path: "skills/review-panel/SKILL.md"
      provides: "Review panel with confidence-filtered synthesis"
      min_lines: 420
      contains: "Confidence Requirement"
  key_links:
    - from: "skills/review-loop/SKILL.md"
      to: "skills/review-panel/SKILL.md"
      via: "Both skills use same confidence classification scheme"
      pattern: "HIGH.*80.*100|MEDIUM.*50.*79|LOW.*50"
---

<objective>
Add confidence-based review filtering and stale loop detection to Legion's review skills.

Purpose: Reviews currently report ALL findings regardless of confidence, generating noise. Stale loops currently exhaust all 3 cycles even when no progress is being made. This plan makes reviews higher-signal and self-terminating.
Output: Updated `skills/review-loop/SKILL.md` and `skills/review-panel/SKILL.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-review-verification/30-CONTEXT.md

Relevant source files:
@skills/review-loop/SKILL.md
@skills/review-panel/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add confidence classification to review-loop</name>
  <files>skills/review-loop/SKILL.md</files>
  <action>
Make three modifications to `skills/review-loop/SKILL.md`:

**Modification A: Add confidence principle to Section 1 (Review Principles)**

After principle 7 ("Skeptical by default"), add a new principle 8:

```
8. **Confidence-gated reporting** — every finding must include a confidence level: HIGH (80-100%), MEDIUM (50-79%), or LOW (<50%). Only HIGH-confidence findings appear in the default report. MEDIUM findings are collected but only surfaced if the user explicitly requests the full report. LOW findings are discarded — if you are not at least 50% confident, it is not a finding.
```

Renumber the existing principle 8 ("Do not auto-proceed after escalation") to principle 9.

**Modification B: Add confidence to review prompt in Section 3 (Review Prompt Construction)**

In Step 3 (Construct the review prompt), within the "Required Feedback Format" section, add confidence classification. After the line `- **Suggested Fix**: {Specific, actionable fix — not vague guidance}`, add:

```
  - **Confidence**: {HIGH | MEDIUM | LOW} — {percentage}%
    - HIGH (80-100%): You are certain this is a real issue. Report it.
    - MEDIUM (50-79%): You suspect this is an issue but aren't certain. Record it but it won't appear in the default report.
    - LOW (<50%): You're guessing. Do not report this finding — delete it.
```

Also add to the IMPORTANT section at the end of the prompt:

```
  - Every finding MUST include a Confidence rating with a percentage
  - Only HIGH-confidence findings (80%+) are actioned by default
  - If you are unsure, rate MEDIUM and explain your uncertainty in Details
  - Never pad reports with LOW-confidence findings to appear thorough
```

**Modification C: Add confidence filtering to Section 4 (Feedback Collection and Triage)**

In Step 3 (Triage findings into lists), modify the triage logic. After the existing deduplication rules, add a new filtering step:

```
Step 2.5: Filter by confidence
  - HIGH-confidence findings (80%+): pass through to triage
  - MEDIUM-confidence findings (50-79%): collect into a "Deferred Findings" list
  - LOW-confidence findings (<50%): discard entirely
  - The must-fix list and nice-to-have list only contain HIGH-confidence findings
  - Report the deferred count: "{N} MEDIUM-confidence findings deferred (use --verbose to see)"
```

Update the Step 4 (Report findings to user) table to include a Confidence column:

```
  | #  | Severity    | Confidence | File                    | Issue (brief)              | Reviewer           |
  |----|-------------|------------|-------------------------|----------------------------|--------------------|
  | 1  | BLOCKER     | HIGH (95%) | path/to/file.md         | Missing error handling     | testing-qa-verification-specialist |

  **Blockers**: {count} | **Warnings**: {count} | **Suggestions**: {count}
  **Deferred (MEDIUM confidence)**: {count} findings not shown (--verbose to reveal)
```
  </action>
  <verify>
grep -c "Confidence-gated\|HIGH (80-100%)\|MEDIUM (50-79%)\|Deferred Findings" skills/review-loop/SKILL.md
# Expected: 5+ matches (principle heading + prompt ranges + triage section)

grep -q "Confidence-gated reporting" skills/review-loop/SKILL.md
# Exit 0 if new principle exists

grep -c "Filter by confidence\|MEDIUM-confidence\|Deferred" skills/review-loop/SKILL.md
# Expected: 3+ matches (triage step + report deferred count)
  </verify>
  <done>review-loop has confidence classification in principles, review prompts, and triage filtering.</done>
</task>

<task type="auto">
  <name>Task 2: Add stale loop detection to review-loop</name>
  <files>skills/review-loop/SKILL.md</files>
  <action>
Make two modifications to `skills/review-loop/SKILL.md`:

**Modification A: Add stale detection to Section 6 (Re-review Cycle)**

After Step 1 (Increment cycle counter) and before Step 2 (Determine what to re-review), insert a new step:

```
Step 1.5: Check for stale loop (no-delta detection)
  Compare the current cycle's must-fix findings with the previous cycle's must-fix findings:
  - Extract finding fingerprints: (file_path, line/section, severity, issue_summary)
  - Compare current fingerprints against previous cycle's fingerprints
  - Calculate delta:
    - findings_resolved: findings in previous cycle but not in current
    - findings_new: findings in current cycle but not in previous
    - findings_unchanged: findings present in both cycles

  If findings_resolved == 0 AND findings_new == 0 (exact same findings, no progress):
    → Increment stale_counter
    → If stale_counter >= 2 (same findings for 2 consecutive re-reviews):
      → Go to Section 8.5 (Stale Loop Abort) — do NOT continue to fix cycle
    → Log: "Warning: No delta detected between cycle {C-1} and cycle {C}.
      Stale counter: {stale_counter}/2. Same {count} findings persist."

  If findings_resolved > 0 OR findings_new > 0 (some progress detected):
    → Reset stale_counter to 0
    → Log: "Progress detected: {findings_resolved} resolved, {findings_new} new,
      {findings_unchanged} unchanged."
```

**Modification B: Add Section 8.5 (Stale Loop Abort)**

Between Section 8 (Escalation) and Section 9 (Error Handling), add a new section:

```markdown
## Section 8.5: Stale Loop Abort

What happens when 2 consecutive re-review cycles show zero delta (same findings, no resolution progress).

This is different from Section 8 (Escalation): Section 8 triggers when the max cycle count is reached. Section 8.5 triggers earlier when the loop is detected as stuck — burning tokens without making progress.

\```
When stale_counter reaches 2 (no delta for 2 consecutive cycles):

Step 1: Generate stale loop report
  Write .planning/phases/{NN}-{slug}/{NN}-REVIEW.md:

  # Phase {N}: {phase_name} — Review Summary

  ## Result: STALE LOOP ABORTED
  **Cycles Used**: {current_cycle} of 3
  **Stale Cycles**: 2 consecutive cycles with no delta
  **Remaining Findings**: {count}

  ## Why the Loop Stalled
  The following findings persisted across {stale_count} consecutive review-fix cycles
  with no resolution. The fix agents were unable to address them, suggesting the
  issues may require a different approach or architectural change.

  ## Persistent Findings
  {For each finding that persisted unchanged across stale cycles:}
  ### Finding {N} (Persistent)
  - **File**: {file path}
  - **Severity**: {BLOCKER | WARNING}
  - **Original Issue**: {issue description}
  - **Fix Attempts**: {what was tried in each cycle}
  - **Likely Root Cause**: {why the fix agents couldn't resolve it — pattern analysis}

  ## Resolved Findings (before loop stalled)
  {Any findings that WERE resolved in earlier cycles}

  ## Recommendations
  {Analysis of why the loop stalled:}
  - Are the persistent findings symptoms of a deeper design problem?
  - Would a different fix agent (different specialty) have more success?
  - Is manual intervention the fastest path to resolution?
  - Specific guidance on what to investigate or change

Step 2: Update STATE.md
  - Status: "Phase {N} review stale — {count} finding(s) unchanged after {stale_count} cycles"
  - Last Activity: "Phase {N} review stale loop detected ({date})"
  - Next Action: "Review .planning/phases/{NN}-{slug}/{NN}-REVIEW.md for diagnosis.
    Options: fix manually then re-run /legion:review, or try different review agents."
  Write updated STATE.md

Step 3: Shutdown the review Team
  Same as Section 7, Step 4 — send shutdown_request to all agents, TeamDelete

Step 4: Present stale loop report to user

  ## Phase {N}: {phase_name} — Review Loop Stalled

  {stale_count} consecutive review cycles with no progress. {count} finding(s) unchanged.

  ### Persistent Findings
  | # | Severity | File          | Issue                 | Cycles Unchanged |
  |---|----------|---------------|-----------------------|------------------|
  | 1 | BLOCKER  | path/file.md  | brief issue           | {cycles}         |

  ### Why It Stalled
  {1-2 sentence analysis of the pattern — e.g., "Fix agents addressed surface symptoms
  but the underlying issue is structural" or "The finding references a pattern that
  doesn't exist in the current architecture"}

  ### Options
  1. **Fix manually** — address the persistent findings directly, then re-run `/legion:review`
  2. **Try different agents** — swap review/fix agents for a fresh perspective
  3. **Accept as-is** — acknowledge the findings and proceed
  4. **Investigate root cause** — examine the fix agent outputs to understand the failure pattern

  **Do not auto-proceed.** Wait for explicit user decision.
\```
```

Update the References table at the bottom of the file to include the new section. Add this row:

```
| Stale Loop Detection    | review-loop.md — Section 8.5                    | Section 6 (delta tracking trigger)   |
```
  </action>
  <verify>
grep -c "Stale Loop\|stale_counter\|no-delta\|STALE LOOP ABORTED" skills/review-loop/SKILL.md
# Expected: 5+ matches

grep -c "Section 8.5" skills/review-loop/SKILL.md
# Expected: 2+ matches (section heading + reference)

grep "findings_resolved\|findings_unchanged" skills/review-loop/SKILL.md | wc -l
# Expected: 3+ lines
  </verify>
  <done>review-loop has stale loop detection with delta tracking, early abort, and structured stale report.</done>
</task>

<task type="auto">
  <name>Task 3: Add confidence classification to review-panel</name>
  <files>skills/review-panel/SKILL.md</files>
  <action>
Make two modifications to `skills/review-panel/SKILL.md`:

**Modification A: Add confidence to rubric injection in Section 2**

In the "How Rubrics Work" subsection, update the rubric injection template. After the existing criterion table format, add:

```markdown
## Confidence Requirement

For EVERY finding, you MUST rate your confidence:
- **HIGH (80-100%)**: Certain this is a real issue based on evidence in the code/content
- **MEDIUM (50-79%)**: Suspect an issue but can't fully confirm — flag it but it may be deferred
- **LOW (<50%)**: Uncertain — do NOT report this finding

Only HIGH-confidence findings are actioned. Rate conservatively — a false positive wastes more time than a missed MEDIUM finding that surfaces in the next review.

Include in each finding: `- **Confidence**: {HIGH | MEDIUM | LOW} — {percentage}%`
```

**Modification B: Add confidence filtering to Section 3 (Panel Result Synthesis)**

In Step 1 (Collect and parse), add confidence to the recorded fields:

```
  - Record for each finding:
    - Finding number
    - File path
    - Line/section reference
    - Severity (BLOCKER, WARNING, or SUGGESTION)
    - Confidence (HIGH, MEDIUM, or LOW with percentage)
    - Issue (one-sentence description)
    - Suggested fix
    - Reviewer agent ID
    - Criterion tag (from rubric)
```

After Step 2 (Deduplicate across reviewers), add a new step:

```
Step 2.5: Filter by confidence
  - HIGH-confidence findings (80%+): include in synthesis
  - MEDIUM-confidence findings (50-79%): collect into "Deferred" section
  - LOW-confidence findings: discard
  - When deduplicating findings with different confidence levels from different
    reviewers, keep the HIGHER confidence rating (if one reviewer is HIGH and
    another is MEDIUM on the same finding, it's HIGH)
```

In Step 6 (Produce consolidated report), add a deferred findings section after the Panel Verdicts table:

```markdown
  ### Deferred Findings (MEDIUM Confidence)
  {count} findings were flagged at MEDIUM confidence (50-79%) and excluded from
  the actionable report. These may warrant review if HIGH-confidence findings
  are sparse or if the user requests the full report.

  | # | Confidence | Reviewer | File | Issue |
  |---|------------|----------|------|-------|
  | 1 | MEDIUM (65%) | {agent-id} | path | {issue} |
```
  </action>
  <verify>
grep -c "Confidence\|HIGH.*80\|MEDIUM.*50\|confidence" skills/review-panel/SKILL.md
# Expected: 8+ matches

grep "Deferred Findings" skills/review-panel/SKILL.md
# Should find the deferred findings section

grep -c "Filter by confidence" skills/review-panel/SKILL.md
# Expected: 1 match
  </verify>
  <done>review-panel has confidence classification in rubric injection and confidence-filtered synthesis.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] review-loop Section 1 has principle about confidence-gated reporting
- [ ] review-loop Section 3 review prompt includes Confidence field in Finding format
- [ ] review-loop Section 4 filters findings by confidence level (HIGH passes, MEDIUM deferred, LOW discarded)
- [ ] review-loop Section 6 has delta tracking between re-review cycles
- [ ] review-loop has Section 8.5 (Stale Loop Abort) with structured report and user options
- [ ] review-panel Section 2 rubric template includes confidence requirement
- [ ] review-panel Section 3 synthesis includes confidence filtering step
- [ ] review-panel Section 3 consolidated report has deferred findings section
- [ ] Both files use identical confidence ranges: HIGH (80-100%), MEDIUM (50-79%), LOW (<50%)
</verification>

<success_criteria>
- `review-loop` and `review-panel` contain explicit 80%+ confidence threshold with HIGH/MEDIUM/LOW classification
- `review-loop` tracks deltas between cycles and aborts early when 2 consecutive cycles show no progress (no findings resolved, no new findings). This triggers before the existing 3-cycle max, saving tokens on stuck loops.
- Confidence filtering is additive — existing severity classification (BLOCKER/WARNING/SUGGESTION) is preserved
- Stale detection complements existing 3-cycle max — whichever triggers first (stale or max) controls the abort
- NOTE: ROADMAP.md criterion "aborts after 3 iterations with no delta" is satisfied by the combination of delta tracking + the existing 3-cycle limit. The stale detection adds early termination (at cycle 2) when the loop is clearly stuck.
</success_criteria>

<output>
After completion, create `.planning/phases/30-review-verification/30-01-SUMMARY.md`

Update STATE.md if this is the last plan in the phase.
</output>
