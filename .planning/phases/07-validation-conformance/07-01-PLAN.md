---
phase: 07-validation-conformance
plan: 07-01
type: execute
wave: 1
depends_on: []
files_modified:
  - adapters/ADAPTER.md
  - adapters/claude-code.md
  - adapters/codex-cli.md
  - adapters/cursor.md
  - adapters/copilot-cli.md
  - adapters/gemini-cli.md
  - adapters/amazon-q.md
  - adapters/windsurf.md
  - adapters/opencode.md
  - adapters/aider.md
files_forbidden:
  - commands/
  - skills/
  - agents/
  - tests/
sequential_files: []
expected_artifacts:
  - path: adapters/ADAPTER.md
    provides: Updated adapter spec with max_prompt_size and known_quirks fields
    required: true
autonomous: false
agents:
  - engineering-senior-developer
requirements:
  - VAL-04
user_setup: []
verification_commands:
  - grep -q 'max_prompt_size' adapters/ADAPTER.md
  - grep -q 'known_quirks' adapters/ADAPTER.md
  - >-
    grep -c 'max_prompt_size' adapters/claude-code.md adapters/codex-cli.md
    adapters/cursor.md adapters/copilot-cli.md adapters/gemini-cli.md
    adapters/amazon-q.md adapters/windsurf.md adapters/opencode.md
    adapters/aider.md | grep -v ':0$' | wc -l | xargs test 9 -eq
  - >-
    grep -c 'known_quirks' adapters/claude-code.md adapters/codex-cli.md
    adapters/cursor.md adapters/copilot-cli.md adapters/gemini-cli.md
    adapters/amazon-q.md adapters/windsurf.md adapters/opencode.md
    adapters/aider.md | grep -v ':0$' | wc -l | xargs test 9 -eq
must_haves:
  truths:
    - >-
      ADAPTER.md Required Fields table includes max_prompt_size with type and
      description
    - >-
      ADAPTER.md Required Fields table includes known_quirks with type and
      description
    - All 9 adapter files have max_prompt_size in YAML frontmatter
    - All 9 adapter files have known_quirks in YAML frontmatter
    - max_prompt_size values are realistic integers (not placeholders)
  artifacts:
    - path: adapters/ADAPTER.md
      provides: Updated adapter specification
      min_lines: 100
      contains: max_prompt_size
  key_links:
    - from: adapters/ADAPTER.md
      to: adapters/claude-code.md
      via: spec compliance
      pattern: max_prompt_size
---

<objective>
Add `max_prompt_size` and `known_quirks` fields to the ADAPTER.md specification and update all 9 adapter files with accurate values.

Purpose: Extend the adapter contract so consumers (skills, commands, wave-executor) can query prompt size limits and CLI-specific quirks at runtime.
Output: Updated ADAPTER.md spec + all 9 adapter files with new fields populated.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-validation-conformance/07-CONTEXT.md
@adapters/ADAPTER.md

Relevant source files:
@adapters/claude-code.md
@adapters/codex-cli.md
@adapters/cursor.md
@adapters/copilot-cli.md
@adapters/gemini-cli.md
@adapters/amazon-q.md
@adapters/windsurf.md
@adapters/opencode.md
@adapters/aider.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add max_prompt_size and known_quirks to ADAPTER.md spec</name>
  <files>adapters/ADAPTER.md</files>
  <action>
Read `adapters/ADAPTER.md`. In the "Required Fields" section, under "### CLI Identity (YAML frontmatter)" or as a new subsection "### Limits & Quirks (YAML frontmatter)", add two new required fields:

| Field | Type | Description |
|-------|------|-------------|
| `max_prompt_size` | integer | Maximum prompt size in tokens that this CLI reliably handles. Used by wave-executor to warn when agent prompts approach the limit. Approximate values acceptable -- use the CLI's documented context window or practical safe limit. |
| `known_quirks` | list of strings | CLI-specific behavioral quirks that skills and commands should account for. Examples: "no-parallel-agents", "no-streaming", "prompt-prefix-only", "no-mcp-support". Empty list `[]` is valid for CLIs with no known quirks. |

Also add a brief paragraph in the "Adding a New CLI Adapter" section noting that these fields must be populated for new adapters.

> verification: grep -q 'max_prompt_size' adapters/ADAPTER.md
> verification: grep -q 'known_quirks' adapters/ADAPTER.md
  </action>
  <verify>
grep -q 'max_prompt_size' adapters/ADAPTER.md
grep -q 'known_quirks' adapters/ADAPTER.md
  </verify>
  <done>ADAPTER.md spec includes max_prompt_size and known_quirks as required fields with type and description.</done>
</task>

<task type="auto">
  <name>Task 2: Update all 9 adapter files with accurate field values</name>
  <files>adapters/claude-code.md, adapters/codex-cli.md, adapters/cursor.md, adapters/copilot-cli.md, adapters/gemini-cli.md, adapters/amazon-q.md, adapters/windsurf.md, adapters/opencode.md, adapters/aider.md</files>
  <action>
Read each of the 9 adapter .md files. Add `max_prompt_size` and `known_quirks` to the YAML frontmatter of each file, placed after the `detection` block.

Use these values (based on documented CLI capabilities as of early 2026):

**claude-code.md** (Claude Code — certified, Opus/Sonnet models):
```yaml
max_prompt_size: 180000
known_quirks: []
```

**codex-cli.md** (OpenAI Codex CLI — beta):
```yaml
max_prompt_size: 128000
known_quirks:
  - "sandbox-execution-only"
  - "no-interactive-prompts"
```

**cursor.md** (Cursor IDE — beta):
```yaml
max_prompt_size: 128000
known_quirks:
  - "ide-embedded-agent"
  - "tab-completion-conflicts"
```

**copilot-cli.md** (GitHub Copilot CLI — experimental):
```yaml
max_prompt_size: 64000
known_quirks:
  - "no-agent-spawning"
  - "prompt-prefix-only"
  - "short-context-window"
```

**gemini-cli.md** (Google Gemini CLI — beta):
```yaml
max_prompt_size: 1000000
known_quirks:
  - "no-structured-messaging"
```

**amazon-q.md** (Amazon Q Developer — experimental):
```yaml
max_prompt_size: 128000
known_quirks:
  - "no-agent-spawning"
  - "aws-scoped-context"
```

**windsurf.md** (Windsurf/Codeium — experimental):
```yaml
max_prompt_size: 128000
known_quirks:
  - "ide-embedded-agent"
  - "cascade-flow-model"
```

**opencode.md** (OpenCode — experimental):
```yaml
max_prompt_size: 128000
known_quirks:
  - "no-agent-spawning"
  - "terminal-ui-only"
```

**aider.md** (Aider — experimental):
```yaml
max_prompt_size: 128000
known_quirks:
  - "no-agent-spawning"
  - "no-parallel-execution"
  - "single-session-only"
  - "no-mcp-support"
```

IMPORTANT: Read each file first to understand the existing frontmatter structure. Insert the new fields after the `detection:` block but before the closing `---`. Do not modify any existing fields or markdown body content.

> verification: grep -c 'max_prompt_size' adapters/claude-code.md adapters/codex-cli.md adapters/cursor.md adapters/copilot-cli.md adapters/gemini-cli.md adapters/amazon-q.md adapters/windsurf.md adapters/opencode.md adapters/aider.md | grep -v ':0$' | wc -l | xargs test 9 -eq
> verification: grep -c 'known_quirks' adapters/claude-code.md adapters/codex-cli.md adapters/cursor.md adapters/copilot-cli.md adapters/gemini-cli.md adapters/amazon-q.md adapters/windsurf.md adapters/opencode.md adapters/aider.md | grep -v ':0$' | wc -l | xargs test 9 -eq
  </action>
  <verify>
grep -c 'max_prompt_size' adapters/claude-code.md adapters/codex-cli.md adapters/cursor.md adapters/copilot-cli.md adapters/gemini-cli.md adapters/amazon-q.md adapters/windsurf.md adapters/opencode.md adapters/aider.md | grep -v ':0$' | wc -l | xargs test 9 -eq
grep -c 'known_quirks' adapters/claude-code.md adapters/codex-cli.md adapters/cursor.md adapters/copilot-cli.md adapters/gemini-cli.md adapters/amazon-q.md adapters/windsurf.md adapters/opencode.md adapters/aider.md | grep -v ':0$' | wc -l | xargs test 9 -eq
  </verify>
  <done>All 9 adapter files have max_prompt_size and known_quirks in their YAML frontmatter with accurate, non-placeholder values.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ADAPTER.md spec includes max_prompt_size field definition with type integer
- [ ] ADAPTER.md spec includes known_quirks field definition with type list of strings
- [ ] All 9 adapter files have max_prompt_size in YAML frontmatter
- [ ] All 9 adapter files have known_quirks in YAML frontmatter
- [ ] max_prompt_size values are positive integers (not zero, not strings)
- [ ] known_quirks values are lowercase-hyphenated strings
- [ ] No existing adapter fields or markdown body content was modified
</verification>

<success_criteria>
- ADAPTER.md Required Fields table includes max_prompt_size and known_quirks
- All 9 adapter files pass frontmatter validation for new fields
- No regressions in existing adapter content
</success_criteria>

<output>
After completion, create `.planning/phases/07-validation-conformance/07-01-SUMMARY.md`

Update STATE.md with plan completion status.
</output>
