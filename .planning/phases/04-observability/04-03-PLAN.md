---
phase: 04-observability
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - tests/observability-summary.test.js
  - tests/observability-cycle-delta.test.js
files_forbidden:
  - skills/agent-registry/SKILL.md
  - skills/wave-executor/SKILL.md
  - skills/phase-decomposer/SKILL.md
  - skills/review-loop/SKILL.md
  - CLAUDE.md
expected_artifacts:
  - tests/observability-summary.test.js
  - tests/observability-cycle-delta.test.js
autonomous: false
agents: [testing-qa-verification-specialist]
requirements: [OBS-01, OBS-02, OBS-03]
user_setup: []
verification_commands:
  - test -f tests/observability-summary.test.js
  - test -f tests/observability-cycle-delta.test.js
  - node --test tests/observability-summary.test.js
  - node --test tests/observability-cycle-delta.test.js

must_haves:
  truths:
    - "Tests validate the Agent Selection Rationale section format in SUMMARY.md skill template"
    - "Tests validate the Phase Decision Summary format in wave-executor"
    - "Tests validate the Cycle Delta section format in REVIEW.md skill template"
    - "Tests validate the two-tier fingerprint classification logic"
    - "Tests validate graceful degradation: autonomous tasks omit rationale, single-cycle reviews omit delta"
    - "Tests use the same pattern as existing plan-schema-conformance.test.js and sequential-files.test.js"
  artifacts:
    - path: "tests/observability-summary.test.js"
      provides: "Tests for Agent Selection Rationale format and Phase Decision Summary in wave-executor"
      min_lines: 40
      contains: "Agent Selection Rationale"
    - path: "tests/observability-cycle-delta.test.js"
      provides: "Tests for Cycle Delta format, fingerprint logic, and sample scenario validation"
      min_lines: 50
      contains: "Cycle Delta"
  key_links:
    - from: "tests/observability-summary.test.js"
      to: "skills/wave-executor/SKILL.md"
      via: "Tests validate that SUMMARY.md template contains required observability sections"
      pattern: "Agent Selection Rationale"
    - from: "tests/observability-cycle-delta.test.js"
      to: "skills/review-loop/SKILL.md"
      via: "Tests validate that REVIEW.md template contains Cycle Delta and fingerprint logic"
      pattern: "Cycle Delta"
---

<objective>
Create test suites for the observability features added in Plans 04-01 and 04-02. Tests validate the format and content of Agent Selection Rationale in SUMMARY.md, Phase Decision Summary in wave-executor, and Cycle Delta in REVIEW.md.

Purpose: Ensure observability features have regression-safe validation, matching the roadmap success criteria "Tests verify decision log format and cycle diff output."
Output: Two test files covering all three OBS requirements.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-observability/04-CONTEXT.md
@.planning/phases/04-observability/04-01-PLAN.md
@.planning/phases/04-observability/04-02-PLAN.md
@skills/wave-executor/SKILL.md
@skills/agent-registry/SKILL.md
@skills/review-loop/SKILL.md
@tests/plan-schema-conformance.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create observability-summary.test.js</name>
  <files>tests/observability-summary.test.js</files>
  <action>
Create `tests/observability-summary.test.js` following the pattern of existing test files (e.g., `tests/plan-schema-conformance.test.js`).

The test file should validate:

1. **Agent Selection Rationale section exists in wave-executor SUMMARY.md template**:
   - Read `skills/wave-executor/SKILL.md`
   - Assert it contains "Agent Selection Rationale" as a section heading or template element
   - Assert it contains the candidate comparison table headers: "Candidate", "Semantic", "Heuristic", "Memory", "Total", "Source"
   - Assert it contains "Task type detected", "Confidence", "Adapter", "Model tier" fields

2. **Phase Decision Summary section exists in wave-executor**:
   - Assert wave-executor contains "Phase Decision Summary"
   - Assert it contains "decision_capture"
   - Assert table headers include: "Plan", "Agent", "Confidence", "Adapter", "Model Tier", "Escalations"

3. **Score Export structure exists in agent-registry**:
   - Read `skills/agent-registry/SKILL.md`
   - Assert it contains "Score Export" section
   - Assert it contains the required fields: "score_export", "semantic_score", "heuristic_score", "memory_boost", "total_score", "recommendation_source"

4. **Graceful degradation documented**:
   - Assert wave-executor contains guidance for omitting rationale when `autonomous: true`
   - Assert the omission language is present (e.g., "omit" and "autonomous")

5. **Score data contract in phase-decomposer**:
   - Read `skills/phase-decomposer/SKILL.md`
   - Assert it contains "Score Data for Observability"
   - Assert it references "score_export"

Use `node:test` and `node:assert` (built-in Node.js test runner).

> verification: test -f tests/observability-summary.test.js
> verification: node --test tests/observability-summary.test.js
  </action>
  <verify>
test -f tests/observability-summary.test.js
node --test tests/observability-summary.test.js
  </verify>
  <done>observability-summary.test.js validates Agent Selection Rationale format, Phase Decision Summary, Score Export structure, graceful degradation, and score data contract.</done>
</task>

<task type="auto">
  <name>Task 2: Create observability-cycle-delta.test.js</name>
  <files>tests/observability-cycle-delta.test.js</files>
  <action>
Create `tests/observability-cycle-delta.test.js` following the existing test pattern.

The test file should validate:

1. **Cycle Comparison section exists in review-loop**:
   - Read `skills/review-loop/SKILL.md`
   - Assert it contains "Cycle Comparison" substep
   - Assert it contains "cycle_delta" data structure
   - Assert all five classification types are present: "findings_resolved", "findings_new", "findings_unchanged", "findings_downgraded", "findings_upgraded"

2. **Two-tier fingerprint strategy documented**:
   - Assert review-loop contains "location fingerprint" or "Location fingerprint"
   - Assert review-loop contains "full fingerprint" or "Full fingerprint"
   - Assert the location fingerprint description excludes severity from the key

3. **Cycle Delta section exists in REVIEW.md template**:
   - Assert it contains "Cycle Delta" section heading
   - Assert it contains "Progression Summary" subsection
   - Assert it contains "Findings Resolved", "Findings New", "Findings Unchanged", "Severity Changes"

4. **Single-cycle graceful degradation**:
   - Assert review-loop contains guidance to omit Cycle Delta for single-cycle reviews

5. **Verification guidance with sample scenarios**:
   - Assert review-loop contains "Cycle Delta verification"
   - Assert it contains "Sample test scenario"
   - Assert it contains the invariant formula referencing all five classification types

6. **Escalation and Stale Loop coverage**:
   - Assert review-loop Section 8 or Section 8.5 also references Cycle Delta

Use `node:test` and `node:assert` (built-in Node.js test runner).

> verification: test -f tests/observability-cycle-delta.test.js
> verification: node --test tests/observability-cycle-delta.test.js
  </action>
  <verify>
test -f tests/observability-cycle-delta.test.js
node --test tests/observability-cycle-delta.test.js
  </verify>
  <done>observability-cycle-delta.test.js validates Cycle Comparison logic, two-tier fingerprint, Cycle Delta template, graceful degradation, verification guidance, and escalation coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Verify all existing tests still pass</name>
  <files>tests/</files>
  <action>
Run the full test suite to verify no regressions:

```bash
node --test tests/
```

If any existing test fails, investigate whether the failure is pre-existing or caused by Phase 4 changes.

Since Plan 04-03 does NOT modify any skill files (they are in files_forbidden), regressions should not occur. This task is a safety check.

> verification: node --test tests/
  </action>
  <verify>
node --test tests/
  </verify>
  <done>Full test suite passes with no regressions from Phase 4 observability changes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `tests/observability-summary.test.js` exists and passes
- [ ] `tests/observability-cycle-delta.test.js` exists and passes
- [ ] All existing tests in `tests/` still pass
- [ ] Tests cover all three OBS requirements (OBS-01, OBS-02, OBS-03)
- [ ] Tests validate graceful degradation (autonomous tasks, single-cycle reviews)
</verification>

<success_criteria>
- Two new test files validate all observability features from Plans 04-01 and 04-02
- Tests follow existing test patterns (node:test, node:assert, read skill files, check content)
- All existing tests pass without regression
- Roadmap success criteria "Tests verify decision log format and cycle diff output" is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-observability/04-03-SUMMARY.md`
</output>
