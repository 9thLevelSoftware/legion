---
phase: 04-observability
plan: 04-02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/review-loop/SKILL.md
files_forbidden:
  - skills/agent-registry/SKILL.md
  - skills/wave-executor/SKILL.md
  - skills/phase-decomposer/SKILL.md
  - CLAUDE.md
  - tests/
expected_artifacts: []
autonomous: false
agents:
  - testing-evidence-collector
requirements:
  - OBS-02
user_setup: []
verification_commands:
  - grep -q "Cycle Delta" skills/review-loop/SKILL.md
  - grep -q "findings_resolved" skills/review-loop/SKILL.md
  - grep -q "findings_new" skills/review-loop/SKILL.md
  - grep -q "findings_unchanged" skills/review-loop/SKILL.md
must_haves:
  truths:
    - >-
      review-loop SKILL.md re-review section tracks which findings appeared or
      disappeared between cycles
    - >-
      REVIEW.md template includes a 'Cycle Delta' section showing resolved, new,
      and unchanged findings
    - >-
      Cycle Delta includes a progression summary showing trend from cycle 1 to
      final cycle
    - >-
      For single-cycle reviews (no re-review), the Cycle Delta section is
      omitted
    - >-
      Finding identity uses a two-tier fingerprint: location fingerprint (file +
      location + issue_summary_hash) for cross-cycle identity, full fingerprint
      (+ severity) for exact-match stale-loop detection
  artifacts: []
  key_links:
    - from: skills/review-loop/SKILL.md (re-review)
      to: skills/review-loop/SKILL.md (REVIEW.md template)
      via: Cycle comparison data feeds into Cycle Delta section of final REVIEW.md
      pattern: Cycle Delta
---

<objective>
Add cycle-over-cycle diff tracking to the review loop, showing what changed between review cycles in the final REVIEW.md output.

Purpose: Make review progress visible — users can see which findings were resolved, which are new, and which persisted across cycles.
Output: Updated review-loop skill with cycle comparison logic and Cycle Delta section in REVIEW.md template.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-observability/04-CONTEXT.md
@skills/review-loop/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cycle comparison logic to re-review section</name>
  <files>skills/review-loop/SKILL.md</files>
  <action>
In `skills/review-loop/SKILL.md`, extend Section 6 (Re-review Cycle) to track finding lifecycle across cycles.

After the existing Step 2 (Determine what to re-review), add a **Cycle Comparison** substep:

```markdown
### Cycle Comparison (for Observability)

Before dispatching the re-review, compare current cycle findings against the previous cycle:

1. **Fingerprint each finding using two-tier strategy**:
   - **Location fingerprint** (for cross-cycle identity matching): `{file}:{line_range}:{issue_summary_hash}`
     Used by Cycle Delta to determine if the same finding exists across cycles, regardless of severity changes.
   - **Full fingerprint** (for exact-match stale-loop detection): `{file}:{line_range}:{severity}:{issue_summary_hash}`
     Preserves existing stale-loop behavior in Section 6, Step 1.5 — do NOT modify the existing stale-loop fingerprint.
2. **Classify each finding using location fingerprints**:
   - `resolved`: location fingerprint present in cycle N-1, absent in cycle N (fix was applied)
   - `new`: location fingerprint absent in cycle N-1, present in cycle N (regression or newly discovered)
   - `unchanged`: location fingerprint present in both cycles AND severity unchanged
   - `downgraded`: location fingerprint present in both cycles AND severity LOWER in cycle N (partial fix)
   - `upgraded`: location fingerprint present in both cycles AND severity HIGHER in cycle N (issue worsened)
3. **Store the classification** for use in REVIEW.md generation (Step 7):
   ```
   cycle_delta:
     cycle: {N}
     vs_cycle: {N-1}
     findings_resolved: [{fingerprint, file, issue}]
     findings_new: [{fingerprint, file, issue}]
     findings_unchanged: [{fingerprint, file, issue}]
     findings_downgraded: [{fingerprint, file, issue, old_severity, new_severity}]
     findings_upgraded: [{fingerprint, file, issue, old_severity, new_severity}]
   ```
4. **Accumulate deltas**: If more than 2 cycles, maintain a list of cycle_delta records to show full progression.

The location fingerprint is derived from the existing deduplication key by dropping the severity component. The full fingerprint remains unchanged for stale-loop detection. This is NOT a new matching algorithm — it is a decomposition of the existing fingerprint into two tiers.
```

> verification: grep -q "Cycle Comparison" skills/review-loop/SKILL.md
> verification: grep -q "cycle_delta" skills/review-loop/SKILL.md
> verification: grep -q "findings_resolved" skills/review-loop/SKILL.md
> verification: grep -q "findings_new" skills/review-loop/SKILL.md
> verification: grep -q "findings_unchanged" skills/review-loop/SKILL.md
  </action>
  <verify>
grep -q "Cycle Comparison" skills/review-loop/SKILL.md
grep -q "cycle_delta" skills/review-loop/SKILL.md
grep -q "findings_resolved" skills/review-loop/SKILL.md
grep -q "findings_new" skills/review-loop/SKILL.md
grep -q "findings_unchanged" skills/review-loop/SKILL.md
  </verify>
  <done>review-loop Section 6 extended with Cycle Comparison substep that classifies findings as resolved, new, unchanged, downgraded, or upgraded between cycles.</done>
</task>

<task type="auto">
  <name>Task 2: Add Cycle Delta section to REVIEW.md template</name>
  <files>skills/review-loop/SKILL.md</files>
  <action>
In `skills/review-loop/SKILL.md`, extend Section 7 (Review Passed — write REVIEW.md) to include the Cycle Delta section.

After the last existing section in the REVIEW.md template (`## Suggestions (Not Required)`), add:

**NOTE**: There is no `## Cross-Cutting Themes` section in the current template — the actual last section is `## Suggestions (Not Required)`. Insert the Cycle Delta section after that.

```markdown
## Cycle Delta

> This section tracks finding progression across review cycles.
> Omitted for single-cycle reviews where no re-review occurred.

### Progression Summary

| Metric | Cycle 1 | Cycle 2 | ... | Final |
|--------|---------|---------|-----|-------|
| Total findings | {count} | {count} | ... | {count} |
| BLOCKER | {count} | {count} | ... | {count} |
| MUST-FIX | {count} | {count} | ... | {count} |
| SUGGESTION | {count} | {count} | ... | {count} |

### Findings Resolved (fixed between cycles)
| Finding | File | Resolved In |
|---------|------|-------------|
| {issue summary} | {file path} | Cycle {N} |

### Findings New (appeared in later cycles)
| Finding | File | Appeared In | Severity |
|---------|------|-------------|----------|
| {issue summary} | {file path} | Cycle {N} | {severity} |

### Findings Unchanged (persisted across all cycles)
| Finding | File | Severity | Cycles Present |
|---------|------|----------|----------------|
| {issue summary} | {file path} | {severity} | {1, 2, ...N} |

### Severity Changes
| Finding | File | From | To | Cycle |
|---------|------|------|----|-------|
| {issue summary} | {file path} | {old severity} | {new severity} | {N} |
```

Add generation instructions:

```markdown
**Cycle Delta generation rules:**
- If only 1 review cycle occurred (passed on first try), omit the entire "Cycle Delta" section
- If 2+ cycles occurred, populate from the accumulated cycle_delta records (Section 6)
- Empty subsections (e.g., no severity changes) should be omitted rather than shown empty
- The Progression Summary table always shows all cycles that ran
```

Also extend BOTH Section 8 (Escalation) AND Section 8.5 (Stale Loop Abort) to include the
same Cycle Delta section, since both write REVIEW.md files with multi-cycle data worth showing.

**NOTE**: There are two separate sections (Section 8 and Section 8.5), not one combined section.
There is also a duplicate "Section 8" heading for "Authority Conflict Resolution" later in the file —
do NOT modify that section. Only modify the Escalation (Section 8) and Stale Loop Abort (Section 8.5)
sections that write REVIEW.md.

> verification: grep -q "Cycle Delta" skills/review-loop/SKILL.md
> verification: grep -q "Progression Summary" skills/review-loop/SKILL.md
> verification: grep -q "Findings Resolved" skills/review-loop/SKILL.md
> verification: grep -q "Severity Changes" skills/review-loop/SKILL.md
  </action>
  <verify>
grep -q "Cycle Delta" skills/review-loop/SKILL.md
grep -q "Progression Summary" skills/review-loop/SKILL.md
grep -q "Findings Resolved" skills/review-loop/SKILL.md
grep -q "Severity Changes" skills/review-loop/SKILL.md
  </verify>
  <done>REVIEW.md template extended with Cycle Delta section showing progression summary, resolved/new/unchanged findings, and severity changes across review cycles.</done>
</task>

<task type="auto">
  <name>Task 3: Add cycle delta verification guidance</name>
  <files>skills/review-loop/SKILL.md</files>
  <action>
In `skills/review-loop/SKILL.md`, add a verification note at the end of the Cycle Delta template section to help future test writers validate the output.

After the Cycle Delta generation rules, add:

```markdown
**Cycle Delta verification (for test authors):**

To verify cycle delta output is correct, check:
1. Sum of (findings_resolved + findings_new + findings_unchanged + findings_downgraded + findings_upgraded) equals total unique location fingerprints across both cycles (no finding lost or double-counted)
2. All findings_resolved entries had a location fingerprint in at least one prior cycle
3. All findings_new entries had a location fingerprint in NO prior cycle
4. Progression Summary row counts match the sum of detailed findings per severity
5. Severity Changes entries (downgraded/upgraded) have different from/to values
6. Single-cycle reviews produce NO Cycle Delta section at all
7. findings_unchanged entries have identical severity in both cycles

Sample test scenario 1 (basic):
- Cycle 1: 5 findings (2 BLOCKER, 2 MUST-FIX, 1 SUGGESTION)
- Cycle 2: 4 findings (1 BLOCKER resolved, 1 new MUST-FIX added)
- Expected: findings_resolved=1, findings_new=1, findings_unchanged=3, findings_downgraded=0, findings_upgraded=0

Sample test scenario 2 (severity change):
- Cycle 1: 3 findings (1 BLOCKER in file.js:10, 1 MUST-FIX in file.js:20, 1 SUGGESTION in file.js:30)
- Cycle 2: 3 findings (1 MUST-FIX in file.js:10 [was BLOCKER], 1 MUST-FIX in file.js:20, 1 SUGGESTION in file.js:30)
- Expected: findings_resolved=0, findings_new=0, findings_unchanged=2, findings_downgraded=1 (file.js:10 BLOCKER→MUST-FIX), findings_upgraded=0
```

> verification: grep -q "Cycle Delta verification" skills/review-loop/SKILL.md
> verification: grep -q "findings_resolved + findings_new + findings_unchanged" skills/review-loop/SKILL.md
  </action>
  <verify>
grep -q "Cycle Delta verification" skills/review-loop/SKILL.md
grep -q "Sample test scenario" skills/review-loop/SKILL.md
  </verify>
  <done>Cycle Delta verification guidance added with invariant checks and sample test scenario for future test authors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `skills/review-loop/SKILL.md` has Cycle Comparison substep in re-review section
- [ ] `skills/review-loop/SKILL.md` has Cycle Delta section in REVIEW.md template
- [ ] Cycle Delta includes: Progression Summary, Findings Resolved, Findings New, Findings Unchanged, Severity Changes
- [ ] Single-cycle reviews omit Cycle Delta section
- [ ] Escalated reviews also include Cycle Delta section
- [ ] Verification guidance with sample test scenario is present
- [ ] All verification commands pass
</verification>

<success_criteria>
- Review-loop tracks finding lifecycle (resolved/new/unchanged/downgraded/upgraded) across cycles
- REVIEW.md includes Cycle Delta section with progression table and categorized findings
- Single-cycle reviews degrade gracefully (no empty section)
- Cycle delta uses two-tier fingerprint: location fingerprint for identity, full fingerprint for stale-loop
- Verification guidance enables future test creation
</success_criteria>

<output>
After completion, create `.planning/phases/04-observability/04-02-SUMMARY.md`
</output>
