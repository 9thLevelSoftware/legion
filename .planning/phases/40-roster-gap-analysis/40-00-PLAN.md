---
phase: 40-roster-gap-analysis
plan: 40-00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/roster-gap-analysis.test.js
  - tests/fixtures/production-roles.yaml
  - tests/fixtures/agent-coverage-matrix.yaml
autonomous: true
requirements:
  - ROSTER-01
  - ROSTER-02
  - ROSTER-03
  - ROSTER-04
  - ROSTER-05
  - ROSTER-06
must_haves:
  truths:
    - Test suite validates gap detection algorithm
    - Test suite validates coverage matrix generation
    - Test suite validates 52-agent limit enforcement
    - All 40+ tests pass before gap analysis runs
  artifacts:
    - path: tests/roster-gap-analysis.test.js
      provides: Gap analysis test suite
      min_lines: 150
    - path: tests/fixtures/production-roles.yaml
      provides: Production-grade role definitions
      min_lines: 80
    - path: tests/fixtures/agent-coverage-matrix.yaml
      provides: Expected coverage mappings
      min_lines: 60
  key_links:
    - from: tests/roster-gap-analysis.test.js
      to: skills/agent-registry/CATALOG.md
      via: Agent registry parsing
      pattern: catalog\.md|agent-registry
    - from: tests/roster-gap-analysis.test.js
      to: .planning/config/intent-teams.yaml
      via: Intent teams validation
      pattern: intent-teams\.yaml|intent_mappings
---

<objective>
Create comprehensive test scaffolding for roster gap analysis functionality.

Purpose: Ensure the gap analysis engine works correctly before analyzing production roster. Tests validate gap detection, coverage scoring, limit enforcement, and agent creation workflows.
Output: 40+ passing tests across gap detection, coverage analysis, and limit validation.
</objective>

<execution_context>
@skills/agent-registry/CATALOG.md
@skills/agent-registry/SKILL.md
@.planning/config/intent-teams.yaml
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@agents/

## Phase Context

Phase 40 analyzes the 53-agent roster against production-grade role requirements. Current state:
- 53 agents across 9 divisions (exceeds 52-agent limit by 1)
- Missing agents referenced in intent-teams.yaml: engineering-security-engineer, product-technical-writer
- Need to verify SRE, security auditor, data scientist coverage
</context>

<tasks>

<task type="auto">
  <name>Create production-grade role definitions fixture</name>
  <files>tests/fixtures/production-roles.yaml</files>
  <action>
Create tests/fixtures/production-roles.yaml with production-grade role taxonomy:

1. Define 15-20 production-grade roles organized by category:
   - **SRE/DevOps**: sre-reliability-engineer, chaos-engineer, platform-engineer, observability-engineer
   - **Security**: security-engineer (OWASP/STRIDE), compliance-auditor, penetration-tester, pii-specialist
   - **Data/AI**: data-scientist, ml-engineer, data-engineer, ai-ethics-reviewer
   - **Engineering**: senior-developer, backend-architect, frontend-specialist, full-stack-developer
   - **Operations**: incident-responder, runbook-maintainer, cost-optimizer

2. For each role, specify:
   - required_capabilities: list of 3-5 key skills
   - coverage_indicators: patterns that indicate coverage (agent name matches, task type overlaps)
   - priority: critical | important | nice-to-have
   - related_divisions: which Legion divisions might cover this

3. Include metadata section with source references (Google SRE book, OWASP guidelines, etc.)

This fixture serves as the "source of truth" for what production-grade coverage looks like.
  </action>
  <verify>
    <automated>cat tests/fixtures/production-roles.yaml | grep -c "role:" | grep -E "[1-9][0-9]"</automated>
    <manual>Verify YAML structure is valid and roles are comprehensive</manual>
  </verify>
  <done>Fixture exists with 15+ production roles, valid YAML structure, and coverage indicators for each</done>
</task>

<task type="auto">
  <name>Create agent coverage matrix fixture</name>
  <files>tests/fixtures/agent-coverage-matrix.yaml</files>
  <action>
Create tests/fixtures/agent-coverage-matrix.yaml defining expected agent-to-role mappings:

1. Map each of the 53 current agents to their covered production roles
2. For each mapping include:
   - agent_id: kebab-case agent name
   - covers_roles: list of production role IDs this agent handles
   - coverage_strength: full | partial | minimal
   - gaps: list of capabilities this agent is missing for full coverage

3. Identify intentionally uncovered roles (e.g., we don't need a dedicated penetration-tester for most projects)

4. Include section for missing-but-referenced agents:
   - engineering-security-engineer: referenced in intent-teams.yaml, missing from roster
   - product-technical-writer: referenced in intent-teams.yaml, missing from roster

This fixture provides expected outputs for test assertions.
  </action>
  <verify>
    <automated>cat tests/fixtures/agent-coverage-matrix.yaml | grep -c "agent_id:" | grep -E "5[0-9]"</automated>
    <manual>Verify all 53 agents are mapped with coverage data</manual>
  </verify>
  <done>Matrix maps all 53 agents to production roles with coverage strength and gap annotations</done>
</task>

<task type="auto">
  <name>Create comprehensive gap analysis test suite</name>
  <files>tests/roster-gap-analysis.test.js</files>
  <action>
Create tests/roster-gap-analysis.test.js with comprehensive test coverage:

**Section 1: Agent Registry Parsing Tests (8 tests)**
- Test parsing CATALOG.md to extract agent list
- Test division count validation (9 divisions)
- Test agent count tracking (currently 53, limit 52)
- Test task type extraction from registry
- Test duplicate agent detection
- Test malformed agent entry handling

**Section 2: Intent Teams Validation Tests (6 tests)**
- Test that all agents referenced in intent-teams.yaml exist in roster
- Test for orphaned intent mappings (security-engineer, technical-writer)
- Test intent-to-agent resolution
- Test fallback behavior for missing agents
- Test harden intent team composition
- Test security-only intent validation

**Section 3: Gap Detection Algorithm Tests (10 tests)**
- Test production role vs agent coverage comparison
- Test coverage strength scoring (full/partial/minimal)
- Test critical gap identification (missing security-engineer)
- Test important gap identification (missing technical-writer)
- Test nice-to-have gap handling (lower priority)
- Test gap severity classification
- Test false positive prevention (don't flag intentional gaps)
- Test division-level gap aggregation
- Test cross-division coverage analysis
- Test gap deduplication

**Section 4: 52-Agent Limit Tests (8 tests)**
- Test current count vs limit comparison
- Test limit exceeded detection (we're at 53)
- Test agent creation blocking when at limit
- Test agent replacement suggestion logic
- Test limit enforcement in /legion:agent workflow
- Test warning vs error severity based on overage amount
- Test agent count by division tracking
- Test limit compliance reporting

**Section 5: Coverage Analysis Tests (8 tests)**
- Test SRE-equivalent coverage detection (ROSTER-02)
  - Verify support-infrastructure-maintainer covers some SRE tasks
  - Identify gaps: chaos engineering, formal SLO management
- Test security-auditor coverage (ROSTER-03)
  - Verify gap: no dedicated security-engineer for OWASP/STRIDE
  - Check if testing-api-tester covers security testing
- Test data-scientist coverage (ROSTER-04)
  - Verify data-analytics-reporter coverage
  - Identify gap: formal data science, statistical modeling
- Test coverage scoring algorithm
- Test coverage report generation
- Test coverage trend tracking

**Section 6: Integration Tests (6 tests)**
- Test full gap analysis workflow end-to-end
- Test gap report generation format
- Test /legion:agent integration for gap closure
- Test agent creation suggestion logic
- Test gap prioritization for user presentation
- Test analysis output matches fixture expectations

Use the existing test infrastructure (similar to tests/intent-teams.test.js, tests/authority-matrix.test.js). Include descriptive test names and clear assertion messages.
  </action>
  <verify>
    <automated>npm test -- tests/roster-gap-analysis.test.js 2>&1 | tail -5</automated>
    <manual>Verify 40+ tests are defined and all pass</manual>
  </verify>
  <done>Test suite has 40+ tests, all passing, covering gap detection, coverage analysis, and limit enforcement</done>
</task>

</tasks>

<verification>
1. Run full test suite: `npm test -- tests/roster-gap-analysis.test.js`
2. Verify all 40+ tests pass
3. Check fixture files exist and are valid YAML
4. Confirm tests match requirements ROSTER-01 through ROSTER-06
</verification>

<success_criteria>
- tests/roster-gap-analysis.test.js exists with 40+ tests
- tests/fixtures/production-roles.yaml exists with 15+ role definitions
- tests/fixtures/agent-coverage-matrix.yaml exists with 53 agent mappings
- All tests pass (npm test shows 100% success)
- Tests validate gap detection, coverage scoring, and 52-agent limit
</success_criteria>

<output>
After completion, create `.planning/phases/40-roster-gap-analysis/40-00-SUMMARY.md` documenting:
- Test coverage summary (N tests across M categories)
- Key test scenarios covered
- Fixture data overview
</output>
