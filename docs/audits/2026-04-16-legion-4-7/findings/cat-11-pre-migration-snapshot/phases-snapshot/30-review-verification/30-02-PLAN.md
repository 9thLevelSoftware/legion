---
phase: 30-review-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/phase-decomposer/SKILL.md
  - skills/wave-executor/SKILL.md
autonomous: true
requirements: [REV-02]
user_setup: []

must_haves:
  truths:
    - "phase-decomposer Section 6 task template includes `> verification:` inline format with bash command examples"
    - "phase-decomposer Section 1 principle 5 references both `<verify>` blocks and `> verification:` inline format"
    - "wave-executor Section 3 agent execution prompt instructs agents to run `> verification:` commands after each task"
    - "wave-executor Section 4 or Section 5 blocks plan completion when verification commands fail"
    - "The `> verification:` format is grep-able and machine-parseable"
  artifacts:
    - path: "skills/phase-decomposer/SKILL.md"
      provides: "Phase decomposition engine with inline verification format"
      min_lines: 700
      contains: "> verification:"
    - path: "skills/wave-executor/SKILL.md"
      provides: "Wave execution engine with verification enforcement"
      min_lines: 640
      contains: "Verification-gated completion"
  key_links:
    - from: "skills/phase-decomposer/SKILL.md"
      to: "skills/wave-executor/SKILL.md"
      via: "phase-decomposer defines `> verification:` format that wave-executor checks"
      pattern: "> verification:"
---

<objective>
Add machine-checkable `> verification:` inline format to phase plans and enforce verification checking in the wave executor.

Purpose: Tasks currently have `<verify>` blocks, but these are only checked by agents optionally. This plan adds a more visible inline format and makes the wave-executor block task completion when verification commands fail.
Output: Updated `skills/phase-decomposer/SKILL.md` and `skills/wave-executor/SKILL.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-review-verification/30-CONTEXT.md

Relevant source files:
@skills/phase-decomposer/SKILL.md
@skills/wave-executor/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `> verification:` format to phase-decomposer</name>
  <files>skills/phase-decomposer/SKILL.md</files>
  <action>
Make three modifications to `skills/phase-decomposer/SKILL.md`:

**Modification A: Update principle 5 in Section 1 (Decomposition Principles)**

Replace the current principle 5:
```
5. **Concrete verification** — every task has a `<verify>` step with bash commands. No "manually check" or "visually inspect" instructions. If you can not script the check, the task is too vague.
```

With:
```
5. **Concrete verification** — every task has BOTH:
   - `> verification:` inline lines after the task description — machine-checkable commands that can be extracted and run automatically by the wave-executor
   - A `<verify>` block with the same commands in executable form
   No "manually check" or "visually inspect" instructions. If you cannot script the check, the task is too vague.
```

**Modification B: Update the task template in Section 6 (Plan File Template)**

In the template, update the `<task>` block to include `> verification:` lines. Replace the existing task template:

````markdown
<task type="auto">
  <name>Task {N}: {descriptive name}</name>
  <files>{files to create or modify}</files>
  <action>
{Detailed, unambiguous instructions for what to do.
Include format examples, specific content guidance,
and enough detail that the executor does not need to guess.}

> verification: {bash command that proves task 1 success — e.g., test -f path/to/file.md}
> verification: {second check — e.g., grep -q "expected string" path/to/file.md}
  </action>
  <verify>
{Same verification commands from above in executable form:}
test -f path/to/file.md
grep -q "expected string" path/to/file.md
  </verify>
  <done>{Single sentence describing the completed state.}</done>
</task>
````

**Modification C: Add verification format guidance to Section 6 "Writing Detailed Task Actions"**

After the existing "Bad action / Good action" examples, add a new subsection:

```markdown
### Writing Verification Lines

Every task MUST include `> verification:` lines at the end of its `<action>` block. These are machine-readable commands that the wave-executor extracts and runs automatically after task execution.

**Format**: `> verification: {bash command}`

The command must:
- Return exit code 0 on success, non-zero on failure
- Be self-contained (no variables, no prior setup needed)
- Run from the repository root directory

**Good verification lines:**
```
> verification: test -f skills/new-skill/SKILL.md
> verification: grep -q "Section 1:" skills/new-skill/SKILL.md
> verification: wc -l < skills/new-skill/SKILL.md | xargs test 50 -le
> verification: grep -c "## Section" skills/new-skill/SKILL.md | xargs test 3 -le
```

**Bad verification lines:**
```
> verification: echo "looks good"          # Always passes — tests nothing
> verification: cat skills/new-skill/SKILL.md  # Reads but doesn't assert
> verification: open http://localhost:3000  # Not scriptable, requires browser
```

**Relationship to `<verify>` blocks:**
The `> verification:` lines and `<verify>` blocks contain the SAME commands. The difference:
- `> verification:` lines are inline, grep-able (`grep '^> verification:' PLAN.md`), and parsed by wave-executor for automated checking
- `<verify>` blocks are the human-readable executable form used during manual review

Both exist for redundancy — inline format for automation, block format for readability.
```
  </action>
  <verify>
grep -c "> verification:" skills/phase-decomposer/SKILL.md
# Expected: 8+ matches (template examples + guidance examples)

grep "Writing Verification Lines" skills/phase-decomposer/SKILL.md
# Should find the new subsection

grep "machine-checkable\|machine-readable" skills/phase-decomposer/SKILL.md | wc -l
# Expected: 2+ lines
  </verify>
  <done>phase-decomposer task template includes `> verification:` inline format with examples and guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Add verification enforcement to wave-executor</name>
  <files>skills/wave-executor/SKILL.md</files>
  <action>
Make three modifications to `skills/wave-executor/SKILL.md`:

**Modification A: Add verification principle to Section 1 (Execution Principles)**

After principle 10 ("Agents report via SendMessage"), add a new principle:

```
11. **Verification-gated completion** — after each task, the agent MUST run all `> verification:` commands from the task's action block. If any verification command returns a non-zero exit code, the task is marked as failed and the agent must report the failure. Do not proceed to the next task until all verifications pass.
```

**Modification B: Update agent execution prompt in Section 3 (Personality Injection)**

In Step 4 (Construct the agent execution prompt), in the "## Important" section, add verification enforcement instructions. After the existing bullet "Run the <verify> commands after each task to confirm completion before moving on", add:

```
  - CRITICAL: Extract all `> verification:` lines from each task's <action> block and run them as bash commands
  - Each `> verification:` line is a bash command that must return exit code 0
  - If ANY verification command fails (non-zero exit code):
    a. Record the failed command and its output
    b. Attempt to fix the issue (re-read the task, check your work)
    c. Re-run the failed verification
    d. If it still fails after one fix attempt: mark the task as FAILED in your summary
    e. Do NOT skip failed verifications or proceed to the next task
  - Run verifications in order — they may have implicit dependencies
```

Also update the autonomous execution prompt (the second prompt template in Section 3, starting at "For autonomous plans (autonomous: true)"). In that prompt's "## Important" section, after the existing bullet "Run the <verify> commands after each task to confirm completion", add the same 5 CRITICAL verification bullets listed above:

```
  - CRITICAL: Extract all `> verification:` lines from each task's <action> block and run them as bash commands
  - Each `> verification:` line is a bash command that must return exit code 0
  - If ANY verification command fails (non-zero exit code):
    a. Record the failed command and its output
    b. Attempt to fix the issue (re-read the task, check your work)
    c. Re-run the failed verification
    d. If it still fails after one fix attempt: mark the task as FAILED in your summary
    e. Do NOT skip failed verifications or proceed to the next task
  - Run verifications in order — they may have implicit dependencies
```

**Modification C: Add verification checking to Section 5 (Agent Result Processing)**

In Step 1 (Parse the agent's SendMessage content), add verification parsing. After the existing "**Verification**: outputs from <verify> commands" field, add:

```
  - **Verification Commands Run**: count of `> verification:` commands executed
  - **Verification Passed**: count that returned exit code 0
  - **Verification Failed**: count that returned non-zero, with command + output for each
```

In Step 2 (Determine result status), update the decision logic:

```
  - SUCCESS: agent completed all tasks, ALL verification commands passed (exit code 0)
  - COMPLETE WITH WARNINGS: agent completed tasks but some non-critical verify steps had issues
    (e.g., line count slightly below expected but content is correct)
  - FAILED: agent could not complete one or more tasks, OR any `> verification:` command
    returned non-zero exit code and the agent could not fix it after one attempt
```

Add a note:
```
  IMPORTANT: Verification failure is a hard gate. Unlike `<verify>` block outputs which
  are advisory, `> verification:` command failures block plan completion. A plan with
  any failed verification command MUST have Status: Failed or Status: Complete with Warnings
  (never Status: Complete).
```

Update the SUMMARY.md template in Step 3 to include a Verification Commands section:

```
  ## Verification Commands
  | Command | Exit Code | Result |
  |---------|-----------|--------|
  | `test -f path/to/file.md` | 0 | PASS |
  | `grep -q "Section 1:" path/to/file.md` | 0 | PASS |
  | `wc -l < path/to/file.md \| xargs test 50 -le` | 1 | FAIL — file has 42 lines |
```
  </action>
  <verify>
grep -q "Verification-gated completion" skills/wave-executor/SKILL.md
# Exit 0 if new principle 11 exists

grep -c "CRITICAL.*Extract all.*verification" skills/wave-executor/SKILL.md
# Expected: 1+ match (verification enforcement instruction in agent prompt)

grep -q "Verification Commands Run" skills/wave-executor/SKILL.md
# Exit 0 if new SendMessage field exists in Section 5

grep -q "Verification Commands" skills/wave-executor/SKILL.md
# Exit 0 if new SUMMARY.md table section exists
  </verify>
  <done>wave-executor enforces `> verification:` commands with hard failure gating and structured reporting.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] phase-decomposer Section 1 principle 5 mentions both `> verification:` and `<verify>` formats
- [ ] phase-decomposer Section 6 task template includes `> verification:` lines in the `<action>` block
- [ ] phase-decomposer has "Writing Verification Lines" guidance with good/bad examples
- [ ] wave-executor Section 1 has principle about verification-gated completion
- [ ] wave-executor Section 3 agent prompt includes `> verification:` extraction and enforcement instructions
- [ ] wave-executor Section 3 autonomous prompt also includes verification enforcement
- [ ] wave-executor Section 5 parses verification command results and uses them in status determination
- [ ] wave-executor Section 5 SUMMARY.md template includes Verification Commands table
- [ ] The `> verification:` format is consistent between phase-decomposer (producer) and wave-executor (consumer)
</verification>

<success_criteria>
- `phase-decomposer` task template includes `> verification:` format with example commands
- `wave-executor` checks verification commands after task execution and blocks completion on failure
- Verification format is grep-able: `grep '^> verification:' PLAN.md` extracts all verification commands
- Both `> verification:` (inline, machine-parsed) and `<verify>` (block, human-readable) coexist
</success_criteria>

<output>
After completion, create `.planning/phases/30-review-verification/30-02-SUMMARY.md`

Update STATE.md if this is the last plan in the phase.
</output>
