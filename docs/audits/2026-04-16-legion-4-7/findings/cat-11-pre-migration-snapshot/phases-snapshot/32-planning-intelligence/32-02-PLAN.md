---
phase: 32-planning-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - skills/phase-decomposer/SKILL.md
  - commands/plan.md
autonomous: true
requirements: [PLN-01]
user_setup: []

must_haves:
  truths:
    - "phase-decomposer has a 'Competing Architecture Proposals' section documenting 3 philosophy archetypes"
    - "plan command has a step 3.5 that spawns 2-3 agents with labeled philosophies before decomposition"
    - "Users can select an approach, request a hybrid, or skip for simple phases"
    - "The competing proposals step defaults to skip — opt-in, not opt-out. Threshold: 3+ requirements AND architectural choices"
  artifacts:
    - path: "skills/phase-decomposer/SKILL.md"
      provides: "Competing Architecture Proposals section with philosophy definitions and presentation format"
      min_lines: 550
      contains: "Competing Architecture Proposals"
    - path: "commands/plan.md"
      provides: "Updated plan command with step 3.5 for architecture selection"
      min_lines: 240
      contains: "ARCHITECTURE PROPOSALS"
  key_links:
    - from: "commands/plan.md"
      to: "skills/phase-decomposer/SKILL.md"
      via: "Step 3.5 references phase-decomposer's Competing Architecture Proposals section"
      pattern: "phase-decomposer.*Competing"
---

<objective>
Add competing architecture proposals to the planning flow — when planning a phase, spawn 2-3 agents with different architectural philosophies (minimal, clean, pragmatic) that present trade-offs, letting the user select an approach before decomposition proceeds.

Purpose: Implements Feature-dev's pattern of offering competing designs, adapted for Legion's personality-first agent model. Prevents single-perspective planning bias.
Output: Updated `skills/phase-decomposer/SKILL.md`, updated `commands/plan.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-planning-intelligence/32-CONTEXT.md

Relevant source files:
@skills/phase-decomposer/SKILL.md — current 8-section structure, will add Section 2.5
@commands/plan.md — current 11-step process, will add step 3.5
@skills/plan-critique/SKILL.md — reference for agent spawning pattern (read-only Explore agents)
@skills/agent-registry/SKILL.md — for agent selection during proposal generation
@docs/plans/2026-03-02-inspiration-audit-and-adoption.md — Task 6 (competing architecture requirements from Feature-dev)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Competing Architecture Proposals section to phase-decomposer</name>
  <files>skills/phase-decomposer/SKILL.md</files>
  <action>
Insert a new **Section 2.5: Competing Architecture Proposals** between the existing Section 2 (Phase Analysis) and Section 3 (Task Decomposition) in `skills/phase-decomposer/SKILL.md`.

The section should document:

**Introduction:**
Before decomposing into plans, optionally generate competing architecture proposals. This ensures the planning approach isn't biased by a single perspective. Adapted from Feature-dev's 2-3 competing designs pattern.

**When to Generate Proposals:**
```
Complexity check:
- If phase has ≤2 requirements AND only modifies existing markdown files → Skip (too simple)
- If phase has 3+ requirements AND involves architectural choices (new abstractions,
  new patterns, structural decisions) → Offer proposals
- If user explicitly requests → Always generate
- Default is skip — proposals are opt-in, not opt-out
- The orchestrator presents the option; user can always decline
```

**Philosophy Archetypes:**
Define 3 labeled approaches that agents adopt:

| Philosophy | Bias | Strengths | Risks |
|-----------|------|-----------|-------|
| **Minimal** | Least changes, smallest footprint | Low risk, easy to review, fast execution | May miss optimization opportunities, can accumulate tech debt |
| **Clean Architecture** | Proper patterns, separation of concerns, future-proof | Maintainable, extensible, well-structured | Over-engineering risk, more files to modify, longer execution |
| **Pragmatic** | Balanced trade-offs, ship fast but don't cut corners | Good enough for now, adaptable later | May not satisfy purists in either direction |

**Proposal Generation Process:**
```
Step 1: Read phase context
  Use the phase analysis output from Section 2: phase goal, requirements,
  success criteria, existing assets, and constraints.

Step 2: Spawn proposal agents
  Spawn 2-3 agents using the read-only Explore subagent type (same pattern
  as plan-critique Section 4). Each agent receives:
  - The full phase context from Step 1
  - Their assigned philosophy (Minimal, Clean, or Pragmatic)
  - Instructions to produce a structured proposal

  Agent selection:
  - Use agent-registry to match agents with architectural or strategic expertise
  - Preferred: engineering-senior-developer (Clean), engineering-rapid-prototyper (Minimal),
    product-sprint-prioritizer (Pragmatic)
  - For non-engineering phases: match to domain-appropriate agents with similar traits
  - Each agent operates in full personality (personality injection pattern from workflow-common)

  Prompt template for each agent:
  "You are analyzing Phase {N}: {phase_name} from the perspective of the
   {philosophy} philosophy. Given the requirements and context below,
   propose how you would structure the implementation.

   Your proposal must include:
   1. Approach summary (2-3 sentences)
   2. Key decisions and why (3-5 bullet points)
   3. Files you would modify/create (list)
   4. Estimated wave structure (how many waves, what in each)
   5. Trade-offs (what this approach gains and what it sacrifices)
   6. Risk assessment (1-2 main risks of this approach)"

Step 3: Collect and format proposals
  Wait for all agents to return. Format proposals side-by-side:

  ## Architecture Proposals — Phase {N}: {phase_name}

  ### Proposal A: Minimal ({agent_name})
  **Approach:** {summary}
  **Key decisions:**
  - {decision 1}
  - {decision 2}
  **Files:** {list}
  **Waves:** {wave structure}
  **Trade-offs:** {gains vs sacrifices}
  **Risk:** {main risk}

  ### Proposal B: Clean Architecture ({agent_name})
  {same structure}

  ### Proposal C: Pragmatic ({agent_name})
  {same structure}

  ### Comparison
  | Dimension | Minimal | Clean | Pragmatic |
  |-----------|---------|-------|-----------|
  | Files modified | {N} | {N} | {N} |
  | Estimated waves | {N} | {N} | {N} |
  | Risk level | {Low/Med/High} | {Low/Med/High} | {Low/Med/High} |
  | Best for | {scenario} | {scenario} | {scenario} |

Step 4: User selection
  Use AskUserQuestion:

  "Which architecture approach for Phase {N}?"
  Options:
  - "Proposal A: Minimal" — {1-line summary}
  - "Proposal B: Clean Architecture" — {1-line summary}
  - "Proposal C: Pragmatic" — {1-line summary}
  - "Hybrid" — "I want to combine elements from multiple proposals"

  If "Hybrid": ask user which elements from which proposals to combine,
  then synthesize a merged approach.

Step 5: Feed into decomposition
  The selected approach (or hybrid) becomes the architectural direction
  for Section 3 (Task Decomposition). Record the selection in the phase
  CONTEXT.md under "Key Design Decisions":

  "Architecture approach: {selected philosophy} — {rationale}"

  If proposals were skipped, note: "Architecture proposals: skipped (simple phase)"
```

**Cost Profile:**
Proposal agents use the Explore subagent type (read-only, no file modifications). Cost: 2-3 Sonnet-tier agent calls. For budget-conscious users, offer 2 proposals instead of 3, or skip entirely.

**Relationship to Spec Pipeline:**
If the spec pipeline (PLN-02) was run before planning, proposal agents receive the spec document as additional context. This produces better-informed proposals since requirements are already clarified.
  </action>
  <verify>
grep "Competing Architecture Proposals" skills/phase-decomposer/SKILL.md && echo "section present" || echo "MISSING SECTION"
grep "Minimal" skills/phase-decomposer/SKILL.md && echo "Minimal philosophy present" || echo "MISSING MINIMAL"
grep "Clean Architecture" skills/phase-decomposer/SKILL.md && echo "Clean philosophy present" || echo "MISSING CLEAN"
grep "Pragmatic" skills/phase-decomposer/SKILL.md && echo "Pragmatic philosophy present" || echo "MISSING PRAGMATIC"
grep "AskUserQuestion" skills/phase-decomposer/SKILL.md | grep -i "architecture\|approach\|proposal" && echo "user selection gate present" || echo "MISSING SELECTION GATE"
  </verify>
  <done>Phase-decomposer has a new Section 2.5 documenting 3 architecture philosophies, agent spawning, side-by-side presentation, and user selection with hybrid option.</done>
</task>

<task type="auto">
  <name>Task 2: Add step 3.5 (Architecture Proposals) to plan command</name>
  <files>commands/plan.md</files>
  <action>
Insert a new **step 3.5: ARCHITECTURE PROPOSALS** between step 3 (READ PHASE DETAILS) and step 4 (DECOMPOSE INTO PLANS) in `commands/plan.md`.

The step should read:

```
3.5. ARCHITECTURE PROPOSALS (optional)
   Follow phase-decomposer skill Section 2.5 (Competing Architecture Proposals):

   a. Run complexity check:
      - If phase has 1 requirement and ≤2 deliverables: skip to step 4
      - Otherwise: offer proposals

   b. Use AskUserQuestion:
      "Phase {N} has enough complexity to benefit from competing architecture proposals. Generate them?"
      Options:
      - "Yes, generate 2-3 proposals (Recommended for complex phases)"
        Description: "Spawn agents with Minimal, Clean, and Pragmatic philosophies to present trade-offs"
      - "Skip, I know the approach I want"
        Description: "Proceed directly to plan decomposition"

   c. If user selects "Yes":
      - CONTEXT BUDGET NOTE: Proposal agents are spawned as Explore sub-agents
        with their own context windows. The orchestrator passes phase context
        to them and receives only structured proposal summaries back (~200 tokens
        each). This does NOT consume the orchestrator's context budget.
      - Spawn 2-3 read-only (Explore) agents per phase-decomposer Section 2.5
      - Include CODEBASE.md context in agent prompts if it exists (brownfield support)
      - Collect and present proposals side-by-side
      - User selects an approach (or requests hybrid)
      - Record selection in CONTEXT.md
      - Pass selected approach to step 4 as architectural direction

   d. If user selects "Skip":
      - Proceed to step 4 with no architectural constraint
      - Note in CONTEXT.md: "Architecture proposals: skipped by user"
```

Also update the `<execution_context>` block to add `skills/spec-pipeline/SKILL.md` as a conditionally loaded skill (it's not always needed, but should be available when offered).

The existing `<execution_context>` currently lists:
```
skills/workflow-common/SKILL.md
skills/agent-registry/SKILL.md
skills/agent-registry/CATALOG.md
skills/phase-decomposer/SKILL.md
skills/memory-manager/SKILL.md
skills/github-sync/SKILL.md
skills/codebase-mapper/SKILL.md
skills/marketing-workflows/SKILL.md
skills/design-workflows/SKILL.md
skills/plan-critique/SKILL.md
```

Add after `skills/plan-critique/SKILL.md`:
```
skills/spec-pipeline/SKILL.md
```
  </action>
  <verify>
grep "3.5" commands/plan.md && echo "step 3.5 present" || echo "MISSING STEP 3.5"
grep "ARCHITECTURE PROPOSALS" commands/plan.md && echo "architecture section present" || echo "MISSING ARCHITECTURE"
grep "spec-pipeline" commands/plan.md && echo "spec-pipeline referenced" || echo "MISSING SPEC-PIPELINE REF"
grep -c "AskUserQuestion" commands/plan.md | xargs -I{} test {} -ge 3 && echo "3+ AskUserQuestion gates" || echo "INSUFFICIENT GATES"
  </verify>
  <done>Plan command has step 3.5 offering competing architecture proposals with complexity gating and user choice.</done>
</task>

<task type="auto">
  <name>Task 3: Add optional spec pipeline offering to plan command</name>
  <files>commands/plan.md</files>
  <action>
Insert a new **step 3.6: SPEC PIPELINE (optional)** immediately after step 3.5 in `commands/plan.md`.

The step should read:

```
3.6. SPEC PIPELINE (optional)
   Offer the spec creation pipeline for phases that would benefit from
   pre-coding specification. Follows spec-pipeline skill.

   a. Check if a spec already exists:
      - Look for `.planning/specs/{NN}-{phase-slug}-spec.md`
      - If exists: inform user "Spec document already exists for Phase {N}."
        Use AskUserQuestion:
        "Use existing spec or regenerate?"
        - "Use existing" — read spec, pass to step 4 as additional context
        - "Regenerate" — run spec pipeline, overwrite existing
      - If not exists: continue to step b

   b. Offer spec pipeline:
      Use AskUserQuestion:
      "Run spec pipeline before planning Phase {N}?"
      Options:
      - "Yes, create a spec first"
        Description: "5-stage pipeline: gather, research, write, critique, assess. Produces a structured spec document."
      - "No, proceed to planning (Recommended for straightforward phases)"
        Description: "Skip spec creation, decompose directly from ROADMAP requirements"

   c. If user selects "Yes":
      - Read spec-pipeline skill
      - Execute all 5 stages sequentially
      - Write output to `.planning/specs/{NN}-{phase-slug}-spec.md`
      - Pass spec document as additional context to step 4

   d. If user selects "No":
      - Proceed to step 4 with no spec document
```

Renumber or adjust language in subsequent steps as needed to reference the potential spec pipeline output. Specifically, in step 4 (DECOMPOSE INTO PLANS), add a note:

"If a spec document was generated in step 3.6, use it as the primary source for deliverable identification alongside ROADMAP.md requirements."
  </action>
  <verify>
grep "3.6" commands/plan.md && echo "step 3.6 present" || echo "MISSING STEP 3.6"
grep "SPEC PIPELINE" commands/plan.md && echo "spec pipeline section present" || echo "MISSING SPEC PIPELINE"
grep ".planning/specs/" commands/plan.md && echo "spec output path referenced" || echo "MISSING SPEC PATH"
  </verify>
  <done>Plan command offers optional spec pipeline at step 3.6, with existing-spec detection and skip option for simple phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] phase-decomposer has Section 2.5: Competing Architecture Proposals with 3 labeled philosophies
- [ ] phase-decomposer documents agent spawning (read-only Explore), side-by-side presentation, and user selection
- [ ] plan command has step 3.5 (Architecture Proposals) between steps 3 and 4
- [ ] plan command has step 3.6 (Spec Pipeline) between steps 3.5 and 4
- [ ] User can skip both proposals and spec pipeline (not forced)
- [ ] Hybrid option available for architecture selection
- [ ] spec-pipeline/SKILL.md referenced in plan command execution_context
- [ ] No existing plan command steps broken or reordered incorrectly
</verification>

<success_criteria>
- plan command spawns 2-3 agents with labeled philosophies (minimal, clean, pragmatic) and presents trade-offs
- User can select an approach or request a hybrid before planning proceeds
- Spec pipeline is offered as an optional pre-planning step
- Both features are gated behind user choice — never forced
- Complexity check prevents unnecessary proposal generation for simple phases
</success_criteria>

<output>
After completion, create `.planning/phases/32-planning-intelligence/32-02-SUMMARY.md`

Update STATE.md: Plan 32-02 complete, competing architecture proposals and spec pipeline integration added to plan command.
</output>
