---
phase: 01-plan-schema-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/phase-decomposer/SKILL.md
files_forbidden:
  - skills/plan-critique/SKILL.md
  - agents/
  - commands/
  - adapters/
  - tests/
expected_artifacts:
  - path: skills/phase-decomposer/SKILL.md
    provides: Updated plan file template with files_forbidden, expected_artifacts, verification_commands fields
    required: true
autonomous: false
agents:
  - engineering-senior-developer
requirements:
  - DSC-01
  - DSC-02
  - DSC-03
user_setup: []
verification_commands:
  - grep -q "files_forbidden" skills/phase-decomposer/SKILL.md
  - grep -q "expected_artifacts" skills/phase-decomposer/SKILL.md
  - grep -q "verification_commands" skills/phase-decomposer/SKILL.md
  - grep -c "files_forbidden\|expected_artifacts\|verification_commands" skills/phase-decomposer/SKILL.md | xargs test 6 -le

must_haves:
  truths:
    - "Phase-decomposer Section 6 template includes files_forbidden as a YAML array field in plan frontmatter"
    - "Phase-decomposer Section 6 template includes expected_artifacts as a structured object separate from must_haves.artifacts"
    - "Phase-decomposer Section 6 template includes verification_commands as a mandatory YAML array field"
    - "Field guidance table has entries for all three new fields with clear filling instructions"
    - "expected_artifacts semantics are clearly distinguished from must_haves.artifacts (contract vs quality)"
  artifacts:
    - path: "skills/phase-decomposer/SKILL.md"
      provides: "Updated plan template and field guidance"
      min_lines: 750
      contains: "files_forbidden"
  key_links:
    - from: "skills/phase-decomposer/SKILL.md"
      to: "skills/plan-critique/SKILL.md"
      via: "Plan-critique validates the schema fields defined in phase-decomposer"
      pattern: "verification_commands"
---

<objective>
Update the phase-decomposer skill's plan file template (Section 6) to include three new schema fields: `files_forbidden`, `expected_artifacts`, and `verification_commands`. Establish clean separation between `expected_artifacts` (contract: what a plan produces) and `must_haves.artifacts` (quality: how good outputs must be).

Purpose: Define the canonical schema that all future plans must follow, enabling plan-critique enforcement (Plan 01-02) and test validation (Plan 01-03).
Output: Updated `skills/phase-decomposer/SKILL.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-plan-schema-hardening/01-CONTEXT.md
@skills/phase-decomposer/SKILL.md

Relevant source files:
@skills/plan-critique/SKILL.md (read-only reference for how critique will consume the new fields)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new fields to plan frontmatter template</name>
  <files>skills/phase-decomposer/SKILL.md</files>
  <action>
In `skills/phase-decomposer/SKILL.md` Section 6 "Plan File Template", update the YAML frontmatter template block to include three new fields. Insert them after `files_modified` and before `autonomous`:

```yaml
files_forbidden:
  - {list of files/directories this plan MUST NOT modify}
expected_artifacts:
  - path: "{primary output file}"
    provides: "{what it delivers}"
    required: {true if plan fails without this artifact, false if optional}
```

And insert `verification_commands` after `user_setup`:

```yaml
verification_commands:
  - "{bash command 1 that proves plan success — exit 0 on pass}"
  - "{bash command 2}"
```

Ensure the template shows these fields with placeholder values that demonstrate the expected format. The `expected_artifacts` field must be visually and structurally separate from the existing `must_haves.artifacts` field — add a comment in the template clarifying:
- `expected_artifacts`: Contract — declares what outputs this plan produces (existence check)
- `must_haves.artifacts`: Quality — declares how good those outputs must be (content checks)

Additionally, update the existing "Writing Verification Lines" subsection in Section 6 to reference the new plan-level `verification_commands` field. Add a brief note explaining the three-layer verification hierarchy:
1. `> verification:` inline lines in task `<action>` blocks — task-level, grep-able by wave-executor for automated per-task checking
2. `<verify>` blocks per task — same commands in executable form for manual review
3. `verification_commands` in plan frontmatter — plan-level aggregation for go/no-go decisions

Clarify that all three layers should reference the same underlying checks but serve different automation stages.

> verification: grep -q "files_forbidden" skills/phase-decomposer/SKILL.md
> verification: grep -q "expected_artifacts" skills/phase-decomposer/SKILL.md
> verification: grep -q "verification_commands" skills/phase-decomposer/SKILL.md
  </action>
  <verify>
grep -q "files_forbidden" skills/phase-decomposer/SKILL.md
grep -q "expected_artifacts" skills/phase-decomposer/SKILL.md
grep -q "verification_commands" skills/phase-decomposer/SKILL.md
  </verify>
  <done>Phase-decomposer template includes all three new schema fields with proper YAML structure and placeholder values.</done>
</task>

<task type="auto">
  <name>Task 2: Update field guidance table</name>
  <files>skills/phase-decomposer/SKILL.md</files>
  <action>
In `skills/phase-decomposer/SKILL.md` Section 6 subsection "Template Field Guidance", add three new rows to the guidance table:

| Field | How to Fill |
|-------|-------------|
| `files_forbidden` | List of file paths and directory patterns this plan MUST NOT modify. Include files owned by other plans in the same wave (prevents parallel conflicts), shared config files not in scope, and any file the plan reads but should not write. Use glob patterns for directories (e.g., `agents/`, `commands/`). Empty array `[]` is valid if no restrictions apply, but prefer explicit declarations for code-modifying plans. |
| `expected_artifacts` | Structured list of output files this plan produces. Each entry has `path` (file path), `provides` (1-sentence description of what it delivers), and `required` (true/false — true means plan fails without this artifact). This is the **contract** — it declares what outputs exist after execution. Distinct from `must_haves.artifacts` which declares quality checks (min_lines, contains) on those outputs. |
| `verification_commands` | **Mandatory** array of bash commands that prove plan-level success. Each command must return exit code 0 on success, be self-contained (no variables or prior setup), and run from repository root. These are plan-level aggregation checks — they may echo task-level `> verification:` lines but should also include integration checks (e.g., "all generated files exist", "no regressions in test suite"). Plan-critique flags plans missing this field as BLOCKER. |

Also add a subsection after the field guidance table titled "Schema Field Relationships" that explains:

```markdown
### Schema Field Relationships

| Field | Purpose | Enforced By |
|-------|---------|-------------|
| `files_modified` | What this plan WILL touch | Wave executor (scope check) |
| `files_forbidden` | What this plan MUST NOT touch | Plan-critique (overlap detection) |
| `expected_artifacts` | What this plan MUST produce (contract) | Plan-critique (existence check) |
| `must_haves.artifacts` | How good outputs must be (quality) | Review agents (quality check) |
| `verification_commands` | Bash commands proving success | Wave executor (automated run) |

`expected_artifacts` and `must_haves.artifacts` are complementary:
- `expected_artifacts` answers: "Does the output exist?"
- `must_haves.artifacts` answers: "Is the output good enough?"

Both should reference the same files, but serve different validation stages.
```

> verification: grep -q "files_forbidden" skills/phase-decomposer/SKILL.md
> verification: grep -q "Schema Field Relationships" skills/phase-decomposer/SKILL.md
> verification: grep -c "files_forbidden\|expected_artifacts\|verification_commands" skills/phase-decomposer/SKILL.md | xargs test 6 -le
  </action>
  <verify>
grep -q "files_forbidden" skills/phase-decomposer/SKILL.md
grep -q "Schema Field Relationships" skills/phase-decomposer/SKILL.md
grep -c "files_forbidden\|expected_artifacts\|verification_commands" skills/phase-decomposer/SKILL.md | xargs test 6 -le
  </verify>
  <done>Field guidance table has entries for all three new fields, plus a Schema Field Relationships subsection clarifying the contract vs quality distinction.</done>
</task>

<task type="auto">
  <name>Task 3: Add writing guidance for new fields in Section 6</name>
  <files>skills/phase-decomposer/SKILL.md</files>
  <action>
In `skills/phase-decomposer/SKILL.md`, after the existing "Writing Verification Lines" subsection in Section 6, add a new subsection:

```markdown
### Writing files_forbidden Declarations

Every plan that modifies code or skill files SHOULD declare `files_forbidden`. Guidelines:

**What to include:**
- Files owned by other plans in the same wave (prevents parallel write conflicts)
- Shared config files not in this plan's scope (e.g., `package.json` if only modifying skills)
- Agent personality files (unless this plan explicitly modifies agents)
- Command entry points (unless this plan explicitly wires commands)

**What NOT to include:**
- Files that don't exist yet (only forbid existing files)
- Entire repository root (`/`) — be specific
- Files in `files_modified` (contradiction — a file can't be both modified and forbidden)

**Validation rule:** Plan-critique checks that `files_modified` and `files_forbidden` have no overlap. Overlap triggers a BLOCKER.

### Writing expected_artifacts Declarations

Every plan SHOULD declare `expected_artifacts` for its primary outputs. Guidelines:

- List every file that must exist after plan completion
- Set `required: true` for files that are the plan's primary deliverable
- Set `required: false` for auxiliary outputs (logs, temp files, optional docs)
- Use the same paths as `files_modified` but with semantic annotations

**Example:**
```yaml
expected_artifacts:
  - path: "skills/new-skill/SKILL.md"
    provides: "New skill definition with 3 sections"
    required: true
  - path: "commands/new-command.md"
    provides: "Command entry point wiring the new skill"
    required: true
```

**Validation rule:** Plan-critique checks that all `required: true` artifacts appear in `files_modified`. Missing entries trigger a WARNING.
```

> verification: grep -q "Writing files_forbidden" skills/phase-decomposer/SKILL.md
> verification: grep -q "Writing expected_artifacts" skills/phase-decomposer/SKILL.md
  </action>
  <verify>
grep -q "Writing files_forbidden" skills/phase-decomposer/SKILL.md
grep -q "Writing expected_artifacts" skills/phase-decomposer/SKILL.md
  </verify>
  <done>Phase-decomposer has comprehensive writing guidance for files_forbidden and expected_artifacts with examples, rules, and validation references.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `files_forbidden` field present in Section 6 template with YAML array format
- [ ] `expected_artifacts` field present in Section 6 template with structured object format (path, provides, required)
- [ ] `verification_commands` field present in Section 6 template with YAML array format
- [ ] Field guidance table has rows for all three new fields
- [ ] Schema Field Relationships subsection explains contract vs quality distinction
- [ ] Writing guidance subsections added for files_forbidden and expected_artifacts
- [ ] No changes made to files outside `skills/phase-decomposer/SKILL.md`
</verification>

<success_criteria>
- Phase-decomposer Section 6 template includes all three new fields with correct YAML structure
- Field guidance table explains how to fill each new field
- Schema Field Relationships table clarifies the contract/quality distinction
- Writing guidance subsections provide examples and validation rules
- `grep -c "files_forbidden\|expected_artifacts\|verification_commands" skills/phase-decomposer/SKILL.md` returns >= 6
</success_criteria>

<output>
After completion, create `.planning/phases/01-plan-schema-hardening/01-01-SUMMARY.md`

Update STATE.md to note Plan 01-01 completion.
</output>
