---
phase: 04-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/agent-registry/SKILL.md
  - skills/wave-executor/SKILL.md
  - skills/phase-decomposer/SKILL.md
files_forbidden:
  - skills/review-loop/SKILL.md
  - skills/authority-enforcer/SKILL.md
  - CLAUDE.md
  - tests/
expected_artifacts: []
autonomous: false
agents: [engineering-senior-developer]
requirements: [OBS-01, OBS-03]
user_setup: []
verification_commands:
  - grep -q "Agent Selection Rationale" skills/wave-executor/SKILL.md
  - grep -q "Score Breakdown" skills/agent-registry/SKILL.md
  - grep -q "decision_capture" skills/wave-executor/SKILL.md
  - grep -q "score_export" skills/agent-registry/SKILL.md

must_haves:
  truths:
    - "agent-registry SKILL.md Section 3 outputs a structured score breakdown alongside recommendations"
    - "wave-executor SKILL.md Section 5 (Agent Result Processing) SUMMARY.md template includes 'Agent Selection Rationale' section"
    - "Agent Selection Rationale includes: task type detected, top 3 candidates with scores, confidence level, adapter and model tier used"
    - "Score breakdown contains: semantic_score, heuristic_score, memory_boost, total_score, confidence"
    - "Phase completion output in wave-executor includes per-plan decision summary"
    - "If no recommendation was run (autonomous task), the rationale section is omitted silently"
    - "phase-decomposer Section 4 documents what score data is captured for downstream consumers"
  artifacts: []
  key_links:
    - from: "skills/agent-registry/SKILL.md"
      to: "skills/wave-executor/SKILL.md"
      via: "Score breakdown data flows from agent-registry recommendation to wave-executor SUMMARY.md"
      pattern: "score_export"
    - from: "skills/wave-executor/SKILL.md"
      to: "skills/phase-decomposer/SKILL.md"
      via: "Phase-decomposer documents score fields that wave-executor persists"
      pattern: "Agent Selection Rationale"
---

<objective>
Extend the agent recommendation and wave execution pipeline to capture and persist decision data. Agent-registry exports structured score breakdowns, wave-executor logs them in SUMMARY.md, and phase-decomposer documents the data contract.

Purpose: Make agent selection decisions explicit and auditable — users can see WHY an agent was chosen, not just which one.
Output: Updated agent-registry, wave-executor, and phase-decomposer skill files with decision capture and logging.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-observability/04-CONTEXT.md
@skills/agent-registry/SKILL.md
@skills/wave-executor/SKILL.md
@skills/phase-decomposer/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structured score export to agent-registry</name>
  <files>skills/agent-registry/SKILL.md</files>
  <action>
In `skills/agent-registry/SKILL.md`, extend Section 3 (Recommendation Algorithm) to produce a structured score breakdown at Step 4 (Classify confidence).

After the existing confidence classification step, add a **Score Export** subsection:

```markdown
### Score Export

After scoring, produce a structured score breakdown for each recommended agent:

```
score_export:
  task_type_detected: "{extracted task type from intent parsing}"
  candidates:
    - agent_id: "{agent-id}"
      semantic_score: "{quality of semantic overlap: strong|moderate|weak}"
      heuristic_score: {numeric total from tiebreak rules}
      memory_boost: {numeric boost from OUTCOMES.md or 0}
      total_score: {heuristic_score + memory_boost}
      confidence: "{HIGH|MEDIUM|LOW}"
    - agent_id: "{agent-id-2}"
      ...
  adapter: "{adapter name from current CLI}"
  model_tier: "{planning|execution|check}"
  recommendation_source: "{semantic|heuristic|memory|override}"
```

This score breakdown is:
- **Produced** by agent-registry during recommendation
- **Consumed** by wave-executor when writing SUMMARY.md (see OBS-01)
- **Optional** — if recommendation was skipped (autonomous task), no export is generated
```

Also update Step 7 (Enforce mandatory roles) to note that mandatory role additions should be reflected in the score_export with `recommendation_source: "mandatory"`.

> verification: grep -q "Score Export" skills/agent-registry/SKILL.md
> verification: grep -q "score_export" skills/agent-registry/SKILL.md
> verification: grep -q "semantic_score" skills/agent-registry/SKILL.md
> verification: grep -q "heuristic_score" skills/agent-registry/SKILL.md
> verification: grep -q "recommendation_source" skills/agent-registry/SKILL.md
  </action>
  <verify>
grep -q "Score Export" skills/agent-registry/SKILL.md
grep -q "score_export" skills/agent-registry/SKILL.md
grep -q "semantic_score" skills/agent-registry/SKILL.md
grep -q "heuristic_score" skills/agent-registry/SKILL.md
grep -q "recommendation_source" skills/agent-registry/SKILL.md
  </verify>
  <done>agent-registry Section 3 extended with Score Export subsection producing structured breakdown of semantic, heuristic, and memory scores per candidate.</done>
</task>

<task type="auto">
  <name>Task 2: Extend wave-executor SUMMARY.md template with Agent Selection Rationale</name>
  <files>skills/wave-executor/SKILL.md</files>
  <action>
In `skills/wave-executor/SKILL.md`, update Section 5 (Agent Result Processing) to include decision capture.

**IMPORTANT**: The SUMMARY.md template is in Section 5 (Agent Result Processing), NOT Section 7. Section 7 is "Two-Wave Pattern."

**A. Add "Agent Selection Rationale" section to the SUMMARY.md template.**

After the existing `## Result` section (which contains the `**Agent**: {agent-id}` line) and before `## What Was Done`, add:

```markdown
## Agent Selection Rationale

> This section is auto-generated from agent-registry score_export data.
> Omitted for autonomous tasks where no recommendation was run.

| Candidate | Semantic | Heuristic | Memory | Total | Source |
|-----------|----------|-----------|--------|-------|--------|
| {selected agent} | {strong/moderate/weak} | {score} | {boost} | {total} | {semantic/heuristic/memory/mandatory} |
| {runner-up 1} | ... | ... | ... | ... | ... |
| {runner-up 2} | ... | ... | ... | ... | ... |

- **Task type detected**: {task_type_detected from score_export}
- **Confidence**: {HIGH/MEDIUM/LOW}
- **Adapter**: {adapter name}
- **Model tier**: {planning/execution/check}
```

Add a note in the generation instructions:

```markdown
If the plan's `autonomous: true` or no score_export data is available, omit the
"Agent Selection Rationale" section entirely. Do not output an empty section.

**Data source**: The executing agent reconstructs the rationale from the plan's context
(plan frontmatter `agents` field, task descriptions, and agent-registry scoring criteria).
This is an LLM-native approach — the agent re-derives WHY this agent was selected based on
the task requirements and agent-registry's scoring rules, rather than consuming a serialized
data payload. The score_export structure in agent-registry Section 3 defines what fields
to populate, but the executing agent computes these at SUMMARY.md generation time.
```

**B. Add structured decision capture to phase completion output.**

This is a NEW section — no phase completion report currently exists in wave-executor.
Add a new subsection at the end of Section 5 (Agent Result Processing), after all per-plan
SUMMARY.md files are written. Title it "Phase Decision Summary (decision_capture)".

Add the following `decision_capture` block:

```markdown
### Phase Decision Summary

| Plan | Agent | Confidence | Adapter | Model Tier | Escalations |
|------|-------|------------|---------|------------|-------------|
| 01 | {agent-id} | {HIGH/MEDIUM/LOW} | {adapter} | {tier} | {count or "none"} |
| 02 | ... | ... | ... | ... | ... |

Escalations are decisions where an agent encountered an out-of-scope action and
documented it per the authority matrix escalation protocol.
```

> verification: grep -q "Agent Selection Rationale" skills/wave-executor/SKILL.md
> verification: grep -q "decision_capture" skills/wave-executor/SKILL.md
> verification: grep -q "Phase Decision Summary" skills/wave-executor/SKILL.md
> verification: grep -q "Escalations" skills/wave-executor/SKILL.md
  </action>
  <verify>
grep -q "Agent Selection Rationale" skills/wave-executor/SKILL.md
grep -q "decision_capture" skills/wave-executor/SKILL.md
grep -q "Phase Decision Summary" skills/wave-executor/SKILL.md
grep -q "Escalations" skills/wave-executor/SKILL.md
  </verify>
  <done>wave-executor SUMMARY.md template extended with Agent Selection Rationale table and Phase Decision Summary in completion output.</done>
</task>

<task type="auto">
  <name>Task 3: Document score data contract in phase-decomposer</name>
  <files>skills/phase-decomposer/SKILL.md</files>
  <action>
In `skills/phase-decomposer/SKILL.md`, update Section 4 (Agent Recommendation) to document the data contract that downstream consumers (wave-executor) depend on.

After the existing agent recommendation logic, add a subsection:

```markdown
### Score Data for Observability

When recommending agents, the phase-decomposer triggers agent-registry's recommendation
algorithm which produces a `score_export` structure (see agent-registry Section 3, Score Export).

This data flows through the pipeline as follows:

1. **phase-decomposer** → triggers agent-registry recommendation → displays score_export to user
2. **User confirmation** → user sees top candidates with rationale, confirms or swaps
3. **Plan file generation** → selected agent recorded in plan frontmatter `agents` field
4. **wave-executor** → at execution time, the executing agent reconstructs the rationale
   by re-deriving scores from the task description + agent-registry scoring rules (LLM-native
   approach — no serialized data bus needed). The score_export structure defines WHAT to
   populate; the agent computes HOW at SUMMARY.md generation time.

The score_export includes per-candidate:
- `semantic_score` (strong/moderate/weak)
- `heuristic_score` (numeric from tiebreak rules)
- `memory_boost` (numeric from OUTCOMES.md history)
- `total_score` (heuristic + memory)
- `confidence` (HIGH/MEDIUM/LOW)
- `recommendation_source` (semantic/heuristic/memory/mandatory/override)

Plus context fields: `task_type_detected`, `adapter`, `model_tier`.

If the user overrides the recommendation, record `recommendation_source: "override"`.
```

> verification: grep -q "Score Data for Observability" skills/phase-decomposer/SKILL.md
> verification: grep -q "score_export" skills/phase-decomposer/SKILL.md
  </action>
  <verify>
grep -q "Score Data for Observability" skills/phase-decomposer/SKILL.md
grep -q "score_export" skills/phase-decomposer/SKILL.md
  </verify>
  <done>phase-decomposer Section 4 documents score_export data contract and pipeline flow for observability consumers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `skills/agent-registry/SKILL.md` has Score Export subsection with structured breakdown format
- [ ] `skills/wave-executor/SKILL.md` has Agent Selection Rationale section in SUMMARY.md template
- [ ] `skills/wave-executor/SKILL.md` has Phase Decision Summary in completion output
- [ ] `skills/phase-decomposer/SKILL.md` documents score_export data contract
- [ ] Autonomous/no-recommendation case handled (section omitted silently)
- [ ] All verification commands pass
</verification>

<success_criteria>
- Agent-registry produces structured score breakdowns (semantic, heuristic, memory, total, confidence)
- SUMMARY.md includes Agent Selection Rationale with candidate comparison table
- Wave executor captures per-plan decision summary at phase completion
- Phase-decomposer documents the score data contract for the pipeline
- Graceful degradation: no rationale section for autonomous tasks
</success_criteria>

<output>
After completion, create `.planning/phases/04-observability/04-01-SUMMARY.md`
</output>
