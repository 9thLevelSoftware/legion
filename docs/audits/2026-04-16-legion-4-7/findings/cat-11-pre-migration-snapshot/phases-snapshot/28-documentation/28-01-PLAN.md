---
phase: 28-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOC-01, ATR-01]

must_haves:
  truths:
    - "README.md opens with 'Legion' branding, not 'The Agency Workflows'"
    - "The quote 'My name is Legion, for we are many.' appears in the README"
    - "Every command reference uses /legion: namespace with zero /agency: references"
    - "Install instructions use 'claude plugin install legion' (not agency-workflows)"
    - "A 'Shoulders of Giants' entry credits 9thLevelSoftware/agency-agents as origin of the 51 agent personalities"
  artifacts:
    - path: "README.md"
      provides: "Complete Legion-branded README"
      contains: "My name is Legion"
  key_links:
    - from: "README.md install section"
      to: ".claude-plugin/plugin.json"
      via: "plugin name 'legion'"
      pattern: "claude plugin install legion"
    - from: "README.md Shoulders of Giants"
      to: "9thLevelSoftware/agency-agents"
      via: "attribution entry"
      pattern: "9thLevelSoftware/agency-agents"
---

<objective>
Rewrite README.md with complete Legion branding, updated commands, and attribution.

Purpose: README.md is the primary user-facing document. It must present Legion as the project identity with the quote, all /legion: commands, correct install instructions, and a Shoulders of Giants entry crediting the original agent personalities.

Output: Fully rewritten README.md with zero /agency: remnants (except in the Shoulders of Giants section where "Agency" is the name of the original project being credited).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-plugin-manifest/27-01-SUMMARY.md

@README.md
@.claude-plugin/plugin.json
@.claude-plugin/marketplace.json

<interfaces>
<!-- Current install commands from plugin.json (name: "legion") -->
<!-- Install: claude plugin install legion -->
<!-- Marketplace: claude plugin marketplace add 9thLevelSoftware/agency-agents -->
<!-- The repository URL agency-agents is the real GitHub repo name, NOT a branding field -->

From .claude-plugin/plugin.json:
- name: "legion"
- version: "3.0.0"
- repository: "https://github.com/9thLevelSoftware/agency-agents"

10 commands (all /legion: namespace):
/legion:start, /legion:plan, /legion:build, /legion:review, /legion:status,
/legion:quick, /legion:advise, /legion:portfolio, /legion:milestone, /legion:agent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md header, install, getting started, and commands sections</name>
  <files>README.md</files>
  <action>
Rewrite the full README.md file. Use the current README.md as the structural template but replace ALL branding and command references. The complete file must be written in a single Write operation.

**Title and intro (lines 1-7):**
- Title: `# Legion`
- Tagline: `Orchestrate 51 AI specialist personalities as a coordinated legion.`
- Add the quote immediately after the tagline: `> *"My name is Legion, for we are many."*`
- Opening paragraph: Replace "Turn a collection of 51 isolated agent personalities into a functional AI agency. Type `/agency:start`..." with Legion equivalent: "Turn 51 isolated agent personalities into a coordinated legion. Type `/legion:start`..."

**Installation section (lines 9-37):**
- Update install commands:
  - `claude plugin marketplace add 9thLevelSoftware/agency-agents` (unchanged — this is the real repo name)
  - `claude plugin install legion` (was `agency-workflows@agency-workflows`)
- TUI equivalent: `/plugin install legion`
- Local dev: `git clone https://github.com/9thLevelSoftware/agency-agents.git` (unchanged — real repo URL)
- Local dev load: `claude --plugin-dir ./agency-agents`

**Getting started section (lines 39-48):**
- Replace all `/agency:` with `/legion:` in step descriptions

**Commands table (lines 50-63):**
- Replace every `/agency:` with `/legion:` in the command column
- Update descriptions to say "Legion" where "Agency" appeared

**How It Works diagram (lines 65-85):**
- Replace all `/agency:` with `/legion:` in the ASCII workflow diagram

**Workflows section (lines 87-228):**
- Replace all `/agency:` with `/legion:` in every heading, description, and inline reference
- Replace "Agency" with "Legion" in descriptive text (e.g., "Agency projects" -> "Legion projects")

**v2.0 Advisory Features section (lines 243-278):**
- Replace `/agency:advise`, `/agency:plan`, `/agency:review` with `/legion:` equivalents
- Keep the content structure identical

**Shoulders of Giants section (lines 280-365):**
- Change the section intro from "The Agency Workflows didn't invent..." to "Legion didn't invent..."
- Replace "The Agency Workflows introduced" with "Legion introduced" in the "What The Agency Added" subsection
- Rename "What The Agency Added" to "What Legion Added"
- In the comparison table at the end, rename the column header from `**Agency**` to `**Legion**`
- ADD a new dedicated attribution entry BEFORE the existing "From GSD" entry (or as the first subsection after the intro paragraph). This entry credits the original agent personalities:

```markdown
#### The 51 Agent Personalities — [9thLevelSoftware/agency-agents](https://github.com/9thLevelSoftware/agency-agents)

The 51 specialist personalities that power Legion originated in the agency-agents repository by 9thLevelSoftware. These are not generic role labels — they are 80-350 line character sheets with deep expertise, communication styles, hard rules, and personality quirks across 9 divisions. Legion builds orchestration, planning, and review workflows on top of these personalities, but the personalities themselves are the foundation everything else stands on.
```

- IMPORTANT: References to "The Agency Workflows" as a proper noun referring to the ORIGINAL project being credited in the Shoulders of Giants section should be kept as-is where they describe the original project (e.g., "From [GSD]" section can keep saying "We adopted" since it's describing what happened historically). However, forward-looking references like "The Agency Workflows introduced" should become "Legion introduced".

**The 51 Agents section (lines 367-383):**
- Keep as-is (no /agency: references here)

**Architecture section (lines 385-430):**
- Change tree comment from `10 /agency: command entry points` to `10 /legion: command entry points`
- Replace "Agency Workflows" or "Agency" in any descriptive text

**Design Principles section (lines 432-445):**
- No /agency: references here, but replace any "Agency" mentions

**Optional Features section (lines 447-459):**
- Replace `/agency:start`, `/agency:plan`, `/agency:review` with `/legion:` equivalents

**Requirements, Contributing, License sections (lines 461-472):**
- Keep as-is (no changes needed)

**Global search-and-replace verification:** After writing, the ONLY occurrences of "agency" in the file should be:
1. The GitHub repository URL `agency-agents` (real repo name)
2. The `9thLevelSoftware/agency-agents` attribution reference
3. Historical references in the Shoulders of Giants section describing what the ORIGINAL project did (past tense)

Zero occurrences of `/agency:` should remain anywhere in the file.
  </action>
  <verify>
    <automated>cd "C:/Users/dasbl/Documents/agency-agents" &amp;&amp; echo "=== /agency: check ===" &amp;&amp; grep -c "/agency:" README.md &amp;&amp; echo "FAIL: /agency: references found" || echo "PASS: zero /agency: references" &amp;&amp; echo "=== Legion title check ===" &amp;&amp; head -1 README.md | grep -q "# Legion" &amp;&amp; echo "PASS: Legion title" || echo "FAIL: wrong title" &amp;&amp; echo "=== Quote check ===" &amp;&amp; grep -q "My name is Legion, for we are many" README.md &amp;&amp; echo "PASS: quote present" || echo "FAIL: quote missing" &amp;&amp; echo "=== Install check ===" &amp;&amp; grep -q "claude plugin install legion" README.md &amp;&amp; echo "PASS: install command" || echo "FAIL: install command" &amp;&amp; echo "=== Attribution check ===" &amp;&amp; grep -q "9thLevelSoftware/agency-agents" README.md &amp;&amp; echo "PASS: attribution present" || echo "FAIL: attribution missing" &amp;&amp; echo "=== /legion: commands check ===" &amp;&amp; grep -c "/legion:" README.md</automated>
  </verify>
  <done>
README.md opens with "# Legion" and the quote "My name is Legion, for we are many.", all commands use /legion: namespace, install instructions reference plugin name "legion", Shoulders of Giants has a dedicated entry crediting 9thLevelSoftware/agency-agents as origin of the 51 agent personalities, and zero /agency: references exist in the file.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "/agency:" README.md` returns 0
2. `head -1 README.md` shows `# Legion`
3. `grep "My name is Legion" README.md` finds the quote
4. `grep "claude plugin install legion" README.md` finds correct install command
5. `grep "9thLevelSoftware/agency-agents" README.md` finds attribution (in Shoulders of Giants)
6. `grep -c "/legion:" README.md` shows commands are using the correct namespace
</verification>

<success_criteria>
- README.md opens with Legion branding and the quote
- All 10 commands listed as /legion: with zero /agency: references
- Install instructions use `claude plugin install legion`
- Shoulders of Giants section has dedicated entry crediting 9thLevelSoftware/agency-agents as origin of the 51 agent personalities
- File reads coherently as a Legion project document, not a find-and-replace artifact
</success_criteria>

<output>
After completion, create `.planning/phases/28-documentation/28-01-SUMMARY.md`
</output>
