---
phase: 28-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [CLAUDE.md, CONTRIBUTING.md, CHANGELOG.md]
autonomous: true
requirements: [DOC-02, DOC-03, DOC-04]

must_haves:
  truths:
    - "CLAUDE.md title and description say 'Legion', not 'The Agency Workflows'"
    - "CLAUDE.md command table lists /legion: commands with zero /agency: references"
    - "CONTRIBUTING.md title says 'Contributing to Legion'"
    - "CONTRIBUTING.md references /legion: commands throughout"
    - "CHANGELOG.md has a v3.0.0 entry documenting the Legion rebrand"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Legion-branded project instructions"
      contains: "/legion:"
    - path: "CONTRIBUTING.md"
      provides: "Legion-branded contribution guide"
      contains: "/legion:"
    - path: "CHANGELOG.md"
      provides: "v3.0.0 changelog entry"
      contains: "[3.0.0]"
  key_links:
    - from: "CLAUDE.md command table"
      to: "commands/*.md"
      via: "/legion: namespace"
      pattern: "/legion:"
    - from: "CONTRIBUTING.md test commands"
      to: "commands/*.md"
      via: "/legion: namespace"
      pattern: "/legion:"
---

<objective>
Update CLAUDE.md, CONTRIBUTING.md, and CHANGELOG.md with Legion branding.

Purpose: These three supporting documents must align with the README.md rebrand. CLAUDE.md is injected into Claude Code context for every session. CONTRIBUTING.md guides new contributors. CHANGELOG.md documents the version history.

Output: Three updated files with consistent Legion identity and zero /agency: remnants.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@CLAUDE.md
@CONTRIBUTING.md
@CHANGELOG.md

<interfaces>
<!-- Current file sizes for context budget -->
<!-- CLAUDE.md: 77 lines -->
<!-- CONTRIBUTING.md: 113 lines -->
<!-- CHANGELOG.md: 54 lines -->
<!-- Total: ~244 lines across 3 files — well within budget -->

10 commands (all /legion: namespace):
/legion:start, /legion:plan, /legion:build, /legion:review, /legion:status,
/legion:quick, /legion:advise, /legion:portfolio, /legion:milestone, /legion:agent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md with Legion identity</name>
  <files>CLAUDE.md</files>
  <action>
Rewrite CLAUDE.md (77 lines). Use the current structure as template.

**Line 1 — Title:**
`# Legion` (was `# The Agency Workflows`)

**Line 3 — Description:**
`A Claude Code plugin for orchestrating 51 AI specialist personalities as a coordinated legion.` (was "...as coordinated teams.")

**Lines 7-19 — Command table:**
Replace every `/agency:` with `/legion:` in the Command column. There are 10 entries:
- `/legion:start` (was `/agency:start`)
- `/legion:plan <N>` (was `/agency:plan <N>`)
- `/legion:build` (was `/agency:build`)
- `/legion:review` (was `/agency:review`)
- `/legion:status` (was `/agency:status`)
- `/legion:quick <task>` (was `/agency:quick <task>`)
- `/legion:advise` (was `/agency:advise`)
- `/legion:portfolio` (was `/agency:portfolio`)
- `/legion:milestone` (was `/agency:milestone`)
- `/legion:agent` (was `/agency:agent`)

**Lines 22-23 — Project Structure comment:**
Change `10 /agency: command entry points` to `10 /legion: command entry points`

**Lines 48-49 — Workflow diagram:**
Change `/agency:start` -> `/legion:start`, `/agency:plan 1` -> `/legion:plan 1`, etc.

**Lines 51-53 — Workflow descriptions:**
Replace `/agency:plan`, `/agency:build`, `/agency:status`, `/agency:advise` with `/legion:` equivalents.

**Lines 55-62 — Integration paragraphs:**
Replace `/agency:start`, `/agency:plan` with `/legion:` equivalents.

**Lines 64-66 — Memory Layer:**
No `/agency:` references here — leave as-is.

**Lines 68-76 — Conventions:**
No `/agency:` references here — leave as-is.

**Global check:** Zero occurrences of `/agency:` or "Agency Workflows" should remain. The only "agency" should be in the repo URL if mentioned anywhere.
  </action>
  <verify>
    <automated>cd "C:/Users/dasbl/Documents/agency-agents" &amp;&amp; echo "=== /agency: check ===" &amp;&amp; grep -c "/agency:" CLAUDE.md &amp;&amp; echo "FAIL" || echo "PASS: zero /agency:" &amp;&amp; echo "=== Agency Workflows check ===" &amp;&amp; grep -c "Agency Workflows" CLAUDE.md &amp;&amp; echo "FAIL" || echo "PASS: no Agency Workflows" &amp;&amp; echo "=== Title check ===" &amp;&amp; head -1 CLAUDE.md | grep -q "# Legion" &amp;&amp; echo "PASS: Legion title" || echo "FAIL: wrong title" &amp;&amp; echo "=== /legion: count ===" &amp;&amp; grep -c "/legion:" CLAUDE.md</automated>
  </verify>
  <done>
CLAUDE.md title is "# Legion", description says "coordinated legion", all 10 commands use /legion: namespace, zero /agency: or "Agency Workflows" references remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CONTRIBUTING.md with Legion identity</name>
  <files>CONTRIBUTING.md</files>
  <action>
Rewrite CONTRIBUTING.md (113 lines). Use the current structure as template.

**Line 1 — Title:**
`# Contributing to Legion` (was `# Contributing to The Agency Workflows`)

**Lines 19-22 — Testing commands:**
Replace `/agency:start`, `/agency:plan 1` with `/legion:start`, `/legion:plan 1`

**Lines 35 — Structure comment:**
Change `10 command entry points (/agency:*)` to `10 command entry points (/legion:*)`

**Lines 44-46 — Commands section:**
Replace "an `/agency:*` slash command" with "a `/legion:*` slash command"

**Lines 57-58 — Agent creation reference:**
Replace `/agency:agent` with `/legion:agent`

**Lines 74 — Quick test reference:**
Replace `/agency:quick` with `/legion:quick`

**Global sweep:** Every occurrence of `/agency:` becomes `/legion:`. Every occurrence of "The Agency Workflows" becomes "Legion". There should be zero `/agency:` references remaining.
  </action>
  <verify>
    <automated>cd "C:/Users/dasbl/Documents/agency-agents" &amp;&amp; echo "=== /agency: check ===" &amp;&amp; grep -c "/agency:" CONTRIBUTING.md &amp;&amp; echo "FAIL" || echo "PASS: zero /agency:" &amp;&amp; echo "=== Title check ===" &amp;&amp; head -1 CONTRIBUTING.md | grep -q "# Contributing to Legion" &amp;&amp; echo "PASS: Legion title" || echo "FAIL: wrong title" &amp;&amp; echo "=== /legion: count ===" &amp;&amp; grep -c "/legion:" CONTRIBUTING.md</automated>
  </verify>
  <done>
CONTRIBUTING.md title is "Contributing to Legion", all command references use /legion: namespace, zero /agency: or "Agency Workflows" references remain.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add v3.0.0 entry to CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <action>
Update CHANGELOG.md by adding a v3.0.0 entry at the top (after the header lines, before the v2.0.0 entry). Also update the document title from "The Agency Workflows" to "Legion".

**Line 3 — Description:**
Change "All notable changes to The Agency Workflows plugin" to "All notable changes to the Legion plugin"

**New v3.0.0 entry — insert between header and v2.0.0:**

```markdown
## [3.0.0] - 2026-03-02

### Changed
- **Project identity**: Rebranded from "The Agency Workflows" to "Legion" — *"My name is Legion, for we are many."*
- All 10 commands renamed from `/agency:*` to `/legion:*` namespace
- Plugin name changed from `agency-workflows` to `legion` — install via `claude plugin install legion`
- Plugin version bumped to 3.0.0 across plugin.json and marketplace.json
- All 17 skill files updated with `/legion:` command references
- workflow-common shared constants updated to Legion namespace
- README.md, CLAUDE.md, CONTRIBUTING.md rewritten with Legion branding
- Commit message prefixes updated from `feat(agency):` to `feat(legion):`

### Added
- Shoulders of Giants attribution entry crediting 9thLevelSoftware/agency-agents as origin of the 51 agent personalities

### Unchanged
- 51 agent personality files (contain zero "agency" references — no changes needed)
- Repository URL remains `9thLevelSoftware/agency-agents` (actual GitHub repo name)
- All features, workflows, and capabilities remain identical
```

**Existing v2.0.0 and v1.0.0 entries:**
Update the command references in v2.0.0 and v1.0.0 entries from `/agency:` to `/legion:`. These entries describe what was added in those versions, and the commands now have the `/legion:` name. Specifically:
- v2.0.0 "Added" section: `/agency:advise` -> `/legion:advise`, `/agency:plan` -> `/legion:plan`, `/agency:review` -> `/legion:review`
- v2.0.0 "Added" section: `agency-workflows@agency-workflows` -> update to `legion` in the install line
- v1.0.0 "Added" section: all `/agency:` -> `/legion:` for the 9 commands listed
- Update "The Agency Workflows plugin" references to "the Legion plugin" in both entries

Wait — on reflection, historical changelog entries should document what ACTUALLY happened at that version. The commands WERE called /agency: in v1.0 and v2.0. Changing history is dishonest. Instead:
- Keep v1.0.0 and v2.0.0 entries EXACTLY as they are (they document what happened at those versions)
- The v3.0.0 entry documents the rename

Only change the title line (line 3) from "The Agency Workflows" to "Legion" since that's the current project name.
  </action>
  <verify>
    <automated>cd "C:/Users/dasbl/Documents/agency-agents" &amp;&amp; echo "=== v3.0.0 entry check ===" &amp;&amp; grep -q "\[3.0.0\]" CHANGELOG.md &amp;&amp; echo "PASS: v3.0.0 entry exists" || echo "FAIL: v3.0.0 entry missing" &amp;&amp; echo "=== Legion rebrand mention ===" &amp;&amp; grep -q "Legion" CHANGELOG.md &amp;&amp; echo "PASS: Legion mentioned" || echo "FAIL: Legion missing" &amp;&amp; echo "=== Quote check ===" &amp;&amp; grep -q "My name is Legion" CHANGELOG.md &amp;&amp; echo "PASS: quote present" || echo "FAIL: quote missing" &amp;&amp; echo "=== Title check ===" &amp;&amp; grep -q "Legion plugin" CHANGELOG.md &amp;&amp; echo "PASS: Legion in title" || echo "FAIL: title not updated"</automated>
  </verify>
  <done>
CHANGELOG.md has a v3.0.0 entry documenting the Legion rebrand (namespace change, plugin rename, doc rewrite, attribution addition), title line says "Legion plugin", and historical v1.0/v2.0 entries are preserved as-is.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "/agency:" CLAUDE.md` returns 0
2. `grep -c "/agency:" CONTRIBUTING.md` returns 0
3. `grep "[3.0.0]" CHANGELOG.md` finds the v3.0.0 entry
4. `head -1 CLAUDE.md` shows `# Legion`
5. `head -1 CONTRIBUTING.md` shows `# Contributing to Legion`
6. `grep "My name is Legion" CHANGELOG.md` finds the quote in v3.0.0 entry
</verification>

<success_criteria>
- CLAUDE.md has Legion title, description, and all /legion: commands
- CONTRIBUTING.md has Legion title and all /legion: references
- CHANGELOG.md has a v3.0.0 entry that documents the rebrand with the quote, namespace change, and all key changes
- Historical changelog entries (v1.0, v2.0) preserved as-is
- Zero /agency: references in CLAUDE.md or CONTRIBUTING.md
</success_criteria>

<output>
After completion, create `.planning/phases/28-documentation/28-02-SUMMARY.md`
</output>
