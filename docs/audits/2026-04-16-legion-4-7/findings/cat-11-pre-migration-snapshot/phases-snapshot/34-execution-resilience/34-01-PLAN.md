---
phase: 34-execution-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/workflow-common/SKILL.md
  - commands/build.md
autonomous: true
requirements: [EXE-02, EXE-03]
user_setup: []

must_haves:
  truths:
    - "workflow-common contains an '### Auto-Remediation Pattern' section with BLOCKER vs ENVIRONMENT classification table"
    - "workflow-common contains an '### Output Redirection Convention' section with verbose command allowlist table"
    - "workflow-common Auto-Remediation Pattern includes remediation flow with classify, remediate, retry, escalate steps"
    - "workflow-common Auto-Remediation Pattern specifies max 1 remediation attempt per error"
    - "workflow-common Auto-Remediation Pattern references authority matrix for scope limits"
    - "workflow-common Output Redirection Convention includes redirect-to-temp-file pattern with exit code check and last-20-lines on failure"
    - "workflow-common Output Redirection Convention lists specific verbose commands (npm install, pip install, cargo build, go build, docker build)"
    - "workflow-common Output Redirection Convention explicitly excludes test output, linting, and type-check results from redirection"
    - "build.md agent prompt includes '## Execution Resilience' section for both personality and autonomous agents"
    - "build.md Execution Resilience references BLOCKER vs ENVIRONMENT classification and output redirection"
  artifacts:
    - path: "skills/workflow-common/SKILL.md"
      provides: "Execution resilience conventions (auto-remediation + output redirection)"
      contains: "### Auto-Remediation Pattern"
    - path: "commands/build.md"
      provides: "Build command with execution resilience instructions in agent prompts"
      contains: "## Execution Resilience"
  key_links:
    - from: "commands/build.md"
      to: "skills/workflow-common/SKILL.md"
      via: "Agent execution resilience instructions"
      pattern: "Auto-Remediation"
---

<objective>
Add auto-remediation and output redirection conventions to the Legion workflow. Auto-remediation classifies errors as BLOCKER vs. ENVIRONMENT and auto-fixes environment issues (missing deps, wrong versions) instead of failing. Output redirection saves context tokens by redirecting verbose command output to temp files.

Purpose: Makes agent execution self-healing for common environment issues and token-efficient for noisy commands.
Output: Updated `skills/workflow-common/SKILL.md`, updated `commands/build.md`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-execution-resilience/34-CONTEXT.md

Relevant source files:
@skills/workflow-common/SKILL.md — Error Handling Pattern section (to add after), Division Constants section (next section)
@commands/build.md — Step 4.c agent prompt construction (personality and autonomous templates)
@CLAUDE.md — Authority Matrix (defines what agents can do autonomously)
@docs/plans/2026-03-02-inspiration-audit-and-adoption.md — bjarne (environment remediation, output redirection) patterns
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Auto-Remediation Pattern and Output Redirection Convention to workflow-common</name>
  <files>skills/workflow-common/SKILL.md</files>
  <action>
Add two new sections to `skills/workflow-common/SKILL.md` between the existing "## Error Handling Pattern" section and the "## Division Constants" section. Preserve all existing sections unchanged.

**Section: Auto-Remediation Pattern**

Insert after the Error Handling Pattern section (after the line "5. If multiple agents fail in a wave, stop the wave and report all failures"):

```markdown
## Auto-Remediation Pattern

When an agent encounters an error during task execution, it classifies the error before reporting failure. Environment issues are auto-fixed; blockers require human judgment.

### Error Classification

| Type | Indicators | Examples | Action |
|------|-----------|----------|--------|
| BLOCKER | Involves business logic, API design, architectural decisions, or test logic failures | Missing API endpoint, schema mismatch, failing test assertions, dependency conflict between packages | Stop. Report to coordinator with full error context. Do not attempt auto-fix. |
| ENVIRONMENT | Involves missing dependencies, wrong versions, missing directories, or configuration | `MODULE_NOT_FOUND`, missing `node_modules/`, wrong Node version, missing `.env` file, port already in use, missing build directory | Auto-remediate: generate fix, execute, retry. |

### Remediation Flow

```
1. Agent encounters error during task execution
2. Classify: BLOCKER or ENVIRONMENT?
   - BLOCKER indicators: error references business logic, API contracts,
     architecture patterns, or test assertions
   - ENVIRONMENT indicators: error references missing packages, wrong versions,
     missing files/directories, or system configuration
3. If BLOCKER:
   - Stop the current task
   - Include in completion summary: "BLOCKER: {error description}"
   - Continue to next independent task in the plan (if any)
4. If ENVIRONMENT:
   a. Log: "ENVIRONMENT ISSUE: {error}. Attempting remediation..."
   b. Generate remediation command based on error type:
      - MODULE_NOT_FOUND / missing package → npm install (or pip install, etc.)
      - Missing directory → mkdir -p {path}
      - Missing config file → check if template exists, copy it
      - Wrong version → check package.json/requirements.txt for declared version
   c. Execute the remediation command
   d. If remediation succeeds: retry the original step that failed
   e. If remediation fails OR retry also fails: escalate to BLOCKER
   f. Max 1 remediation attempt per unique error — no retry loops
5. Report all remediation actions in the task completion summary:
   "Auto-remediated: {error} → {fix applied} → {retry result}"
```

### Remediation Scope (Authority Matrix)

Auto-remediation is limited to the autonomous scope defined in the CLAUDE.md authority matrix:

| Allowed (autonomous) | Not Allowed (requires human approval) |
|----------------------|--------------------------------------|
| Install dependencies declared in package.json, requirements.txt, Cargo.toml, go.mod | Add NEW dependencies not in any manifest |
| Create directories the codebase expects to exist | Create new architectural directories |
| Run standard build setup (npm install, pip install -r requirements.txt) | Modify build configuration files |
| Set environment variables documented in the project | Change CI/CD or deployment configuration |
| Clear caches or temp files that are safe to regenerate | Delete source files or user data |

If an environment fix would require an action outside autonomous scope, escalate to BLOCKER with the message: "ENVIRONMENT issue requires human approval: {description of needed fix}."
```

**Section: Output Redirection Convention**

Insert immediately after the Auto-Remediation Pattern section:

```markdown
## Output Redirection Convention

Commands known to produce verbose output waste context tokens when captured inline. Agents redirect them to temp files and only surface errors.

### Verbose Commands (always redirect)

| Command Pattern | Why Redirect |
|----------------|-------------|
| `npm install`, `yarn install`, `pnpm install` | Package download/resolution logs are verbose and rarely informative |
| `pip install`, `pip install -r requirements.txt` | Same — dependency resolution output |
| `cargo build`, `cargo test` (initial compile only) | Compilation progress bars and dependency resolution |
| `go build`, `go mod tidy`, `go mod download` | Module download output |
| `mvn install`, `mvn package`, `gradle build` | Java/Kotlin build system output |
| `dotnet build`, `dotnet restore` | .NET build/restore output |
| `docker build`, `docker pull`, `docker compose up` | Layer download and build progress |
| `bundle install`, `gem install` | Ruby dependency installation |

### Never Redirect (informative output)

| Command Pattern | Why Keep Inline |
|----------------|----------------|
| `npm test`, `jest`, `pytest`, `cargo test` (results) | Test results are essential feedback — pass/fail, assertion messages |
| `eslint`, `prettier --check`, `ruff`, `clippy` | Linting output identifies specific issues to fix |
| `tsc --noEmit`, `mypy`, `pyright` | Type-check errors are actionable and concise |
| `git status`, `git diff`, `git log` | Version control state is always relevant |
| Custom scripts and application output | Unknown verbosity — keep visible |

### Redirection Pattern

```
# Redirect verbose command output to temp file
{command} > /tmp/legion-{command-slug}-$(date +%s).log 2>&1
exit_code=$?

if [ $exit_code -ne 0 ]; then
  echo "FAILED: {command} (exit code $exit_code)"
  echo "Last 20 lines of output:"
  tail -20 /tmp/legion-{command-slug}-*.log
else
  echo "OK: {command} completed successfully"
fi
```

### Rules

1. Redirect BOTH stdout and stderr to the temp file (`2>&1`)
2. Always check exit code immediately after the command
3. On success: report one-line confirmation, do NOT read or display the log
4. On failure: display the last 20 lines of the log for diagnosis
5. Never redirect interactive commands, test runners, or linting tools
6. Temp files are ephemeral — do not commit, track, or clean them up
7. If unsure whether a command is verbose: do NOT redirect (safe default is to keep output visible)
```
  </action>
  <verify>
grep "## Auto-Remediation Pattern" skills/workflow-common/SKILL.md && echo "Auto-remediation section present" || echo "MISSING AUTO-REMEDIATION"
grep "## Output Redirection Convention" skills/workflow-common/SKILL.md && echo "Output redirection section present" || echo "MISSING OUTPUT REDIRECTION"
grep "BLOCKER" skills/workflow-common/SKILL.md | wc -l | xargs -I{} test {} -ge 3 && echo "BLOCKER referenced 3+ times" || echo "INSUFFICIENT BLOCKER REFS"
grep "ENVIRONMENT" skills/workflow-common/SKILL.md | wc -l | xargs -I{} test {} -ge 3 && echo "ENVIRONMENT referenced 3+ times" || echo "INSUFFICIENT ENVIRONMENT REFS"
grep "Max 1 remediation attempt" skills/workflow-common/SKILL.md && echo "Max attempts documented" || echo "MISSING MAX ATTEMPTS"
grep "authority matrix" skills/workflow-common/SKILL.md && echo "Authority matrix referenced" || echo "MISSING AUTHORITY REF"
grep "npm install" skills/workflow-common/SKILL.md | wc -l | xargs -I{} test {} -ge 2 && echo "npm install in both sections" || echo "INSUFFICIENT NPM REFS"
grep "tail -20" skills/workflow-common/SKILL.md && echo "Last 20 lines pattern present" || echo "MISSING TAIL PATTERN"
grep "Never redirect" skills/workflow-common/SKILL.md && echo "Never-redirect list present" || echo "MISSING NEVER-REDIRECT"
grep "npm test\|jest\|pytest" skills/workflow-common/SKILL.md && echo "Test commands excluded" || echo "MISSING TEST EXCLUSIONS"
  </verify>
  <done>workflow-common expanded with Auto-Remediation Pattern (BLOCKER vs ENVIRONMENT classification, remediation flow, scope limits per authority matrix) and Output Redirection Convention (verbose command allowlist, never-redirect list, temp file pattern).</done>
</task>

<task type="auto">
  <name>Task 2: Update build command with execution resilience instructions</name>
  <files>commands/build.md</files>
  <action>
Make two updates to `commands/build.md` to inject execution resilience instructions into agent prompts.

**Update 1: Add Execution Resilience section to personality-injected agent prompt**

In Step 4.c, for personality-injected agents (autonomous: false), add a new section after the existing "## Important" block (after line "and document the decision in your summary"). The new section goes between "## Important" and the closing of the prompt (before line "6. Agent name:"):

```markdown
           ## Execution Resilience
           - If you encounter an error, classify it before reporting failure:
             BLOCKER: architecture/design/logic issues → stop, report in summary, move to next task
             ENVIRONMENT: missing deps, wrong versions, missing dirs → auto-remediate and retry
           - Auto-remediation steps:
             1. Log: "ENVIRONMENT ISSUE: {error}. Attempting remediation..."
             2. Generate and execute a fix (npm install, mkdir -p, etc.)
             3. Retry the failed step once
             4. If retry fails: escalate to BLOCKER in your summary
           - Only remediate within autonomous scope (declared deps, expected dirs).
             Do NOT install new unplanned dependencies.
           - For verbose commands (npm/pip/cargo install, docker build, go build):
             redirect output to a temp file, check exit code, only show last 20
             lines if the command failed. Keep test/lint/typecheck output visible.
```

**Update 2: Add Execution Resilience section to autonomous agent prompt**

In Step 4.c, for autonomous agents (autonomous: true), add the same section after the existing "## Important" block (after line "verification command outputs, and any issues encountered"):

```markdown
           ## Execution Resilience
           - If you encounter an error, classify it before reporting failure:
             BLOCKER: architecture/design/logic issues → stop, report in summary, move to next task
             ENVIRONMENT: missing deps, wrong versions, missing dirs → auto-remediate and retry
           - Auto-remediation steps:
             1. Log: "ENVIRONMENT ISSUE: {error}. Attempting remediation..."
             2. Generate and execute a fix (npm install, mkdir -p, etc.)
             3. Retry the failed step once
             4. If retry fails: escalate to BLOCKER in your summary
           - Only remediate within autonomous scope (declared deps, expected dirs).
             Do NOT install new unplanned dependencies.
           - For verbose commands (npm/pip/cargo install, docker build, go build):
             redirect output to a temp file, check exit code, only show last 20
             lines if the command failed. Keep test/lint/typecheck output visible.
```

The content is identical in both prompts — all agents get the same resilience instructions.

**Update 3: Update Step 4.f result parsing for remediation reporting**

In Step 4.f (Process results for each completed agent), add a note after the line "Determine status: Complete | Complete with Warnings | Failed":

```
      - Check the agent's summary for auto-remediation reports:
        If the summary contains "Auto-remediated:" lines, include them in the SUMMARY.md
        under a "## Auto-Remediation" section with what was fixed and the retry result
      - An agent that auto-remediated and succeeded is still "Complete" (not "Complete with Warnings")
      - An agent that escalated an ENVIRONMENT issue to BLOCKER is "Failed" for that task
```

No other changes to build.md. The coordinator flow (wave execution, stopping on failure, etc.) remains unchanged — only the agent prompts and result processing are updated.
  </action>
  <verify>
grep "## Execution Resilience" commands/build.md | wc -l | xargs -I{} test {} -ge 2 && echo "Resilience section in both prompts" || echo "MISSING IN ONE OR BOTH PROMPTS"
grep "BLOCKER" commands/build.md | wc -l | xargs -I{} test {} -ge 2 && echo "BLOCKER referenced in build" || echo "MISSING BLOCKER IN BUILD"
grep "ENVIRONMENT" commands/build.md | wc -l | xargs -I{} test {} -ge 2 && echo "ENVIRONMENT referenced in build" || echo "MISSING ENVIRONMENT IN BUILD"
grep "Auto-remediated" commands/build.md && echo "Remediation reporting present" || echo "MISSING REMEDIATION REPORTING"
grep "auto-remediate and retry" commands/build.md && echo "Remediation instruction present" || echo "MISSING REMEDIATION INSTRUCTION"
grep "verbose commands" commands/build.md && echo "Output redirection instruction present" || echo "MISSING OUTPUT REDIRECTION"
grep "temp file" commands/build.md && echo "Temp file reference present" || echo "MISSING TEMP FILE REF"
grep "last 20" commands/build.md && echo "Last 20 lines reference present" || echo "MISSING LAST 20 LINES"
  </verify>
  <done>Build command updated with Execution Resilience section in both personality and autonomous agent prompts (BLOCKER/ENVIRONMENT classification, auto-remediation instructions, output redirection), plus remediation reporting in result processing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] workflow-common has Auto-Remediation Pattern section with BLOCKER vs ENVIRONMENT classification table
- [ ] workflow-common has Output Redirection Convention section with verbose command allowlist and never-redirect list
- [ ] Auto-Remediation Pattern includes remediation flow with max 1 attempt per error
- [ ] Auto-Remediation Pattern references authority matrix for scope limits
- [ ] Output Redirection Convention includes temp file pattern with exit code check and last-20-lines on failure
- [ ] Output Redirection Convention explicitly excludes test output, linting, and type-check results
- [ ] build.md personality agent prompt includes "## Execution Resilience" section
- [ ] build.md autonomous agent prompt includes "## Execution Resilience" section
- [ ] build.md Execution Resilience references BLOCKER vs ENVIRONMENT classification
- [ ] build.md Execution Resilience references output redirection for verbose commands
- [ ] build.md result processing includes auto-remediation reporting in SUMMARY.md
- [ ] Both new workflow-common sections appear between Error Handling Pattern and Division Constants
</verification>

<success_criteria>
- Missing dependencies detected during execution auto-generate remediation tasks (install, configure) instead of failing
- Commands known to produce verbose output redirect stdout to temp files, check exit codes, and only surface errors
- Auto-remediation respects the authority matrix: only declared deps and expected dirs, never unplanned additions
- Test output, linting, and type-check results are never redirected (they stay visible)
</success_criteria>

<output>
After completion, create `.planning/phases/34-execution-resilience/34-01-SUMMARY.md`

Update STATE.md: Plan 34-01 complete, execution resilience conventions added to workflow-common and build command.
</output>
