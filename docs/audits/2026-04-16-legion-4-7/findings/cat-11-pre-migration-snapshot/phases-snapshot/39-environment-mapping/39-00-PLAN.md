---
phase: 39-environment-mapping
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/environment-mapping.test.js
  - tests/directory-mappings.test.js
  - tests/path-enforcement.test.js
  - tests/fixtures/sample-codebase-mappings.yaml
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - ENV-04
  - ENV-05
must_haves:
  truths:
    - "Test suite validates directory mapping extraction from codebase"
    - "Test suite validates path enforcement against directory mappings"
    - "Test suite validates file placement in correct directories"
    - "Test suite validates auto-update detection logic"
  artifacts:
    - path: "tests/environment-mapping.test.js"
      provides: "Integration tests for environment mapping features"
      min_lines: 80
    - path: "tests/directory-mappings.test.js"
      provides: "Unit tests for directory mapping extraction"
      min_lines: 60
    - path: "tests/path-enforcement.test.js"
      provides: "Unit tests for path enforcement validation"
      min_lines: 60
    - path: "tests/fixtures/sample-codebase-mappings.yaml"
      provides: "Sample directory mappings for tests"
      min_lines: 30
  key_links:
    - from: "tests/environment-mapping.test.js"
      to: "skills/codebase-mapper/SKILL.md"
      via: "mock analysis of directory structures"
      pattern: "directory.*mapping"
---

<objective>
Create comprehensive test scaffolding for Phase 39 — Environment Mapping features. This includes tests for directory mapping extraction, path enforcement validation, and auto-update detection.

Purpose: Ensure all environment mapping features work correctly before implementation, following the TDD approach established in Phase 37-00 and Phase 38-00.
Output: 4 test files with 90+ passing tests covering directory mappings, path enforcement, and auto-update logic.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/codebase-mapper/SKILL.md
@tests/authority-matrix.test.js
@tests/intent-flag-parsing.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory mapping extraction tests</name>
  <files>tests/directory-mappings.test.js</files>
  <action>
    Create `tests/directory-mappings.test.js` with 30+ unit tests for directory mapping extraction logic:

    Test categories:
    1. Standard location detection (10 tests)
       - Detect routes directory (app/routes/, src/routes/, pages/api/)
       - Detect tests directory (tests/, __tests__/, *.test.* co-location)
       - Detect components directory (src/components/, app/components/)
       - Detect config directory (config/, .config/)
       - Detect types directory (types/, src/types/)
       - Detect utils directory (utils/, src/utils/, lib/)
       - Detect services directory (services/, src/services/)
       - Detect middleware directory (middleware/, src/middleware/)
       - Detect assets directory (public/, static/, assets/)
       - Detect styles directory (styles/, css/, scss/)

    2. Directory mapping structure validation (8 tests)
       - Mappings follow expected schema (category, pattern, priority, description)
       - Priority ordering (explicit > inferred > default)
       - Pattern glob matching validation
       - Category normalization (routes, tests, components, etc.)

    3. Edge cases (7 tests)
       - Empty or minimal codebase (no standard directories)
       - Multiple matching directories (disambiguation rules)
       - Nested directories (parent/child resolution)
       - Case sensitivity handling
       - Symlink handling
       - Monorepo package boundaries
       - Custom directory names via configuration

    4. Integration with CODEBASE.md (5 tests)
       - Mappings serialize to YAML correctly
       - Mappings deserialize from YAML correctly
       - File path lookup against mappings
       - Priority-based resolution

    Use the same test framework as other Legion tests (Node.js assert or similar). Include descriptive test names and clear assertions.
  </action>
  <verify>
    <automated>node tests/directory-mappings.test.js</automated>
    <manual>Review test coverage - should have 30+ tests, all passing</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>30+ directory mapping tests created and passing</done>
</task>

<task type="auto">
  <name>Task 2: Create path enforcement validation tests</name>
  <files>tests/path-enforcement.test.js</files>
  <action>
    Create `tests/path-enforcement.test.js` with 30+ unit tests for path enforcement logic:

    Test categories:
    1. Path validation against mappings (10 tests)
       - Valid paths for each category (routes, tests, components, etc.)
       - Invalid paths that violate category rules
       - Path suggestions for invalid paths
       - Category inference from file type/purpose
       - Multiple valid categories (ambiguous placement)

    2. Spec pipeline integration (8 tests)
       - validateDeliverablePath() function
       - Path validation during spec drafting
       - Warnings vs errors based on strictness
       - Override mechanisms for exceptions

    3. Wave executor integration (7 tests)
       - File placement validation before agent spawn
       - Auto-correction of misplaced files
       - Warnings for out-of-directory files
       - Blocking vs non-blocking enforcement modes

    4. Configuration-based enforcement (5 tests)
       - strict mode: error on violations
       - warn mode: warning on violations
       - off mode: no enforcement
       - Per-category strictness settings

    Example test structure:
    ```javascript
    test('routes file should be in routes directory', () => {
      const mappings = loadMappings('sample-codebase-mappings.yaml');
      const result = validatePath('src/users.ts', 'route', mappings);
      assert.strictEqual(result.valid, false);
      assert.ok(result.suggestions.includes('app/routes/users.ts'));
    });
    ```
  </action>
  <verify>
    <automated>node tests/path-enforcement.test.js</automated>
    <manual>Review test coverage - should have 30+ tests, all passing</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>30+ path enforcement tests created and passing</done>
</task>

<task type="auto">
  <name>Task 3: Create test fixtures and integration tests</name>
  <files>
    tests/fixtures/sample-codebase-mappings.yaml
    tests/environment-mapping.test.js
  </files>
  <action>
    Create test fixtures and integration tests:

    1. Create `tests/fixtures/sample-codebase-mappings.yaml`:
       - Sample directory mappings for a typical Node.js/TypeScript project
       - Include all standard categories: routes, tests, components, services, utils, types, config, middleware, assets
       - Include priority levels: explicit (10), inferred (5), default (1)
       - Include pattern examples for each category
       - Minimum 30 lines

    2. Create `tests/environment-mapping.test.js` with 30+ integration tests:
       - End-to-end workflow tests combining mapping extraction + path validation
       - Auto-update detection tests (file system change monitoring)
       - CODEBASE.md section integration tests
       - Cross-feature integration (mappings + spec pipeline + wave executor)
       - Error handling and edge cases

    Integration test examples:
    - "Full workflow: detect mappings → validate new file path → suggest correction"
    - "Auto-update: detect new directory → update mappings → validate against new mappings"
    - "Spec pipeline: draft spec with deliverable → validate paths → enforce or warn"
    - "Wave executor: spawn agent with file → validate placement → allow or redirect"
  </action>
  <verify>
    <automated>node tests/environment-mapping.test.js && node tests/directory-mappings.test.js && node tests/path-enforcement.test.js</automated>
    <manual>All 90+ tests should pass across all three test files</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    - Sample fixture created with realistic directory mappings
    - 30+ integration tests created and passing
    - All 90+ tests passing across the test suite
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run full test suite: `node tests/*.test.js` (or equivalent)
2. Verify 90+ tests pass with 0 failures
3. Verify test coverage includes directory mapping extraction, path enforcement, and auto-update detection
4. Verify test fixtures are realistic and usable for subsequent plans
5. Create SUMMARY.md documenting test structure and coverage
</verification>

<success_criteria>
- 90+ tests created across 3 test files
- All tests passing
- Test fixtures created with realistic directory mappings
- Tests follow same patterns as Phase 37-00 and 38-00 test scaffolding
- Test coverage: directory extraction (30+), path enforcement (30+), integration (30+)
</success_criteria>

<output>
After completion, create `.planning/phases/39-environment-mapping/39-00-SUMMARY.md`
</output>
