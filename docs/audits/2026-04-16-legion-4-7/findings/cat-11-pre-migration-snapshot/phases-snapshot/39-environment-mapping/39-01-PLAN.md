---
phase: 39-environment-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/codebase-mapper/SKILL.md
  - .planning/templates/codebase-mappings.yaml
  - .planning/config/directory-mappings.yaml
autonomous: true
requirements:
  - ENV-01
  - ENV-02
must_haves:
  truths:
    - "CODEBASE.md includes Directory Mappings section with standardized locations"
    - "Directory mappings are extracted from codebase structure automatically"
    - "Mappings identify standard locations for routes, tests, components, and other categories"
    - "Mappings stored in both human-readable (CODEBASE.md) and machine-readable (YAML) formats"
  artifacts:
    - path: "skills/codebase-mapper/SKILL.md"
      provides: "Enhanced codebase mapper with directory mapping extraction (Sections 2.5, 15)"
      min_lines: 100
    - path: ".planning/templates/codebase-mappings.yaml"
      provides: "Template for directory mappings structure"
      min_lines: 40
    - path: ".planning/config/directory-mappings.yaml"
      provides: "Generated mappings for current Legion codebase"
      min_lines: 30
  key_links:
    - from: "skills/codebase-mapper/SKILL.md Section 2.5"
      to: "CODEBASE.md ## Directory Mappings"
      via: "extractDirectoryMappings() function"
      pattern: "Directory Mappings"
    - from: "skills/codebase-mapper/SKILL.md Section 15"
      to: ".planning/config/directory-mappings.yaml"
      via: "YAML serialization"
      pattern: "directory-mappings.yaml"
---

<objective>
Enhance the codebase-mapper skill to generate directory mappings that identify standard locations (routes, tests, components, etc.) and store them in both CODEBASE.md and a machine-readable YAML format.

Purpose: Enable automatic path enforcement by establishing a canonical map of where different file types belong in the project structure.
Output: Enhanced codebase-mapper skill with directory mapping extraction, plus generated mappings for the Legion codebase itself.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/codebase-mapper/SKILL.md
@tests/directory-mappings.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add directory mapping extraction to codebase-mapper</name>
  <files>skills/codebase-mapper/SKILL.md</files>
  <action>
    Extend `skills/codebase-mapper/SKILL.md` with directory mapping capabilities:

    1. Add new Section 2.5: Directory Mapping Extraction (after Section 2.4 Module Structure)
       
       Section 2.5: Directory Mapping Extraction (ENV-01, ENV-02)
       
       Automatically identifies standard directory locations based on codebase structure and conventions.
       
       2.5.1: Standard Category Detection
       
       Detect these standard categories by examining directory structure:
       
       | Category | Detection Patterns | Priority |
       |----------|-------------------|----------|
       | routes | `app/routes/`, `src/routes/`, `pages/api/`, `routes/`, `api/` | explicit (10) if framework-specific |
       | tests | `tests/`, `__tests__/`, `*.test.*` co-location, `spec/`, `test/` | explicit (10) if dedicated dir |
       | components | `src/components/`, `app/components/`, `components/`, `ui/`, `widgets/` | explicit (10) |
       | services | `src/services/`, `services/`, `lib/services/`, `core/` | inferred (5) |
       | utils | `src/utils/`, `utils/`, `lib/`, `helpers/`, `common/` | inferred (5) |
       | types | `src/types/`, `types/`, `interfaces/`, `models/` | inferred (5) |
       | config | `config/`, `.config/`, `configuration/` | inferred (5) |
       | middleware | `src/middleware/`, `middleware/`, `plugins/` | inferred (5) |
       | assets | `public/`, `static/`, `assets/`, `resources/` | inferred (5) |
       | styles | `styles/`, `css/`, `scss/`, `sass/`, `src/styles/` | inferred (5) |
       | hooks | `src/hooks/`, `hooks/`, `composables/` | inferred (5) |
       | stores | `src/stores/`, `stores/`, `state/`, `redux/`, `pinia/` | inferred (5) |
       
       Detection Protocol:
       ```
       Step 1: List all directories up to depth 3
       Step 2: Match directory names against patterns above
       Step 3: For each match, determine priority:
         - explicit (10): Framework-standard location (e.g., Next.js app/routes/)
         - inferred (5): Common convention but not framework-mandated
         - default (1): Fallback or generic location
       Step 4: Handle conflicts (same category, multiple dirs):
         - Use explicit over inferred
         - Use higher file count as tiebreaker
         - Document both if significant usage (>20% of files)
       ```
       
       2.5.2: Monorepo Package Boundaries
       
       For monorepos (detected in Section 2.4), create per-package mappings:
       ```
       packages/web/:
         - routes: packages/web/app/routes/ (explicit)
         - components: packages/web/src/components/ (explicit)
       packages/api/:
         - routes: packages/api/src/routes/ (explicit)
         - services: packages/api/src/services/ (explicit)
       ```

    2. Add new Section 15: Machine-Readable Mappings (after Section 14)
       
       Section 15: Machine-Readable Mappings Output (ENV-01)
       
       In addition to the human-readable CODEBASE.md, generate a machine-readable
       YAML file for programmatic access: `.planning/config/directory-mappings.yaml`
       
       15.1: YAML Schema
       
       ```yaml
       generated: "2026-03-05"
       source: "CODEBASE.md"
       version: "1.0"
       
       # Project root mappings (single-package projects)
       mappings:
         routes:
           paths:
             - "app/routes"
             - "src/routes"
           priority: 10
           pattern: "**/*route*.{ts,js}"
           description: "API and page routes"
         tests:
           paths:
             - "tests"
             - "__tests__"
           priority: 10
           pattern: "**/*.test.{ts,js}"
           description: "Test files"
         # ... other categories
       
       # For monorepos, per-package mappings
       packages:
         web:
           routes:
             paths: ["packages/web/app/routes"]
             priority: 10
           # ...
         api:
           routes:
             paths: ["packages/api/src/routes"]
             priority: 10
           # ...
       
       # Validation rules for path enforcement
       rules:
         strictness: warn  # strict | warn | off
         exceptions: []    # List of allowed exceptions
       ```
       
       15.2: Generation Protocol
       
       ```
       Step 1: After completing CODEBASE.md sections 1-14
       Step 2: Extract directory mappings from Section 2.5 findings
       Step 3: Write to .planning/config/directory-mappings.yaml
       Step 4: Verify file is valid YAML
       Step 5: Log: "Directory mappings written to .planning/config/directory-mappings.yaml"
       ```

    3. Update Section 5 (CODEBASE.md Format) to include Directory Mappings section template:
       
       Add after "## Monorepo Structure" section:
       
       ```markdown
       ## Directory Mappings
       
       Standard locations for different file categories:
       
       | Category | Primary Location | Priority | Pattern |
       |----------|-----------------|----------|---------|
       | {category} | {path} | {explicit/inferred/default} | {file pattern} |
       
       ### Path Enforcement Rules
       - **Strictness**: {strict/warn/off}
       - New files should follow these mappings where applicable
       - Exceptions require explicit override
       ```

    Ensure all additions follow existing skill conventions and reference ENV-01 and ENV-02 requirements.
  </action>
  <verify>
    <automated>grep -c "Section 2.5" skills/codebase-mapper/SKILL.md && grep -c "Section 15" skills/codebase-mapper/SKILL.md && grep -c "ENV-01\|ENV-02" skills/codebase-mapper/SKILL.md</automated>
    <manual>Review the skill file to confirm:
    - Section 2.5 exists with directory mapping extraction
    - Section 15 exists with YAML output specification
    - CODEBASE.md format updated with Directory Mappings section</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    - Section 2.5 added with directory extraction protocol
    - Section 15 added with YAML output specification
    - Section 5 updated with Directory Mappings template
  </done>
</task>

<task type="auto">
  <name>Task 2: Create directory mappings template</name>
  <files>.planning/templates/codebase-mappings.yaml</files>
  <action>
    Create `.planning/templates/codebase-mappings.yaml` as a reference template:

    ```yaml
    # Directory Mappings Template
    # Generated by codebase-mapper skill
    # Used for path enforcement in spec-pipeline and wave-executor

    generated: "YYYY-MM-DD"
    source: "CODEBASE.md"
    version: "1.0"

    # Mapping categories and their standard locations
    # Priority: explicit (10) = framework-standard, inferred (5) = convention, default (1) = fallback
    mappings:
      routes:
        paths:
          - "app/routes"
          - "src/routes"
          - "pages/api"
          - "routes"
        priority: 10
        pattern: "**/*.{ts,js}"
        description: "API routes, page routes, and endpoint handlers"
        examples:
          - "app/routes/users.ts"
          - "src/routes/api.ts"

      tests:
        paths:
          - "tests"
          - "__tests__"
          - "test"
          - "spec"
        priority: 10
        pattern: "**/*.test.{ts,js,tsx,jsx,py}"
        description: "Test files - co-located or in dedicated directories"
        examples:
          - "tests/unit/utils.test.ts"
          - "src/components/Button.test.tsx"

      components:
        paths:
          - "src/components"
          - "app/components"
          - "components"
          - "ui"
          - "widgets"
        priority: 10
        pattern: "**/*.{tsx,jsx,vue,svelte}"
        description: "UI components and reusable visual elements"
        examples:
          - "src/components/Button.tsx"
          - "app/components/Header.vue"

      services:
        paths:
          - "src/services"
          - "services"
          - "lib/services"
          - "core"
        priority: 5
        pattern: "**/*.{ts,js}"
        description: "Business logic services and domain operations"
        examples:
          - "src/services/userService.ts"

      utils:
        paths:
          - "src/utils"
          - "utils"
          - "lib"
          - "helpers"
          - "common"
        priority: 5
        pattern: "**/*.{ts,js}"
        description: "Utility functions and helper modules"
        examples:
          - "src/utils/date.ts"

      types:
        paths:
          - "src/types"
          - "types"
          - "interfaces"
          - "models"
        priority: 5
        pattern: "**/*.{ts,d.ts}"
        description: "TypeScript type definitions and interfaces"
        examples:
          - "src/types/user.ts"

      config:
        paths:
          - "config"
          - ".config"
          - "configuration"
        priority: 5
        pattern: "**/*.{ts,js,json,yaml,yml}"
        description: "Configuration files and settings"
        examples:
          - "config/database.ts"

      middleware:
        paths:
          - "src/middleware"
          - "middleware"
          - "plugins"
        priority: 5
        pattern: "**/*.{ts,js}"
        description: "Middleware, plugins, and request interceptors"
        examples:
          - "src/middleware/auth.ts"

      assets:
        paths:
          - "public"
          - "static"
          - "assets"
          - "resources"
        priority: 5
        pattern: "**/*.{png,jpg,svg,css,woff,woff2}"
        description: "Static assets, images, fonts, and stylesheets"
        examples:
          - "public/logo.svg"

      styles:
        paths:
          - "styles"
          - "css"
          - "scss"
          - "sass"
          - "src/styles"
        priority: 5
        pattern: "**/*.{css,scss,sass,less}"
        description: "Stylesheets and CSS modules"
        examples:
          - "styles/global.css"

      hooks:
        paths:
          - "src/hooks"
          - "hooks"
          - "composables"
        priority: 5
        pattern: "**/*.{ts,js}"
        description: "React hooks, Vue composables, or similar"
        examples:
          - "src/hooks/useAuth.ts"

      stores:
        paths:
          - "src/stores"
          - "stores"
          - "state"
          - "redux"
          - "pinia"
        priority: 5
        pattern: "**/*.{ts,js}"
        description: "State management stores and reducers"
        examples:
          - "src/stores/authStore.ts"

    # For monorepos, per-package mappings
    packages: {}

    # Path enforcement configuration
    enforcement:
      strictness: warn  # Options: strict (error), warn (warning), off (no enforcement)
      exceptions: []    # List of glob patterns exempt from enforcement
      suggestions: true # Whether to suggest correct paths for violations
    ```

    Minimum 40 lines with comprehensive documentation.
  </action>
  <verify>
    <automated>wc -l .planning/templates/codebase-mappings.yaml && grep -c "mappings:" .planning/templates/codebase-mappings.yaml</automated>
    <manual>Verify template includes all 13 categories and follows YAML schema</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Template file created with 40+ lines covering all standard categories</done>
</task>

<task type="auto">
  <name>Task 3: Generate directory mappings for Legion codebase</name>
  <files>.planning/config/directory-mappings.yaml</files>
  <action>
    Analyze the Legion codebase structure and generate `.planning/config/directory-mappings.yaml`:

    1. Examine the directory structure:
       - commands/ (markdown command files)
       - skills/ (skill directories with SKILL.md files)
       - agents/ (agent personality files)
       - adapters/ (CLI adapter files)
       - tests/ (test files)
       - bin/ (installer scripts)
       - .planning/ (planning state)

    2. Map Legion-specific categories:

    ```yaml
    generated: "2026-03-05"
    source: "CODEBASE.md"
    version: "1.0"

    mappings:
      commands:
        paths:
          - "commands"
        priority: 10
        pattern: "**/*.md"
        description: "Legion command entry points (/legion:* commands)"
        examples:
          - "commands/build.md"
          - "commands/plan.md"

      skills:
        paths:
          - "skills"
        priority: 10
        pattern: "**/SKILL.md"
        description: "Legion workflow skills (reusable capabilities)"
        examples:
          - "skills/wave-executor/SKILL.md"
          - "skills/codebase-mapper/SKILL.md"

      agents:
        paths:
          - "agents"
        priority: 10
        pattern: "**/*.md"
        description: "Agent personality definitions"
        examples:
          - "agents/engineering-senior-developer.md"

      adapters:
        paths:
          - "adapters"
        priority: 10
        pattern: "**/*.md"
        description: "CLI adapter configurations"
        examples:
          - "adapters/claude-code.md"

      tests:
        paths:
          - "tests"
        priority: 10
        pattern: "**/*.test.js"
        description: "Test files for Legion functionality"
        examples:
          - "tests/authority-matrix.test.js"

      bin:
        paths:
          - "bin"
        priority: 5
        pattern: "**/*.js"
        description: "Installer and utility scripts"
        examples:
          - "bin/install.js"

      planning:
        paths:
          - ".planning"
        priority: 5
        pattern: "**/*.md"
        description: "Planning state and configuration"
        examples:
          - ".planning/ROADMAP.md"
          - ".planning/config/authority-matrix.yaml"

    # Legion is not a monorepo
    packages: {}

    enforcement:
      strictness: warn
      exceptions:
        - ".planning/**"  # Planning files can be anywhere in .planning/
        - "CLAUDE.md"     # Root config files exempt
      suggestions: true
    ```

    3. Verify the YAML is valid and follows the schema from the template.

    4. Ensure all detected directories are represented with appropriate priorities.
  </action>
  <verify>
    <automated>wc -l .planning/config/directory-mappings.yaml && node -e "const fs=require('fs'); const yaml=require('js-yaml'); yaml.load(fs.readFileSync('.planning/config/directory-mappings.yaml')); console.log('Valid YAML')" 2>&1 || echo "YAML validation requires js-yaml"</automated>
    <manual>Verify mappings accurately reflect Legion's directory structure</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    - Directory mappings YAML generated for Legion codebase
    - All Legion-specific categories mapped (commands, skills, agents, adapters, tests, bin, planning)
    - Enforcement rules configured appropriately
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify skills/codebase-mapper/SKILL.md has Sections 2.5 and 15
2. Verify template file exists at .planning/templates/codebase-mappings.yaml
3. Verify generated mappings exist at .planning/config/directory-mappings.yaml
4. Run tests to confirm directory mapping extraction works: `node tests/directory-mappings.test.js`
5. Verify all ENV-01 and ENV-02 requirements are addressed
</verification>

<success_criteria>
- codebase-mapper skill enhanced with directory mapping extraction (Sections 2.5, 15)
- CODEBASE.md format updated with Directory Mappings section template
- Template created at .planning/templates/codebase-mappings.yaml
- Legion directory mappings generated at .planning/config/directory-mappings.yaml
- Requirements ENV-01 and ENV-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/39-environment-mapping/39-01-SUMMARY.md`
</output>
