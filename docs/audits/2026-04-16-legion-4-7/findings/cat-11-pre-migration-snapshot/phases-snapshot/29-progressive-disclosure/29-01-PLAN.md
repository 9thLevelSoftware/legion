---
phase: 29-progressive-disclosure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/agent-creator/SKILL.md
  - skills/agent-registry/SKILL.md
  - skills/codebase-mapper/SKILL.md
  - skills/design-workflows/SKILL.md
  - skills/execution-tracker/SKILL.md
  - skills/github-sync/SKILL.md
  - skills/marketing-workflows/SKILL.md
  - skills/memory-manager/SKILL.md
  - skills/milestone-tracker/SKILL.md
  - skills/phase-decomposer/SKILL.md
  - skills/plan-critique/SKILL.md
  - skills/portfolio-manager/SKILL.md
  - skills/questioning-flow/SKILL.md
  - skills/review-loop/SKILL.md
  - skills/review-panel/SKILL.md
  - skills/wave-executor/SKILL.md
  - skills/workflow-common/SKILL.md
autonomous: true
requirements: [PRG-01, PRG-02]
user_setup: []

must_haves:
  truths:
    - "All 17 SKILL.md files have YAML frontmatter containing name, triggers, token_cost, and summary fields"
    - "Each summary field is ≤100 tokens (roughly ≤400 characters)"
    - "workflow-common contains a 'Skill Loading Protocol' section with load-on-activate instructions"
    - "token_cost values are one of: low, medium, high"
    - "triggers values are arrays of 3-6 lowercase keyword strings"
  artifacts:
    - path: "skills/workflow-common/SKILL.md"
      provides: "Shared conventions including Skill Loading Protocol"
      min_lines: 400
      contains: "Skill Loading Protocol"
    - path: "skills/wave-executor/SKILL.md"
      provides: "Wave execution skill with progressive disclosure metadata"
      min_lines: 587
      contains: "triggers:"
  key_links:
    - from: "skills/workflow-common/SKILL.md"
      to: "skills/*/SKILL.md"
      via: "Skill Loading Protocol defines the frontmatter schema all skills follow"
      pattern: "triggers:"
---

<objective>
Add progressive disclosure metadata to all 17 Legion skill files and document the Skill Loading Protocol in workflow-common.

Purpose: Reduce context overhead from ~17,000 tokens (loading all skills) to ~1,700 tokens (metadata only) at orchestrator startup. Full skill content is injected only when a skill is activated by a command.
Output: Updated frontmatter on all 17 SKILL.md files + new "Skill Loading Protocol" section in workflow-common.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-progressive-disclosure/29-CONTEXT.md

Relevant source files:
@skills/workflow-common/SKILL.md
@skills/agent-creator/SKILL.md
@skills/agent-registry/SKILL.md
@skills/codebase-mapper/SKILL.md
@skills/design-workflows/SKILL.md
@skills/execution-tracker/SKILL.md
@skills/github-sync/SKILL.md
@skills/marketing-workflows/SKILL.md
@skills/memory-manager/SKILL.md
@skills/milestone-tracker/SKILL.md
@skills/phase-decomposer/SKILL.md
@skills/plan-critique/SKILL.md
@skills/portfolio-manager/SKILL.md
@skills/questioning-flow/SKILL.md
@skills/review-loop/SKILL.md
@skills/review-panel/SKILL.md
@skills/wave-executor/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add progressive disclosure frontmatter to all 17 SKILL.md files</name>
  <files>
    skills/agent-creator/SKILL.md
    skills/agent-registry/SKILL.md
    skills/codebase-mapper/SKILL.md
    skills/design-workflows/SKILL.md
    skills/execution-tracker/SKILL.md
    skills/github-sync/SKILL.md
    skills/marketing-workflows/SKILL.md
    skills/memory-manager/SKILL.md
    skills/milestone-tracker/SKILL.md
    skills/phase-decomposer/SKILL.md
    skills/plan-critique/SKILL.md
    skills/portfolio-manager/SKILL.md
    skills/questioning-flow/SKILL.md
    skills/review-loop/SKILL.md
    skills/review-panel/SKILL.md
    skills/wave-executor/SKILL.md
    skills/workflow-common/SKILL.md
  </files>
  <action>
For each of the 17 SKILL.md files, read the file and update the YAML frontmatter block (between the `---` delimiters) to add three new fields: `triggers`, `token_cost`, and `summary`. Keep the existing `name` and `description` fields unchanged.

**Frontmatter schema after update:**
```yaml
---
name: legion:{skill-name}
description: {existing description — do not change}
triggers: [{3-6 lowercase keyword strings}]
token_cost: low|medium|high
summary: "{≤100 token summary of what this skill does and when to use it}"
---
```

**Token cost tiers** (based on file line count):
- `low`: <300 lines (quick to load)
- `medium`: 300-500 lines (moderate context)
- `high`: >500 lines (heavy context)

**Use the exact metadata below for each skill:**

### 1. agent-creator (420 lines → medium)
```yaml
triggers: [agent, create, custom, personality, new-agent]
token_cost: medium
summary: "Guided workflow for creating new agent personalities. Validates uniqueness, generates frontmatter and personality sections, registers in catalog. Use when adding a custom agent to the legion."
```

### 2. agent-registry (148 lines → low)
```yaml
triggers: [agent, recommend, team, catalog, assign, match]
token_cost: low
summary: "Maps all 51 agents by division, capability, and task type. Recommendation algorithm scores agents against task descriptions. Use when assembling teams or selecting agents for plans."
```

### 3. codebase-mapper (622 lines → high)
```yaml
triggers: [codebase, analyze, brownfield, architecture, map, existing]
token_cost: high
summary: "Parallel codebase analysis producing structured CODEBASE.md. Maps architecture, frameworks, patterns, and risks. Use when starting work on an existing codebase (brownfield)."
```

### 4. design-workflows (706 lines → high)
```yaml
triggers: [design, ui, ux, brand, visual, component]
token_cost: high
summary: "Domain-specific workflows for design phases. Covers design system creation, UX research, three-lens review (brand, accessibility, usability). Activates when DSN-* requirements or design keywords detected."
```

### 5. execution-tracker (282 lines → low)
```yaml
triggers: [track, progress, execution, status, state]
token_cost: low
summary: "Tracks phase execution progress and updates STATE.md. Records plan completions, failures, and wave advancement. Use during /legion:build to maintain execution state."
```

### 6. github-sync (678 lines → high)
```yaml
triggers: [github, issue, pr, milestone, sync, remote]
token_cost: high
summary: "Optional GitHub integration via gh CLI. Creates issues from plans, PRs from builds, syncs milestones. Checks prerequisites (gh installed, authenticated, remote exists) and degrades gracefully."
```

### 7. marketing-workflows (538 lines → high)
```yaml
triggers: [marketing, campaign, content, social, channel, audience]
token_cost: high
summary: "Domain-specific workflows for marketing phases. Covers campaign planning, content calendars, cross-channel coordination across 8 marketing agents. Activates when MKT-* requirements or marketing keywords detected."
```

### 8. memory-manager (351 lines → medium)
```yaml
triggers: [memory, outcome, pattern, recall, learn]
token_cost: medium
summary: "Cross-session learning layer via OUTCOMES.md. Stores agent performance and task outcomes with importance scoring and time-based decay. Enhances agent recommendations and status briefings."
```

### 9. milestone-tracker (434 lines → medium)
```yaml
triggers: [milestone, complete, archive, version, release, ship]
token_cost: medium
summary: "Manages milestone lifecycle: status checking, completion validation, summary generation, phase archiving, and next milestone creation. Use during /legion:milestone for version transitions."
```

### 10. phase-decomposer (643 lines → high)
```yaml
triggers: [plan, decompose, phase, task, wave, breakdown]
token_cost: high
summary: "Decomposes roadmap phases into wave-structured plans. Analyzes requirements, groups deliverables by dependency, recommends agents, generates PLAN.md files. Core engine for /legion:plan."
```

### 11. plan-critique (313 lines → medium)
```yaml
triggers: [critique, pre-mortem, assumption, risk, review-plan]
token_cost: medium
summary: "Pre-mortem analysis and assumption hunting for generated plans. Identifies risks, blind spots, and over-optimism before execution begins. Use after plan generation to stress-test the approach."
```

### 12. portfolio-manager (328 lines → medium)
```yaml
triggers: [portfolio, project, multi-project, dashboard, cross-project]
token_cost: medium
summary: "Multi-project dashboard reading each project's STATE.md and ROADMAP.md. Shows cross-project dependencies, health status, and Studio Producer insights. Use for /legion:portfolio."
```

### 13. questioning-flow (255 lines → low)
```yaml
triggers: [start, initialize, project, question, discovery, setup]
token_cost: low
summary: "Guided questioning flow for new project initialization. Extracts vision, requirements, constraints, and architecture preferences through structured conversation. Generates PROJECT.md, ROADMAP.md, STATE.md."
```

### 14. review-loop (685 lines → high)
```yaml
triggers: [review, quality, fix, iterate, qa, test]
token_cost: high
summary: "Iterative review cycle: test → review → fix → re-test. Spawns testing agents, collects findings, coordinates fixes, re-validates. Core engine for /legion:review with max iteration limits."
```

### 15. review-panel (380 lines → medium)
```yaml
triggers: [review, panel, expert, opinion, advisory, evaluate]
token_cost: medium
summary: "Dynamic expert review panels assembled from relevant agents. Each panelist reviews independently, findings are synthesized. Used for targeted quality assessment within /legion:review."
```

### 16. wave-executor (587 lines → high)
```yaml
triggers: [build, execute, parallel, wave, dispatch, spawn]
token_cost: high
summary: "Executes wave-structured plans via Claude Code Teams. Parallel within waves, sequential between. Full personality injection, SendMessage-based result collection. Core engine for /legion:build."
```

### 17. workflow-common (373 lines → medium)
```yaml
triggers: [common, shared, paths, conventions, state, config]
token_cost: medium
summary: "Shared constants, paths, and patterns for all /legion: commands. Defines state file locations, personality injection pattern, wave execution pattern, cost profiles, and all workflow conventions."
```

**Important implementation notes:**
- Use the Edit tool to update each file's frontmatter block. Replace the existing `---` frontmatter block with the updated version that includes the new fields.
- Preserve the existing `name` and `description` values exactly — only add the three new fields.
- Do NOT modify any content below the closing `---` of the frontmatter.
- The `triggers` field must be a YAML array (square brackets), not a string.
- The `summary` field must be a quoted YAML string.
  </action>
  <verify>
# Verify all 17 files have the required frontmatter fields
for f in skills/*/SKILL.md; do
  echo "=== $f ==="
  head -20 "$f" | grep -E "^(triggers|token_cost|summary):" || echo "MISSING FIELDS"
done

# Count files with all three new fields present
echo "---"
echo "Files with triggers field:"
grep -rl "^triggers:" skills/*/SKILL.md | wc -l
echo "Files with token_cost field:"
grep -rl "^token_cost:" skills/*/SKILL.md | wc -l
echo "Files with summary field:"
grep -rl "^summary:" skills/*/SKILL.md | wc -l

# Verify summary lengths (rough check: each summary should be under 400 chars)
for f in skills/*/SKILL.md; do
  summary_len=$(grep "^summary:" "$f" | wc -c)
  if [ "$summary_len" -gt 400 ]; then
    echo "WARNING: Summary too long in $f ($summary_len chars)"
  fi
done
  </verify>
  <done>All 17 SKILL.md files have updated YAML frontmatter with triggers, token_cost, and summary fields.</done>
</task>

<task type="auto">
  <name>Task 2: Add Skill Loading Protocol to workflow-common</name>
  <files>skills/workflow-common/SKILL.md</files>
  <action>
Read `skills/workflow-common/SKILL.md` and add a new section called "Skill Loading Protocol" immediately after the "## Cost Profile Convention" section (around line 107) and before the "## Error Handling Pattern" section.

**Insert the following section:**

```markdown
## Skill Loading Protocol

Legion skills use progressive disclosure: lightweight metadata at startup, full content on activation.

### Metadata Schema

Every SKILL.md file has YAML frontmatter with these fields:

| Field | Type | Purpose |
|-------|------|---------|
| `name` | string | Skill identifier (`legion:{skill-name}`) |
| `description` | string | One-line description for catalog display |
| `triggers` | string[] | 3-6 keywords that indicate this skill should be loaded |
| `token_cost` | enum | `low` (<300 lines), `medium` (300-500), `high` (500+) |
| `summary` | string | ≤100 token summary of purpose and activation conditions |

### Loading Stages

**Stage 1: Metadata Only (orchestrator startup)**
When a `/legion:` command initializes, load ONLY the frontmatter block from each skill. This provides:
- Skill names and summaries for routing decisions (~100 tokens per skill, ~1,700 total)
- Trigger keywords for matching user intent to relevant skills
- Token cost estimates for context budget planning

**Stage 2: Full Injection (skill activation)**
When a command determines it needs a specific skill, load the ENTIRE SKILL.md content. This happens when:
- A command's workflow references the skill (e.g., `/legion:build` activates `wave-executor`)
- Trigger keywords in user input match a skill's triggers array
- An agent is spawned that requires the skill's instructions

### Implementation Rules

1. **Never load all skills at once** — only the orchestrating command's direct dependencies
2. **Metadata is free** — reading frontmatter blocks adds negligible context (~100 tokens each)
3. **Full load is expensive** — a high-cost skill adds 500+ lines to context; only load when needed
4. **Commands know their skills** — each command declares which skills it needs (e.g., `/legion:build` always loads `wave-executor` and `execution-tracker`)
5. **Trigger matching is secondary** — commands have fixed skill dependencies; trigger matching is for edge cases and `/legion:quick` routing

### Command-to-Skill Mapping

| Command | Always Loads | Conditionally Loads |
|---------|-------------|-------------------|
| `/legion:start` | questioning-flow, workflow-common | codebase-mapper (brownfield) |
| `/legion:plan` | phase-decomposer, agent-registry, workflow-common | marketing-workflows, design-workflows, plan-critique |
| `/legion:build` | wave-executor, execution-tracker, workflow-common | github-sync |
| `/legion:review` | review-loop, review-panel, workflow-common | — |
| `/legion:status` | execution-tracker, workflow-common | memory-manager |
| `/legion:quick` | agent-registry, workflow-common | {matched by triggers} |
| `/legion:portfolio` | portfolio-manager, workflow-common | — |
| `/legion:milestone` | milestone-tracker, workflow-common | — |
| `/legion:agent` | agent-creator, agent-registry, workflow-common | — |
| `/legion:advise` | agent-registry, workflow-common | {matched by triggers} |

### Context Budget Guideline

Before loading a skill, check the estimated token impact:
- `low` skills: load freely (negligible context impact)
- `medium` skills: load when clearly needed (moderate context, ~300-500 lines)
- `high` skills: load only when the command requires them (heavy context, 500+ lines)

If multiple high-cost skills are needed in a single command, consider whether the orchestrator can delegate to sub-agents (each with their own context window) rather than loading all skills into the main context.
```

**Important:** Insert this as a new section. Do not modify any existing sections in workflow-common.
  </action>
  <verify>
# Verify the new section exists
grep -c "Skill Loading Protocol" skills/workflow-common/SKILL.md

# Verify key subsections exist
grep "Metadata Schema" skills/workflow-common/SKILL.md
grep "Loading Stages" skills/workflow-common/SKILL.md
grep "Command-to-Skill Mapping" skills/workflow-common/SKILL.md

# Verify the file grew (was 373 lines, should be ~430+)
wc -l skills/workflow-common/SKILL.md
  </verify>
  <done>workflow-common contains the Skill Loading Protocol section documenting progressive disclosure stages, metadata schema, command-to-skill mapping, and context budget guidelines.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 17 SKILL.md files have `triggers:` field in frontmatter
- [ ] All 17 SKILL.md files have `token_cost:` field in frontmatter (value is low, medium, or high)
- [ ] All 17 SKILL.md files have `summary:` field in frontmatter (≤100 tokens each)
- [ ] `skills/workflow-common/SKILL.md` contains "## Skill Loading Protocol" section
- [ ] Skill Loading Protocol documents the two-stage loading pattern (metadata → full injection)
- [ ] Skill Loading Protocol includes command-to-skill mapping table
- [ ] No existing skill content was modified (only frontmatter additions + new workflow-common section)
- [ ] Existing `name` and `description` fields in all frontmatters are unchanged
</verification>

<success_criteria>
- All 17 SKILL.md files have valid YAML frontmatter with name, triggers, token_cost, and summary
- `workflow-common` documents the Skill Loading Protocol with clear load-on-activate instructions
- Each frontmatter summary is ≤100 tokens
- No regression in existing skill content
</success_criteria>

<output>
After completion, create `.planning/phases/29-progressive-disclosure/29-01-SUMMARY.md`

Update `.planning/STATE.md`:
- Mark Phase 29 as "In Progress" during execution
- Update last activity timestamp
</output>
