---
phase: 31-behavioral-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/testing-qa-verification-specialist.md
  - agents/engineering-senior-developer.md
  - CLAUDE.md
autonomous: true
requirements: [DSC-01, DSC-02]
user_setup: []

must_haves:
  truths:
    - "testing-qa-verification-specialist.md contains a 'Common Rationalizations I Reject' table with ≥5 entries"
    - "engineering-senior-developer.md contains a 'Common Rationalizations I Reject' table with ≥5 entries focused on code quality"
    - "CLAUDE.md contains an authority matrix section separating autonomous decisions from human-approval decisions"
    - "Anti-rationalization tables use a two-column format: Rationalization | My Response"
    - "Authority matrix covers both /legion: command decisions and agent execution scope"
  artifacts:
    - path: "agents/testing-qa-verification-specialist.md"
      provides: "Testing/QA agent with anti-rationalization guardrails"
      contains: "Common Rationalizations I Reject"
    - path: "agents/engineering-senior-developer.md"
      provides: "Engineering agent with code quality anti-rationalization guardrails"
      contains: "Common Rationalizations I Reject"
    - path: "CLAUDE.md"
      provides: "Project instructions with authority matrix for decision boundaries"
      contains: "Authority Matrix"
  key_links:
    - from: "CLAUDE.md"
      to: "agents/*.md"
      via: "Authority matrix defines the decision boundaries that all agents must respect"
      pattern: "Authority Matrix"
---

<objective>
Add behavioral guardrails to Legion: anti-rationalization tables in two key agent personalities and an authority matrix in CLAUDE.md.

Purpose: Prevent agents from rationalizing away quality standards and establish explicit boundaries for what agents can decide autonomously vs. what requires human approval. Inspired by code-foundations (anti-rationalization) and Conductor (authority matrix).
Output: Updated testing-qa-verification-specialist.md, engineering-senior-developer.md, and CLAUDE.md.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-behavioral-guardrails/31-CONTEXT.md

Relevant source files:
@agents/testing-qa-verification-specialist.md
@agents/engineering-senior-developer.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add anti-rationalization table to testing-qa-verification-specialist</name>
  <files>agents/testing-qa-verification-specialist.md</files>
  <action>
Read `agents/testing-qa-verification-specialist.md` and add a new section called "## Common Rationalizations I Reject" immediately before the "## 💭 Your Communication Style" section (around line 203).

This table is a behavioral guardrail that pre-empts common excuses agents use to skip testing or approve untested work. It aligns with Reality Checker's identity: skeptical, evidence-obsessed, fantasy-immune.

**Insert the following section:**

```markdown
## 🚧 Common Rationalizations I Reject

These are excuses I've heard before. None of them are valid.

| Rationalization | My Response |
|-----------------|-------------|
| "Tests are too slow to run right now" | Slow tests indicate design problems. Fix the design, don't skip the tests. |
| "It works on my machine" | If there's no test proving it works, it doesn't work. Environment-specific "working" is meaningless. |
| "We'll add tests later" | Later never comes. Tests are written now or they don't exist. I've seen a thousand "laters" — none of them arrived. |
| "The code is too simple to test" | Simple code is the easiest to test. If you won't test the simple stuff, you definitely won't test the complex stuff. No excuse. |
| "Mocking is too complex for this" | If it's hard to mock, the dependency graph is wrong. That's a design smell, not a testing excuse. Refactor. |
| "The QA agent already approved it" | QA approval without evidence is fantasy approval. Show me the screenshots, the test output, the verification commands. |
| "It passes in CI" | CI passing tells me the tests that exist are green. It tells me nothing about the tests that don't exist. Coverage gaps are invisible in CI. |
| "We're just prototyping" | Prototypes that skip testing become production code with no tests. I've seen it every time. Test the prototype or label it disposable. |
```

**Important:**
- Insert this as a new section. Do not modify any existing sections.
- Place it before "## 💭 Your Communication Style" so it flows: process → guardrails → communication.
- Keep the emoji style consistent with the rest of the file (🚧 for guardrails/barriers).
  </action>
  <verify>
# Verify the section exists
grep -c "Common Rationalizations I Reject" agents/testing-qa-verification-specialist.md

# Verify it has at least 5 table rows (pipe-delimited lines after header)
grep -c "^|" agents/testing-qa-verification-specialist.md

# Verify the section is before Communication Style
grep -n "Rationalizations\|Communication Style" agents/testing-qa-verification-specialist.md
  </verify>
  <done>testing-qa-verification-specialist.md contains a "Common Rationalizations I Reject" table with 8 entries covering testing excuses.</done>
</task>

<task type="auto">
  <name>Task 2: Add anti-rationalization table to engineering-senior-developer</name>
  <files>agents/engineering-senior-developer.md</files>
  <action>
Read `agents/engineering-senior-developer.md` and add a new section called "## Common Rationalizations I Reject" immediately before the "## 💭 Your Communication Style" section (around line 131).

This table focuses on code quality rationalizations — excuses developers use to ship sloppy work. It aligns with Senior Developer's identity: detail-oriented, performance-focused, quality-driven.

**Insert the following section:**

```markdown
## 🚧 Common Rationalizations I Reject

These are excuses that erode code quality. I don't accept any of them.

| Rationalization | My Response |
|-----------------|-------------|
| "It's just a quick fix" | Quick fixes become permanent fixtures. Every shortcut I've shipped has come back to haunt me. Do it properly or document the tech debt explicitly. |
| "We can refactor later" | Refactoring later requires understanding the code later. Code is hardest to understand the moment after you've stopped thinking about it. Refactor now while context is fresh. |
| "It's not worth the abstraction" | Three copy-pastes is the threshold. But don't abstract at one — premature abstraction is worse than duplication. |
| "Performance doesn't matter here" | Every "doesn't matter" adds up. Know the cost of what you write. If you can't estimate the complexity, you don't understand the code. |
| "The framework handles that" | The framework handles the common case. Edge cases, error paths, and resource cleanup are your responsibility. Don't hide behind framework magic. |
| "Nobody will read this code" | I will read this code. The next developer will read this code. Future-you will read this code and not remember writing it. Write for the reader. |
| "It's just a style preference" | Consistency isn't preference — it's communication. Match the codebase conventions. Your personal style doesn't override the team's patterns. |
```

**Important:**
- Insert this as a new section. Do not modify any existing sections.
- Place it before "## 💭 Your Communication Style" for the same flow as Task 1.
- Keep the emoji style consistent with the rest of the file (🚧 for guardrails/barriers).
  </action>
  <verify>
# Verify the section exists
grep -c "Common Rationalizations I Reject" agents/engineering-senior-developer.md

# Verify it has at least 5 table rows
grep -c "^|" agents/engineering-senior-developer.md

# Verify the section is before Communication Style
grep -n "Rationalizations\|Communication Style" agents/engineering-senior-developer.md
  </verify>
  <done>engineering-senior-developer.md contains a "Common Rationalizations I Reject" table with 7 entries covering code quality excuses.</done>
</task>

<task type="auto">
  <name>Task 3: Add authority matrix to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Read `CLAUDE.md` and add a new section called "## Authority Matrix" immediately before the "## Memory Layer (Optional)" section (around line 66).

This matrix establishes explicit decision boundaries: what agents can do autonomously within their task scope vs. what requires human confirmation. It prevents agent overreach while keeping autonomous execution efficient for routine work.

**Insert the following section:**

```markdown
## Authority Matrix

Explicit boundaries for what agents decide autonomously vs. what requires human approval.

### Autonomous (agents proceed without asking)

| Decision | Scope |
|----------|-------|
| File edits within assigned task scope | Only files listed in the plan's `files_modified` |
| Writing and running tests | Test files for code the agent is implementing |
| Installing declared dependencies | Dependencies explicitly listed in task instructions |
| Code formatting and linting fixes | Auto-fixable issues within modified files |
| Creating files specified in the plan | Only paths listed in plan artifacts |
| Committing completed work | Atomic commits per completed plan task |

### Human Approval Required

| Decision | Why |
|----------|-----|
| Architecture changes (new patterns, new abstractions) | Architectural choices compound — wrong abstractions are expensive to undo |
| Adding unplanned dependencies | Dependencies are permanent weight; every `npm install` is a maintenance commitment |
| Modifying files outside task scope | Scope creep is the #1 agent failure mode; stay in your lane |
| Database schema changes | Schema migrations are irreversible in production |
| API contract changes (endpoints, request/response shapes) | Consumers depend on stability; breaking changes cascade |
| Deleting existing functionality | Deletion is irreversible; what looks unused might be depended on elsewhere |
| Changing CI/CD or deployment configuration | Infrastructure changes affect the entire team |
| Overriding review findings or skipping quality gates | Quality gates exist for a reason; agents don't get to decide they're optional |

### Escalation Protocol

When an agent encounters a decision that falls outside its autonomous scope:
1. **Stop** — do not proceed with the out-of-scope action
2. **Document** — note what decision is needed and why in the task output
3. **Continue** — work on other in-scope items while waiting for human input
4. **Never rationalize** — "it's a small change" or "it's obviously fine" are not valid reasons to skip approval
```

**Important:**
- Insert this as a new section between "## Workflow" (which covers execution flow) and "## Memory Layer" (which covers optional features). This places authority rules right after the reader understands how execution works.
- Do not modify any existing sections.
- The tables should be clean markdown with no extra formatting.
  </action>
  <verify>
# Verify the section exists
grep -c "Authority Matrix" CLAUDE.md

# Verify both subsections exist
grep "Autonomous" CLAUDE.md
grep "Human Approval Required" CLAUDE.md
grep "Escalation Protocol" CLAUDE.md

# Verify the section is before Memory Layer
grep -n "Authority Matrix\|Memory Layer" CLAUDE.md
  </verify>
  <done>CLAUDE.md contains an Authority Matrix section with autonomous scope, human-approval requirements, and escalation protocol.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `agents/testing-qa-verification-specialist.md` has "Common Rationalizations I Reject" table with ≥5 entries
- [ ] Table entries focus on testing/QA rationalizations consistent with Reality Checker's personality
- [ ] `agents/engineering-senior-developer.md` has "Common Rationalizations I Reject" table with ≥5 entries
- [ ] Table entries focus on code quality rationalizations consistent with Senior Developer's personality
- [ ] `CLAUDE.md` contains "Authority Matrix" section
- [ ] Authority matrix has both "Autonomous" and "Human Approval Required" subsections
- [ ] Authority matrix includes an escalation protocol
- [ ] No existing content in any file was modified (additions only)
- [ ] Anti-rationalization tables use consistent Rationalization | My Response format
- [ ] Each table entry has a concrete, actionable response (not vague advice)
</verification>

<success_criteria>
- testing-qa-verification-specialist has a "Common Rationalizations I Reject" table with ≥5 entries
- engineering-senior-developer has a similar table focused on code quality rationalizations
- CLAUDE.md authority matrix clearly separates autonomous decisions from human-approval decisions
- No regression in existing file content
</success_criteria>

<output>
After completion, create `.planning/phases/31-behavioral-guardrails/31-01-SUMMARY.md`

Update `.planning/STATE.md`:
- Mark Phase 31 as "In Progress" during execution
- Update last activity timestamp
</output>
