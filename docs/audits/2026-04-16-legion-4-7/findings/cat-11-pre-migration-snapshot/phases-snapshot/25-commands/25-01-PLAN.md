---
phase: 25-commands
plan: 01
type: execute
wave: 1
depends_on: [24-01]
files_modified:
  - commands/advise.md
  - commands/agent.md
  - commands/build.md
  - commands/milestone.md
  - commands/plan.md
  - commands/portfolio.md
  - commands/quick.md
  - commands/review.md
  - commands/start.md
  - commands/status.md
autonomous: true
requirements:
  - CMD-01
  - CMD-02
  - CMD-03

must_haves:
  truths:
    - "Zero case-insensitive occurrences of 'agency' remain across all 10 files in commands/"
    - "All 10 frontmatter name fields declare legion:{command} (not agency:{command})"
    - "All 68 command references use /legion: prefix (not /agency:)"
    - "All 12 commit message prefixes use (legion) scope (not (agency))"
    - "All 10 filesystem path references use ~/.claude/legion/ (not ~/.claude/agency/)"
    - "GitHub label references in plan.md use 'legion' (not 'agency')"
    - "Branch name pattern in build.md uses legion/phase- (not agency/phase-)"
    - "portfolio.md line 228 reads 'a Legion project' (not 'an Legion project')"
  artifacts:
    - path: "commands/advise.md"
      provides: "/legion:advise command entry point"
      contains: "legion:advise"
    - path: "commands/agent.md"
      provides: "/legion:agent command entry point"
      contains: "legion:agent"
    - path: "commands/build.md"
      provides: "/legion:build command entry point"
      contains: "legion:build"
    - path: "commands/milestone.md"
      provides: "/legion:milestone command entry point"
      contains: "legion:milestone"
    - path: "commands/plan.md"
      provides: "/legion:plan command entry point"
      contains: "legion:plan"
    - path: "commands/portfolio.md"
      provides: "/legion:portfolio command entry point"
      contains: "legion:portfolio"
    - path: "commands/quick.md"
      provides: "/legion:quick command entry point"
      contains: "legion:quick"
    - path: "commands/review.md"
      provides: "/legion:review command entry point"
      contains: "legion:review"
    - path: "commands/start.md"
      provides: "/legion:start command entry point"
      contains: "legion:start"
    - path: "commands/status.md"
      provides: "/legion:status command entry point"
      contains: "legion:status"
  key_links:
    - from: "commands/*.md"
      to: "skills/workflow-common/SKILL.md"
      via: "commands consume shared constants from workflow-common (updated in Phase 24)"
      pattern: "/legion:"
    - from: "commands/*.md"
      to: "skills/*/SKILL.md"
      via: "commands reference skills for execution; skills still use /agency: until Phase 26"
      pattern: "cross-phase dependency"
---

<objective>
Rebrand all 10 command files in `commands/` from `/agency:` namespace to `/legion:` namespace.

Purpose: Every user-facing command becomes `/legion:` (requirements CMD-01, CMD-02, CMD-03). This is the second phase of the v3.0 Legion rebrand, following the Phase 24 foundation layer.
Output: 10 updated command files with zero "agency" references. 107 total substitutions across 5 patterns (name declarations, command references, brand prose, commit prefixes, paths/labels/branches).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-commands/25-CONTEXT.md
@.planning/phases/25-commands/25-RESEARCH.md

@commands/advise.md
@commands/agent.md
@commands/build.md
@commands/milestone.md
@commands/plan.md
@commands/portfolio.md
@commands/quick.md
@commands/review.md
@commands/start.md
@commands/status.md

<interfaces>
From 25-CONTEXT.md — 5 locked decisions:
1. Single plan, three tasks (all 10 files independent, substitution map exhaustive)
2. Atomic Write per file (same pattern as Phase 24)
3. Article change: portfolio.md "an Agency" → "a Legion" (only instance)
4. Description field: only advise.md needs description change (other 9 are clean)
5. Processing order by density (doesn't affect correctness)

From 25-RESEARCH.md — Complete substitution map:
- Pattern A: 10 frontmatter name declarations
- Pattern B: 68 command references (/agency:X → /legion:X)
- Pattern C: 12 brand prose ("Agency" → "Legion")
- Pattern D: 12 commit message prefixes (feat(agency): → feat(legion):)
- Pattern E: 15 filesystem paths, labels, branch names
- Total: 107 occurrences across 10 files
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read all 10 command files and verify baseline count</name>
  <files>commands/advise.md, commands/agent.md, commands/build.md, commands/milestone.md, commands/plan.md, commands/portfolio.md, commands/quick.md, commands/review.md, commands/start.md, commands/status.md</files>
  <action>
Read all 10 command files into context. Then run baseline verification:

```bash
grep -inc "agency" commands/*.md
```

Expected per-file counts:
- advise.md: 7
- agent.md: 3
- build.md: 15
- milestone.md: 8
- plan.md: 8
- portfolio.md: 13
- quick.md: 11
- review.md: 15
- start.md: 11
- status.md: 16
- **Total: 107**

If any count differs, reconcile against the 25-RESEARCH.md substitution map before proceeding. The research was conducted from fresh file reads, so counts should match exactly unless a file was modified between research and execution.

This establishes the pre-condition baseline.
  </action>
  <verify>
    <automated>grep -inc "agency" commands/*.md | awk -F: '{sum+=$2} END {print sum}'</automated>
  </verify>
  <done>All 10 files read into context. Baseline count confirmed at 107 (or reconciled if different).</done>
</task>

<task type="auto">
  <name>Task 2: Apply all substitutions — 10 atomic file writes</name>
  <files>commands/advise.md, commands/agent.md, commands/build.md, commands/milestone.md, commands/plan.md, commands/portfolio.md, commands/quick.md, commands/review.md, commands/start.md, commands/status.md</files>
  <action>
For each of the 10 command files, apply ALL substitutions from the 25-RESEARCH.md substitution map in a single Write operation per file. Do NOT use incremental Edit calls — each file must be written atomically to prevent partial state.

After each file write, immediately verify with:
```bash
grep -in "agency" commands/{filename}
```
Expected: zero output. Any match = incomplete substitution for that file.

**Processing order (by density, highest first):**

### Wave 1 — High density (3 files, 46 occurrences)

**status.md (16 occurrences):**
Apply from substitution map — 1 Pattern A, 13 Pattern B, 1 Pattern C, 1 Pattern C (line 33). All `/agency:` → `/legion:`, "No Agency project" → "No Legion project", frontmatter name.

**build.md (15 occurrences):**
Apply from substitution map — 1 Pattern A, 8 Pattern B, 2 Pattern C, 3 Pattern D, 1 Pattern E. Key: "The Agency Workflows" → "The Legion Workflows" (lines 118, 140), commit prefixes feat/chore(agency) → feat/chore(legion), branch pattern `agency/phase-` → `legion/phase-` (line 273).

**review.md (15 occurrences):**
Apply from substitution map — 1 Pattern A, 10 Pattern B, 1 Pattern C, 2 Pattern D. Key: "The Agency Workflows project is finished" → "The Legion Workflows project is finished" (line 425).

### Wave 2 — Medium density (3 files, 35 occurrences)

**portfolio.md (13 occurrences):**
Apply from substitution map — 1 Pattern A, 4 Pattern B, 3 Pattern C, 5 Pattern E. Key: `~/.claude/agency/` → `~/.claude/legion/` (5 path occurrences), "# Agency Portfolio" → "# Legion Portfolio" (line 64), **"an Agency project" → "a Legion project"** on line 228 (article change).

**start.md (11 occurrences):**
Apply from substitution map — 1 Pattern A, 2 Pattern B, 2 Pattern C, 5 Pattern E. Key: `~/.claude/agency/` → `~/.claude/legion/` (5 path occurrences), "# Agency Portfolio" → "# Legion Portfolio" (line 121), "non-Agency" → "non-Legion" (line 36).

**quick.md (11 occurrences):**
Apply from substitution map — 1 Pattern A, 4 Pattern B, 6 Pattern D. Key: 6 commit prefix patterns covering fix/test/docs/refactor/feat(agency) → fix/test/docs/refactor/feat(legion), plus the template line `{type}(agency)` → `{type}(legion)`.

### Wave 3 — Low density (4 files, 26 occurrences)

**plan.md (8 occurrences):**
Apply from substitution map — 1 Pattern A, 5 Pattern B, 2 Pattern E. Key: GitHub label `"agency"` → `"legion"` (lines 206, 213).

**milestone.md (8 occurrences):**
Apply from substitution map — 1 Pattern A, 4 Pattern B, 1 Pattern C, 2 Pattern D. Key: "No Agency project" → "No Legion project" (line 33).

**advise.md (7 occurrences):**
Apply from substitution map — 1 Pattern A, 5 Pattern B, 1 Pattern C. Key: description "Agency's 51 agent personalities" → "Legion's 51 agent personalities" (line 3). Only file needing a description change.

**agent.md (3 occurrences):**
Apply from substitution map — 1 Pattern A, 1 combined C+B (line 33: "No Agency project found. Run `/agency:start`" → "No Legion project found. Run `/legion:start`"), 1 Pattern B (line 99).

After ALL 10 files are written and individually verified, run the aggregate check:
```bash
grep -inc "agency" commands/*.md
```
Expected: all zeros.
  </action>
  <verify>
    <automated>grep -inc "agency" commands/*.md | awk -F: '{sum+=$2} END {print sum}'</automated>
  </verify>
  <done>All 10 files written atomically. Per-file grep confirms zero "agency" hits. Aggregate grep confirms 0 total.</done>
</task>

<task type="auto">
  <name>Task 3: Post-write verification — success criteria + spot-checks</name>
  <files>commands/advise.md, commands/agent.md, commands/build.md, commands/milestone.md, commands/plan.md, commands/portfolio.md, commands/quick.md, commands/review.md, commands/start.md, commands/status.md</files>
  <action>
Run five verification checks:

**Check 1 — Zero "agency" remnants across all command files:**
```bash
grep -in "agency" commands/*.md
```
Expected: zero output. Any match = incomplete substitution.

**Check 2 — Correct "legion" count across all command files:**
```bash
grep -inc "legion" commands/*.md
```
Expected per-file counts (each file should have AT LEAST the same count that previously had "agency"):
- advise.md: ≥7
- agent.md: ≥3
- build.md: ≥15
- milestone.md: ≥8
- plan.md: ≥8
- portfolio.md: ≥13
- quick.md: ≥11
- review.md: ≥15
- start.md: ≥11
- status.md: ≥16

(Some files may have slightly higher counts if "legion" already appeared elsewhere — the key constraint is that no count should be LOWER than the former "agency" count.)

**Check 3 — Frontmatter name declarations (CMD-01):**
```bash
grep -n "^name:" commands/*.md
```
Expected: every file shows `name: legion:{command}` — no `agency:` name declarations remain.

Verify these 10 exact values:
- `name: legion:advise`
- `name: legion:agent`
- `name: legion:build`
- `name: legion:milestone`
- `name: legion:plan`
- `name: legion:portfolio`
- `name: legion:quick`
- `name: legion:review`
- `name: legion:start`
- `name: legion:status`

**Check 4 — Commit prefixes (CMD-03):**
```bash
grep -in "agency)" commands/*.md
```
Expected: zero output. All `feat(agency)`, `chore(agency)`, `fix(agency)`, `test(agency)`, `docs(agency)`, `refactor(agency)` are now `(legion)`.

**Check 5 — Spot-check pitfall areas:**

5a. Article correction:
```bash
grep -in "a Legion project" commands/portfolio.md
```
Expected: line 228 matches. Verify it does NOT say "an Legion".

5b. GitHub label:
```bash
grep -in '"legion"' commands/plan.md
```
Expected: lines 206 and 213 show `"legion"` label.

5c. Branch pattern:
```bash
grep -in "legion/phase" commands/build.md
```
Expected: line 273 shows `legion/phase-{NN}-{slug}`.

5d. Filesystem paths:
```bash
grep -in "~/.claude/legion/" commands/portfolio.md commands/start.md
```
Expected: 5 hits in portfolio.md and 5 hits in start.md. Zero hits for `~/.claude/agency/`.

If any check fails, identify which substitution was missed using the 25-RESEARCH.md map, apply targeted fixes, then re-run all five checks.
  </action>
  <verify>
    <automated>grep -ic "agency" commands/*.md | awk -F: '{sum+=$2} END {print sum}'</automated>
  </verify>
  <done>All five verification checks pass. Zero "agency" remnants, correct "legion" counts, frontmatter names verified, commit prefixes verified, pitfall spot-checks confirmed. Phase 25 success criteria met.</done>
</task>

</tasks>

<verification>
- `grep -ic "agency" commands/*.md | awk -F: '{sum+=$2} END {print sum}'` returns 0
- All 10 frontmatter name fields match `legion:{command}` pattern
- `grep -in "agency)" commands/*.md` returns empty (commit prefixes clean)
- `grep -in "~/.claude/agency" commands/*.md` returns empty (filesystem paths clean)
- `grep -in '"agency"' commands/*.md` returns empty (GitHub labels clean)
- `grep -in "agency/" commands/*.md` returns empty (branch patterns clean)
- portfolio.md line 228 reads "a Legion" (not "an Legion")
- No lines were added or removed in any file (pure text substitution)
</verification>

<success_criteria>
Phase 25 success criteria (from ROADMAP.md):
1. All 10 command files declare their name as /legion:<command> with no /agency: name declarations remaining ✓ (verified by frontmatter grep)
2. Cross-command routing within each file points to /legion: equivalents ✓ (68 command references transformed, verified by agency grep)
3. Commit message prefix examples within command files show feat(legion): / chore(legion): ✓ (12 prefixes transformed, verified by pattern grep)
4. A user following any command's inline instructions is directed only to /legion: commands ✓ (zero "agency" remnants across all 10 files)
</success_criteria>

<output>
After completion, create `.planning/phases/25-commands/25-01-SUMMARY.md`
</output>
