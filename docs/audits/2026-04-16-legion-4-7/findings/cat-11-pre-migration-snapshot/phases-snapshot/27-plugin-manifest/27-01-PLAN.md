---
phase: 27-plugin-manifest
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
autonomous: true
requirements:
  - PLG-01
  - PLG-02

must_haves:
  truths:
    - "plugin.json has name 'legion' — plugin installs as `claude plugin install legion`"
    - "marketplace.json has name 'legion' in all name fields"
    - "Neither manifest file contains 'agency' or 'Agency Workflows' in any field"
    - "Both files have descriptions that match the Legion identity and core value"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest with Legion identity"
      contains: '"name": "legion"'
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace entry with Legion identity"
      contains: '"name": "legion"'
  key_links:
    - from: ".claude-plugin/plugin.json"
      to: "claude plugin install"
      via: "name field"
      pattern: '"name": "legion"'
    - from: ".claude-plugin/marketplace.json"
      to: "plugins[0].name"
      via: "nested plugin entry"
      pattern: '"name": "legion"'
---

<objective>
Update both Claude Code plugin manifest files from agency-workflows identity to Legion identity.

Purpose: The plugin must be installable as `claude plugin install legion` and present the Legion brand across all manifest fields. This is the prerequisite for Phase 28 documentation (README install instructions depend on the final plugin name).
Output: Two updated JSON files with no remaining "agency" or "Agency Workflows" references.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update plugin.json to Legion identity</name>
  <files>.claude-plugin/plugin.json</files>
  <action>
Rewrite `.claude-plugin/plugin.json` with the following exact content:

```json
{
  "name": "legion",
  "version": "3.0.0",
  "description": "Orchestrate 51 AI specialist personalities as a coordinated legion — \"My name is Legion, for we are many.\" Project initialization, phase planning, parallel execution, quality review, and portfolio management.",
  "author": {
    "name": "9thLevelSoftware",
    "url": "https://github.com/9thLevelSoftware"
  },
  "repository": "https://github.com/9thLevelSoftware/agency-agents",
  "license": "MIT",
  "keywords": [
    "ai-agents",
    "workflow-orchestration",
    "team-coordination",
    "project-management",
    "multi-agent",
    "legion",
    "parallel-execution"
  ]
}
```

Key changes from current file:
- `name`: `"agency-workflows"` → `"legion"`
- `version`: `"2.0.0"` → `"3.0.0"` (Legion rebrand is v3.0)
- `description`: updated to Legion identity with the core quote
- `keywords`: replaced `"code-review"` with `"legion"` to reinforce brand
  </action>
  <verify>
    <automated>node -e "const f=require('./.claude-plugin/plugin.json'); console.assert(f.name==='legion','name wrong'); console.assert(!JSON.stringify(f).includes('agency'),'agency remnant found'); console.assert(f.version==='3.0.0','version wrong'); console.log('plugin.json OK')"</automated>
  </verify>
  <done>plugin.json has name "legion", version "3.0.0", Legion description, and zero "agency" occurrences in any field.</done>
</task>

<task type="auto">
  <name>Task 2: Update marketplace.json to Legion identity</name>
  <files>.claude-plugin/marketplace.json</files>
  <action>
Rewrite `.claude-plugin/marketplace.json` with the following exact content:

```json
{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "legion",
  "description": "Orchestrate 51 AI specialist personalities as a coordinated legion — \"My name is Legion, for we are many.\" Project initialization, phase planning, parallel execution, quality review, and portfolio management.",
  "owner": {
    "name": "9thLevelSoftware",
    "url": "https://github.com/9thLevelSoftware"
  },
  "plugins": [
    {
      "name": "legion",
      "description": "Orchestrate 51 AI specialist personalities as a coordinated legion — \"My name is Legion, for we are many.\" Project initialization, phase planning, parallel execution, quality review, and portfolio management.",
      "version": "3.0.0",
      "author": {
        "name": "9thLevelSoftware",
        "url": "https://github.com/9thLevelSoftware"
      },
      "source": ".",
      "category": "productivity",
      "homepage": "https://github.com/9thLevelSoftware/agency-agents"
    }
  ]
}
```

Key changes from current file:
- Top-level `name`: `"agency-workflows"` → `"legion"`
- Top-level `description`: updated to Legion identity with the core quote
- `plugins[0].name`: `"agency-workflows"` → `"legion"`
- `plugins[0].description`: updated to match top-level Legion description
- `plugins[0].version`: `"2.0.0"` → `"3.0.0"`
  </action>
  <verify>
    <automated>node -e "const f=require('./.claude-plugin/marketplace.json'); const s=JSON.stringify(f); console.assert(f.name==='legion','top name wrong'); console.assert(f.plugins[0].name==='legion','plugin name wrong'); console.assert(!s.includes('agency'),'agency remnant found'); console.assert(f.plugins[0].version==='3.0.0','version wrong'); console.log('marketplace.json OK')"</automated>
  </verify>
  <done>marketplace.json has name "legion" at top level and inside plugins[], version "3.0.0", Legion description in both locations, and zero "agency" occurrences anywhere.</done>
</task>

</tasks>

<verification>
After both tasks complete, run a final sweep to confirm zero "agency" or "Agency Workflows" remnants across both manifest files:

```bash
grep -ri "agency" .claude-plugin/ && echo "FAIL: agency references found" || echo "PASS: no agency references"
grep -ri "agency workflows" .claude-plugin/ && echo "FAIL: Agency Workflows found" || echo "PASS: clean"
```

Both commands must print PASS (no matches = grep exits non-zero, which is the success case — no output + "PASS" message from the `|| echo`).
</verification>

<success_criteria>
1. `claude plugin install legion` would succeed (name field is "legion" in plugin.json)
2. `plugin.json` — name: legion, version: 3.0.0, Legion description, zero "agency" in any field
3. `marketplace.json` — name: legion at top level and in plugins[0], version: 3.0.0, Legion description in both locations, zero "agency" in any field
4. grep for "agency" across `.claude-plugin/` returns no matches
</success_criteria>

<output>
After completion, create `.planning/phases/27-plugin-manifest/27-01-SUMMARY.md` following the summary template.
</output>
