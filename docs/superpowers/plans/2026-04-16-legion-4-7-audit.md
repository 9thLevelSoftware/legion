# Legion v7.4.0 — Claude 4.7 Literalism Audit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Execute a 23-session structured audit of Legion's 125 LLM-facing files against a Claude 4.7 literalism rubric, producing per-file findings, a master `INDEX.md`, a machine-readable `FINDINGS-DB.jsonl`, and a thematically-clustered `REMEDIATION.md` that feeds directly into Legion's own `/legion:plan` workflow for v7.4.0 hardening.

**Architecture:** Solo manual sweep. One session per task. Each session loads the frozen rubric, audits a bounded batch of files (hard cap 10 per session), writes per-file findings, appends to a JSONL findings database, and commits per-file with a session-end `INDEX.md` + `SESSIONS.md` update. A `/legion:advise` review gate runs before the remediation roadmap is generated, validating finding quality and consistency.

**Tech Stack:** Markdown (findings files, docs), JSONL (findings database), JSON (metadata sidecar), git (version control + baseline tagging), Bash (helper scripts for finding ID generation and database validation).

**Source spec:** `docs/superpowers/specs/2026-04-16-legion-4-7-audit-design.md` (commit `cbe267d`).

**Scope reconciliation note:** the approved spec said 124 files; actual scope is **125** (agent count is 48, not 47 as stated in the spec). This plan uses the correct count of 125 throughout.

---

## File Structure

The audit produces these artifacts. All paths relative to repo root.

### Created by setup (Task 1)

- `docs/audits/2026-04-16-legion-4-7/RUBRIC.md` — frozen audit criteria v1.0
- `docs/audits/2026-04-16-legion-4-7/METHODOLOGY.md` — per-file audit procedure, reference for all subsequent tasks
- `docs/audits/2026-04-16-legion-4-7/INDEX.md` — master dashboard, updated per file
- `docs/audits/2026-04-16-legion-4-7/SESSIONS.md` — session log, updated per session
- `docs/audits/2026-04-16-legion-4-7/FINDINGS-DB.jsonl` — append-only findings database
- `docs/audits/2026-04-16-legion-4-7/FINDINGS-DB.meta.json` — top-level metadata sidecar
- `scripts/audit/next-finding-id.sh` — emits next `LEGION-47-NNN` ID based on FINDINGS-DB.jsonl
- `scripts/audit/validate-findings-db.sh` — schema-check JSONL + cross-reference against findings/ directory
- `scripts/audit/update-index.sh` — regenerate INDEX.md tables from findings/ directory and FINDINGS-DB.jsonl

### Created by audit tasks (Tasks 2-19)

- `docs/audits/2026-04-16-legion-4-7/findings/<layer>/<file-slug>.md` — one per audited file (125 total)

### Created by finalization tasks (Tasks 20-23)

- `docs/audits/2026-04-16-legion-4-7/REMEDIATION.md` — thematically-clustered remediation roadmap (Task 22)

### Git baseline

- Tag `audit-v47-baseline` — placed at HEAD during Task 1, used as reference for all `line_range` values in findings.

---

## Common Conventions (used across all audit tasks)

### Finding ID format
`LEGION-47-NNN` where NNN is zero-padded 3-digit sequential number starting at 001. Generated by `scripts/audit/next-finding-id.sh`.

### File layer slug mapping
- `commands/plan.md` → `findings/commands/plan.md`
- `skills/wave-executor/SKILL.md` → `findings/skills/wave-executor.md`
- `agents/engineering-senior-developer.md` → `findings/agents/engineering-senior-developer.md`
- `adapters/claude-code.md` → `findings/adapters/claude-code.md`
- `CLAUDE.md` → `findings/root/CLAUDE.md`
- `.planning/config/authority-matrix.yaml` → `findings/root/config-authority-matrix.md`
- `docs/schemas/plan-frontmatter.schema.json` → `findings/root/schema-plan-frontmatter.md`

### Per-file commit message
`audit: <file-path> — N findings (<severity-summary>)`

Examples:
- `audit: commands/plan.md — 7 findings (1 P0, 3 P1, 3 P2)`
- `audit: agents/engineering-ai-engineer.md — 0 findings (clean)`

### Session-end commit message
`audit: close session SNN — M files audited, K total findings`

---

## Task 1: Setup — Folder, Rubric, Methodology, Database, Baseline Tag (S01)

**Files:**
- Create: `docs/audits/2026-04-16-legion-4-7/RUBRIC.md`
- Create: `docs/audits/2026-04-16-legion-4-7/METHODOLOGY.md`
- Create: `docs/audits/2026-04-16-legion-4-7/INDEX.md`
- Create: `docs/audits/2026-04-16-legion-4-7/SESSIONS.md`
- Create: `docs/audits/2026-04-16-legion-4-7/FINDINGS-DB.jsonl` (empty)
- Create: `docs/audits/2026-04-16-legion-4-7/FINDINGS-DB.meta.json`
- Create: `scripts/audit/next-finding-id.sh`
- Create: `scripts/audit/validate-findings-db.sh`
- Create: `scripts/audit/update-index.sh`
- Create: `docs/audits/2026-04-16-legion-4-7/findings/.gitkeep` (per layer directory)

- [ ] **Step 1: Create the audit directory tree**

```bash
mkdir -p docs/audits/2026-04-16-legion-4-7/findings/commands
mkdir -p docs/audits/2026-04-16-legion-4-7/findings/skills
mkdir -p docs/audits/2026-04-16-legion-4-7/findings/agents
mkdir -p docs/audits/2026-04-16-legion-4-7/findings/adapters
mkdir -p docs/audits/2026-04-16-legion-4-7/findings/root
mkdir -p scripts/audit
touch docs/audits/2026-04-16-legion-4-7/findings/commands/.gitkeep
touch docs/audits/2026-04-16-legion-4-7/findings/skills/.gitkeep
touch docs/audits/2026-04-16-legion-4-7/findings/agents/.gitkeep
touch docs/audits/2026-04-16-legion-4-7/findings/adapters/.gitkeep
touch docs/audits/2026-04-16-legion-4-7/findings/root/.gitkeep
```

Expected: tree created, `.gitkeep` files placed so empty directories survive git.

- [ ] **Step 2: Write `RUBRIC.md` (frozen v1.0)**

Copy the exact 10-category rubric from the design spec into `docs/audits/2026-04-16-legion-4-7/RUBRIC.md`. The content is Section 4 (CAT-1 through CAT-10) of `docs/superpowers/specs/2026-04-16-legion-4-7-audit-design.md`. Add a header:

```markdown
# RUBRIC.md — Legion v7.4.0 Audit Criteria

**Version:** 1.0 (frozen 2026-04-16)
**Source spec:** docs/superpowers/specs/2026-04-16-legion-4-7-audit-design.md
**Do not edit without bumping version AND flagging all files for re-scoring.**

---

[Insert the 10 categories CAT-1 through CAT-10 verbatim from spec Section 4]

---

## Severity Scale

[Insert the 4-tier severity scale from spec Section 5 verbatim]
```

- [ ] **Step 3: Write `METHODOLOGY.md` (the per-file audit procedure)**

Write `docs/audits/2026-04-16-legion-4-7/METHODOLOGY.md` with exact content:

````markdown
# METHODOLOGY.md — Per-File Audit Procedure

**This document is the definitive procedure for auditing a single file. Every file-audit task in the plan references this procedure. Do not deviate.**

## Procedure (applied to each file in a session)

1. **Read the file completely.** Note its layer (command / skill / agent / adapter / root). Note whether it is prose-heavy (most files) or schema-heavy (JSON schemas).

2. **Identify the interaction surface.** What parts of this file become LLM-context at runtime? For commands/skills/agents: usually the whole file. For schemas: usually only `title`/`description`/`examples` fields.

3. **Apply each of the 10 rubric categories as a sequential pass.** For CAT-1 through CAT-10:
   - Scan for the category's detection patterns (see RUBRIC.md).
   - For each match, note: line range, exact excerpt, preliminary severity.
   - Distinguish `confirmed` (matches the observed open-set-interpretation failure class OR another externally-reported failure) vs `suspected` (structural risk only, no observed evidence).

4. **Generate finding IDs.** For each finding, run `bash scripts/audit/next-finding-id.sh` to get the next `LEGION-47-NNN`.

5. **Write the findings file** at `docs/audits/2026-04-16-legion-4-7/findings/<layer>/<slug>.md` using the template in this document.

6. **Append each finding to FINDINGS-DB.jsonl** using the JSONL schema below. One finding per line.

7. **Update INDEX.md** with the file's row: status, per-category finding counts, max severity, session ID.

8. **Commit per-file:** `audit: <file-path> — N findings (<severity-summary>)`

### Finding Record JSONL Schema

One line per finding. All fields required.

```json
{"id":"LEGION-47-042","file":"skills/wave-executor/SKILL.md","line_range":"87-94","category":"CAT-3","severity":"P0","confirmed":true,"excerpt":"Consider dispatching peer tasks in parallel when it makes sense.","issue":"Ambiguous trigger and missing fan-out criteria. 4.7 defaults to sequential dispatch.","remediation_sketch":"Replace with explicit condition: dispatch as parallel agents when (a) no shared files_modified, (b) same wave, (c) CLI supports parallel.","remediation_effort":"small","remediation_cluster":"dispatch-specification","status":"open","rubric_version":"1.0","session":"S06"}
```

**Field constraints:**
- `id`: matches regex `^LEGION-47-[0-9]{3}$`
- `file`: relative path from repo root
- `line_range`: `N` or `N-M` where N,M are 1-indexed line numbers
- `category`: one of `CAT-1`..`CAT-10`
- `severity`: one of `P0`,`P1`,`P2`,`P3`
- `confirmed`: boolean
- `remediation_effort`: one of `small`,`medium`,`large`
- `remediation_cluster`: lowercase kebab-case identifier (e.g., `closed-set-enforcement`, `dispatch-specification`, `intent-front-loading`, `persona-calibration`, `acceptance-criteria`, `authority-language`, `prohibitive-to-positive`, `response-calibration`, `precondition-verification`, `trigger-explicitness`)
- `status`: one of `open`,`in-remediation`,`resolved`,`wontfix`
- `session`: matches regex `^S[0-9]+[a-d]?$`

### Per-File Findings Document Template

```markdown
# Audit Findings — <file-path>

**Audited in session:** <SXX>
**Rubric version:** 1.0
**File layer:** <command|skill|agent|adapter|root>
**File length:** <N lines>
**Total findings:** <N> (<severity breakdown>)
**Baseline commit:** audit-v47-baseline

---

## LEGION-47-NNN — <severity>, <category short name> (<confirmed|suspected>)

**Lines NN-MM**

> <exact excerpt from file>

**Issue:** <1-3 sentences describing the 4.7-literalism risk>

**Remediation sketch:** <1-3 sentences describing the fix pattern>

**Remediation cluster:** `<cluster-slug>`
**Effort estimate:** <small|medium|large>

---

[Repeat for each finding in file]
```

### Zero-Findings Files

If a file has no findings, still create the findings file. Content:

```markdown
# Audit Findings — <file-path>

**Audited in session:** <SXX>
**Rubric version:** 1.0
**File layer:** <layer>
**File length:** <N lines>
**Total findings:** 0 (clean)
**Baseline commit:** audit-v47-baseline

---

No findings. All 10 rubric categories applied; no matches detected.

## Category Coverage Notes

(Optional) Notes on why specific categories were checked but produced no findings, if non-obvious.
```

### Session-End Procedure

At the end of each session:
1. Update `SESSIONS.md` with session end time, files completed, total findings surfaced, any blockers.
2. Run `bash scripts/audit/validate-findings-db.sh` to verify JSONL integrity.
3. Run `bash scripts/audit/update-index.sh` to refresh INDEX.md aggregates.
4. Commit: `audit: close session <SXX> — <N> files audited, <K> total findings`

### Heavy-File Rule

A file is "heavy" if any of: >200 lines, 5+ H2 sections, 3+ code fences. Heavy files get their own session slot or share a session with at most 2 other heavy files. If a session hits 70% of context budget before completing, stop, commit, and split the remainder to a new session (update SESSIONS.md accordingly).

### Context Loading Discipline

At session start, load ONLY: RUBRIC.md, METHODOLOGY.md, SESSIONS.md (tail), and the files being audited this session. Do NOT load the full FINDINGS-DB.jsonl — use `tail -5 FINDINGS-DB.jsonl` for ID continuity, and let `next-finding-id.sh` handle ID generation.
````

- [ ] **Step 4: Write `scripts/audit/next-finding-id.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail

DB="docs/audits/2026-04-16-legion-4-7/FINDINGS-DB.jsonl"

if [[ ! -f "$DB" ]] || [[ ! -s "$DB" ]]; then
  echo "LEGION-47-001"
  exit 0
fi

LAST=$(tail -n 1 "$DB" | grep -oE '"id":"LEGION-47-[0-9]{3}"' | grep -oE '[0-9]{3}$')
NEXT=$((10#$LAST + 1))
printf "LEGION-47-%03d\n" "$NEXT"
```

Make executable: `chmod +x scripts/audit/next-finding-id.sh`

- [ ] **Step 5: Write `scripts/audit/validate-findings-db.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail

DB="docs/audits/2026-04-16-legion-4-7/FINDINGS-DB.jsonl"
FINDINGS_DIR="docs/audits/2026-04-16-legion-4-7/findings"

if [[ ! -f "$DB" ]]; then
  echo "ERROR: $DB not found"; exit 1
fi

# 1. Every line is valid JSON
LINE_NUM=0
while IFS= read -r line; do
  LINE_NUM=$((LINE_NUM + 1))
  if [[ -z "$line" ]]; then continue; fi
  if ! echo "$line" | python3 -c "import sys,json; json.loads(sys.stdin.read())" 2>/dev/null; then
    echo "ERROR: invalid JSON at line $LINE_NUM"; exit 2
  fi
done < "$DB"

# 2. All IDs are unique and sequential
IDS=$(grep -oE '"id":"LEGION-47-[0-9]{3}"' "$DB" | grep -oE 'LEGION-47-[0-9]{3}' | sort)
DUPES=$(echo "$IDS" | uniq -d)
if [[ -n "$DUPES" ]]; then
  echo "ERROR: duplicate IDs: $DUPES"; exit 3
fi

# 3. Every finding in DB references a findings file that exists
while IFS= read -r line; do
  if [[ -z "$line" ]]; then continue; fi
  FILE=$(echo "$line" | python3 -c "import sys,json; print(json.loads(sys.stdin.read())['file'])")
  # Map source path to findings path
  case "$FILE" in
    commands/*) SLUG="${FILE#commands/}"; EXPECTED="$FINDINGS_DIR/commands/${SLUG}" ;;
    skills/*/SKILL.md) SLUG="${FILE#skills/}"; SLUG="${SLUG%/SKILL.md}.md"; EXPECTED="$FINDINGS_DIR/skills/$SLUG" ;;
    agents/*) SLUG="${FILE#agents/}"; EXPECTED="$FINDINGS_DIR/agents/${SLUG}" ;;
    adapters/*) SLUG="${FILE#adapters/}"; EXPECTED="$FINDINGS_DIR/adapters/${SLUG}" ;;
    *) EXPECTED="" ;;  # root files — manual mapping, skip strict check
  esac
  if [[ -n "$EXPECTED" ]] && [[ ! -f "$EXPECTED" ]]; then
    echo "ERROR: finding references file $FILE but $EXPECTED missing"; exit 4
  fi
done < "$DB"

echo "OK: $(wc -l < "$DB") findings, all valid"
```

Make executable: `chmod +x scripts/audit/validate-findings-db.sh`

- [ ] **Step 6: Write `scripts/audit/update-index.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail

AUDIT_DIR="docs/audits/2026-04-16-legion-4-7"
DB="$AUDIT_DIR/FINDINGS-DB.jsonl"
INDEX="$AUDIT_DIR/INDEX.md"

TOTAL=$(wc -l < "$DB" | tr -d ' ')
P0=$(grep -c '"severity":"P0"' "$DB" || echo 0)
P1=$(grep -c '"severity":"P1"' "$DB" || echo 0)
P2=$(grep -c '"severity":"P2"' "$DB" || echo 0)
P3=$(grep -c '"severity":"P3"' "$DB" || echo 0)

AUDITED_COUNT=$(find "$AUDIT_DIR/findings" -name "*.md" -not -name ".gitkeep" | wc -l | tr -d ' ')

cat > "$INDEX.tmp" <<EOF
# Audit Index — Legion v7.3.2 → v7.4.0

**Audit started:** 2026-04-16
**Rubric version:** 1.0
**Baseline tag:** audit-v47-baseline
**Status:** in_progress ($AUDITED_COUNT / 125 files audited)

## Summary by Severity

- P0: $P0 findings
- P1: $P1 findings
- P2: $P2 findings
- P3: $P3 findings
- **Total:** $TOTAL findings

## Summary by Category

| Category | Count | Max Severity |
|----------|-------|--------------|
EOF

for CAT in CAT-1 CAT-2 CAT-3 CAT-4 CAT-5 CAT-6 CAT-7 CAT-8 CAT-9 CAT-10; do
  COUNT=$(grep -c "\"category\":\"$CAT\"" "$DB" || echo 0)
  MAX_SEV=$(grep "\"category\":\"$CAT\"" "$DB" | grep -oE '"severity":"P[0-3]"' | sort | head -n 1 | grep -oE 'P[0-3]' || echo "-")
  echo "| $CAT | $COUNT | $MAX_SEV |" >> "$INDEX.tmp"
done

echo "" >> "$INDEX.tmp"
echo "## Files" >> "$INDEX.tmp"
echo "" >> "$INDEX.tmp"
echo "See per-layer index sections generated by hand or re-run this script." >> "$INDEX.tmp"
echo "Findings files live under \`findings/\` by layer." >> "$INDEX.tmp"

mv "$INDEX.tmp" "$INDEX"
echo "INDEX.md updated. $AUDITED_COUNT / 125 audited, $TOTAL findings."
```

Make executable: `chmod +x scripts/audit/update-index.sh`

- [ ] **Step 7: Initialize empty FINDINGS-DB.jsonl and metadata sidecar**

```bash
touch docs/audits/2026-04-16-legion-4-7/FINDINGS-DB.jsonl
```

Write `docs/audits/2026-04-16-legion-4-7/FINDINGS-DB.meta.json`:

```json
{
  "audit_version": "1.0",
  "rubric_version": "1.0",
  "baseline_git_tag": "audit-v47-baseline",
  "total_files_in_scope": 125,
  "started": "2026-04-16",
  "source_spec": "docs/superpowers/specs/2026-04-16-legion-4-7-audit-design.md"
}
```

- [ ] **Step 8: Initialize SESSIONS.md**

Write `docs/audits/2026-04-16-legion-4-7/SESSIONS.md`:

```markdown
# SESSIONS.md — Audit Session Log

**Audit started:** 2026-04-16
**Rubric version:** 1.0

---

## Session S01 — Setup

**Started:** 2026-04-16
**Target:** Folder, rubric, methodology, scripts, baseline tag, initial state files
**Files audited:** 0
**Findings:** 0
**Status:** completed (at commit of this file)

Notes: establishes audit infrastructure. No files audited in this session.
```

- [ ] **Step 9: Initialize INDEX.md**

```bash
bash scripts/audit/update-index.sh
```

Expected: INDEX.md created showing "0 / 125 files audited, 0 findings".

- [ ] **Step 10: Tag baseline and commit**

```bash
git add docs/audits/2026-04-16-legion-4-7/ scripts/audit/
git commit -m "$(cat <<'EOF'
audit: initialize Legion v7.4.0 literalism audit infrastructure

- Create audit folder tree under docs/audits/2026-04-16-legion-4-7/
- Freeze RUBRIC.md v1.0 (10 categories: CAT-1 through CAT-10)
- Write METHODOLOGY.md with per-file audit procedure and finding schemas
- Add scripts/audit/ helpers: next-finding-id, validate-findings-db, update-index
- Initialize FINDINGS-DB.jsonl (empty), metadata sidecar, SESSIONS.md, INDEX.md
- Scope: 125 LLM-facing files across commands (17), skills (31), agents (48),
  adapters (10), root markdown (3), reference docs (4), config YAMLs (7),
  JSON schemas (5)

Spec: docs/superpowers/specs/2026-04-16-legion-4-7-audit-design.md
EOF
)"

git tag -a audit-v47-baseline -m "Baseline commit for Legion v7.4.0 4.7 literalism audit. All finding line_range values reference this commit."
```

Expected: commit succeeds; `git tag` lists `audit-v47-baseline`.

- [ ] **Step 11: Verify setup**

```bash
bash scripts/audit/next-finding-id.sh
```
Expected output: `LEGION-47-001`

```bash
bash scripts/audit/validate-findings-db.sh
```
Expected output: `OK: 0 findings, all valid`

```bash
bash scripts/audit/update-index.sh
```
Expected output: `INDEX.md updated. 0 / 125 audited, 0 findings.`

```bash
git tag | grep audit-v47-baseline
```
Expected output: `audit-v47-baseline`

---

## Task 2: Audit Session S02a — Root Markdown (3 files)

**Target files:**
- `CLAUDE.md`
- `AGENTS.md`
- `README.md`

**Estimated duration:** 1.5 hr
**Expected output:** 3 findings files under `findings/root/`, N appended findings, INDEX updated.

- [ ] **Step 1: Open session in SESSIONS.md**

Append to `docs/audits/2026-04-16-legion-4-7/SESSIONS.md`:

```markdown
## Session S02a — Root Markdown

**Started:** <YYYY-MM-DD HH:MM>
**Target:** CLAUDE.md, AGENTS.md, README.md
**Files audited:** (in progress)
**Findings:** (in progress)
**Status:** in_progress
```

- [ ] **Step 2: Audit `CLAUDE.md`**

Follow the procedure in `docs/audits/2026-04-16-legion-4-7/METHODOLOGY.md`:
1. Read `CLAUDE.md` completely.
2. Identify interaction surface (the whole file is LLM context).
3. Apply CAT-1 through CAT-10 sequentially.
4. For each finding, run `bash scripts/audit/next-finding-id.sh` to get ID.
5. Write `docs/audits/2026-04-16-legion-4-7/findings/root/CLAUDE.md` using the per-file template in METHODOLOGY.md.
6. Append each finding to FINDINGS-DB.jsonl using the JSONL schema.
7. Update INDEX.md for this row.

Commit:
```bash
bash scripts/audit/validate-findings-db.sh
git add docs/audits/2026-04-16-legion-4-7/findings/root/CLAUDE.md \
        docs/audits/2026-04-16-legion-4-7/FINDINGS-DB.jsonl \
        docs/audits/2026-04-16-legion-4-7/INDEX.md
git commit -m "audit: CLAUDE.md — N findings (<severity summary>)"
```

- [ ] **Step 3: Audit `AGENTS.md`** — same procedure as Step 2, applied to `AGENTS.md`. Commit: `audit: AGENTS.md — N findings (<severity summary>)`

- [ ] **Step 4: Audit `README.md`** — same procedure as Step 2, applied to `README.md`. Commit: `audit: README.md — N findings (<severity summary>)`

- [ ] **Step 5: Close session**

Run:
```bash
bash scripts/audit/update-index.sh
bash scripts/audit/validate-findings-db.sh
```

Update SESSIONS.md's S02a section: set ended time, file count (3), finding count, status `completed`.

Commit:
```bash
git add docs/audits/2026-04-16-legion-4-7/SESSIONS.md docs/audits/2026-04-16-legion-4-7/INDEX.md
git commit -m "audit: close session S02a — 3 files audited, K total findings"
```

---

## Task 3: Audit Session S02b — Reference Docs (4 files)

**Target files:**
- `docs/control-modes.md`
- `docs/runtime-audit.md`
- `docs/runtime-certification-checklists.md`
- `docs/security/install-integrity.md`

**Estimated duration:** 1 hr
**Expected output:** 4 findings files under `findings/root/` (slugs: `doc-control-modes.md`, `doc-runtime-audit.md`, `doc-runtime-certification-checklists.md`, `doc-security-install-integrity.md`).

- [ ] **Step 1: Open session in SESSIONS.md** — append S02b section with start time and target list.

- [ ] **Step 2: Audit `docs/control-modes.md`**

Per METHODOLOGY.md. Write findings to `docs/audits/2026-04-16-legion-4-7/findings/root/doc-control-modes.md`. Append findings to FINDINGS-DB.jsonl. Commit: `audit: docs/control-modes.md — N findings (<severity summary>)`

- [ ] **Step 3: Audit `docs/runtime-audit.md`**

Per METHODOLOGY.md. Findings file: `findings/root/doc-runtime-audit.md`. Commit: `audit: docs/runtime-audit.md — N findings (<severity summary>)`

- [ ] **Step 4: Audit `docs/runtime-certification-checklists.md`**

Per METHODOLOGY.md. Findings file: `findings/root/doc-runtime-certification-checklists.md`. Commit: `audit: docs/runtime-certification-checklists.md — N findings (<severity summary>)`

- [ ] **Step 5: Audit `docs/security/install-integrity.md`**

Per METHODOLOGY.md. Findings file: `findings/root/doc-security-install-integrity.md`. Commit: `audit: docs/security/install-integrity.md — N findings (<severity summary>)`

- [ ] **Step 6: Close session**

Run index update + validation. Update SESSIONS.md S02b with end time, 4 files, K findings, status completed. Commit: `audit: close session S02b — 4 files audited, K total findings`

---

## Task 4: Audit Session S02c — Config YAMLs (7 files)

**Target files:**
- `.planning/config/agent-communication.yaml`
- `.planning/config/authority-matrix.yaml`
- `.planning/config/control-modes.yaml`
- `.planning/config/directory-mappings.yaml`
- `.planning/config/escalation-protocol.yaml`
- `.planning/config/intent-teams.yaml`
- `.planning/config/roster-gap-config.yaml`

**Estimated duration:** 1.25 hr
**Note:** YAMLs mostly carry structural data but `description`, `trigger`, and similar fields contain prose that is LLM-facing. Focus rubric application on those fields.

- [ ] **Step 1: Open session S02c in SESSIONS.md.**

- [ ] **Step 2: Audit `.planning/config/agent-communication.yaml`** — findings file: `findings/root/config-agent-communication.md`. Commit per METHODOLOGY.md.

- [ ] **Step 3: Audit `.planning/config/authority-matrix.yaml`** — findings file: `findings/root/config-authority-matrix.md`. Commit.

- [ ] **Step 4: Audit `.planning/config/control-modes.yaml`** — findings file: `findings/root/config-control-modes.md`. Commit.

- [ ] **Step 5: Audit `.planning/config/directory-mappings.yaml`** — findings file: `findings/root/config-directory-mappings.md`. Commit.

- [ ] **Step 6: Audit `.planning/config/escalation-protocol.yaml`** — findings file: `findings/root/config-escalation-protocol.md`. Commit.

- [ ] **Step 7: Audit `.planning/config/intent-teams.yaml`** — findings file: `findings/root/config-intent-teams.md`. Commit.

- [ ] **Step 8: Audit `.planning/config/roster-gap-config.yaml`** — findings file: `findings/root/config-roster-gap-config.md`. Commit.

- [ ] **Step 9: Close session S02c.** Run validators, update SESSIONS.md, commit session close.

---

## Task 5: Audit Session S02d — JSON Schemas (5 files)

**Target files:**
- `docs/settings.schema.json`
- `docs/schemas/outcomes-record.schema.json`
- `docs/schemas/plan-frontmatter.schema.json`
- `docs/schemas/review-finding.schema.json`
- `docs/schemas/summary.schema.json`

**Estimated duration:** 45 min
**Note:** Fast audit. JSON schemas are structural; focus rubric application only on `title`, `description`, `examples`, and any free-prose annotations. Expect low finding count per file.

- [ ] **Step 1: Open session S02d in SESSIONS.md.**

- [ ] **Step 2: Audit `docs/settings.schema.json`** — findings file: `findings/root/schema-settings.md`. Commit.

- [ ] **Step 3: Audit `docs/schemas/outcomes-record.schema.json`** — findings file: `findings/root/schema-outcomes-record.md`. Commit.

- [ ] **Step 4: Audit `docs/schemas/plan-frontmatter.schema.json`** — findings file: `findings/root/schema-plan-frontmatter.md`. Commit.

- [ ] **Step 5: Audit `docs/schemas/review-finding.schema.json`** — findings file: `findings/root/schema-review-finding.md`. Commit.

- [ ] **Step 6: Audit `docs/schemas/summary.schema.json`** — findings file: `findings/root/schema-summary.md`. Commit.

- [ ] **Step 7: Close session S02d.** Run validators, update SESSIONS.md, commit session close.

---

## Task 6: Audit Session S03 — Commands 1-6 (6 files)

**Target files:**
- `commands/advise.md`
- `commands/agent.md`
- `commands/board.md`
- `commands/build.md`
- `commands/explore.md`
- `commands/learn.md`

**Estimated duration:** 1.5 hr
**Note:** Commands are heavy on user-facing prompts and agent dispatch — expect high CAT-1, CAT-3, CAT-4 yields. `commands/build.md` in particular is a primary orchestration entry point.

- [ ] **Step 1: Open session S03 in SESSIONS.md.**

- [ ] **Step 2-7: Audit each file** using METHODOLOGY.md procedure. Findings files under `findings/commands/`. Per-file commits.

- [ ] **Step 8: Close session S03.** Run validators, update SESSIONS.md, commit session close.

---

## Task 7: Audit Session S04 — Commands 7-12 (6 files)

**Target files:**
- `commands/milestone.md`
- `commands/plan.md`
- `commands/portfolio.md`
- `commands/quick.md`
- `commands/retro.md`
- `commands/review.md`

**Estimated duration:** 1.5 hr
**Note:** `commands/plan.md` and `commands/review.md` are high-traffic orchestration commands; expect P0/P1 yields.

- [ ] **Step 1: Open session S04.**

- [ ] **Step 2-7: Audit each file.** Findings files under `findings/commands/`. Per-file commits.

- [ ] **Step 8: Close session S04.**

---

## Task 8: Audit Session S05 — Commands 13-17 (5 files)

**Target files:**
- `commands/ship.md`
- `commands/start.md`
- `commands/status.md`
- `commands/update.md`
- `commands/validate.md`

**Estimated duration:** 1.25 hr
**Note:** `commands/start.md` is the project-initialization flow with extensive user interaction — likely CAT-1 hotspot. `commands/ship.md` has dispatch-heavy content — likely CAT-3 hotspot.

- [ ] **Step 1: Open session S05.**

- [ ] **Step 2-6: Audit each file.** Findings files under `findings/commands/`. Per-file commits.

- [ ] **Step 7: Close session S05.**

---

## Task 9: Audit Session S06 — Core Orchestration Skills (7 files)

**Target files:**
- `skills/workflow-common/SKILL.md`
- `skills/workflow-common-core/SKILL.md`
- `skills/workflow-common-domains/SKILL.md`
- `skills/workflow-common-github/SKILL.md`
- `skills/workflow-common-memory/SKILL.md`
- `skills/wave-executor/SKILL.md`
- `skills/cli-dispatch/SKILL.md`

**Estimated duration:** 1.75 hr
**Note:** These skills ARE the orchestration spine. Defects here affect every command. `wave-executor` and `cli-dispatch` likely have P0 CAT-3 findings (underspecified dispatch is core to what they do).

- [ ] **Step 1: Open session S06.**

- [ ] **Step 2-8: Audit each SKILL.md file.** Findings slugs: `workflow-common.md`, `workflow-common-core.md`, `workflow-common-domains.md`, `workflow-common-github.md`, `workflow-common-memory.md`, `wave-executor.md`, `cli-dispatch.md`. Per-file commits.

- [ ] **Step 9: Close session S06.**

---

## Task 10: Audit Session S07 — Planning Skills (5 files)

**Target files:**
- `skills/phase-decomposer/SKILL.md`
- `skills/plan-critique/SKILL.md`
- `skills/spec-pipeline/SKILL.md`
- `skills/questioning-flow/SKILL.md`
- `skills/intent-router/SKILL.md`

**Estimated duration:** 1.25 hr
**Note:** `questioning-flow` is a CAT-1 hotspot (it IS the closed-set-option-presentation skill). `plan-critique` likely has CAT-4 (intent) findings.

- [ ] **Step 1: Open session S07.**

- [ ] **Step 2-6: Audit each SKILL.md.** Findings under `findings/skills/`. Per-file commits.

- [ ] **Step 7: Close session S07.**

---

## Task 11: Audit Session S08 — Execution & Review Skills (5 files)

**Target files:**
- `skills/execution-tracker/SKILL.md`
- `skills/review-loop/SKILL.md`
- `skills/review-panel/SKILL.md`
- `skills/review-evaluators/SKILL.md`
- `skills/authority-enforcer/SKILL.md`

**Estimated duration:** 1.25 hr
**Note:** `authority-enforcer` is a CAT-10 reference point (authority boundaries). Review skills likely have CAT-8 (acceptance criteria) and CAT-3 (dispatch) findings.

- [ ] **Step 1: Open session S08.**

- [ ] **Step 2-6: Audit each SKILL.md.** Findings under `findings/skills/`. Per-file commits.

- [ ] **Step 7: Close session S08.**

---

## Task 12: Audit Session S09 — Domain & Integration Skills (6 files)

**Target files:**
- `skills/design-workflows/SKILL.md`
- `skills/marketing-workflows/SKILL.md`
- `skills/security-review/SKILL.md`
- `skills/ship-pipeline/SKILL.md`
- `skills/board-of-directors/SKILL.md`
- `skills/codebase-mapper/SKILL.md`

**Estimated duration:** 1.5 hr

- [ ] **Step 1: Open session S09.**

- [ ] **Step 2-7: Audit each SKILL.md.** Findings under `findings/skills/`. Per-file commits.

- [ ] **Step 8: Close session S09.**

---

## Task 13: Audit Session S10 — Intelligence & Tracking Skills (8 files)

**Target files:**
- `skills/polymath-engine/SKILL.md`
- `skills/memory-manager/SKILL.md`
- `skills/milestone-tracker/SKILL.md`
- `skills/portfolio-manager/SKILL.md`
- `skills/agent-creator/SKILL.md`
- `skills/agent-registry/SKILL.md`
- `skills/github-sync/SKILL.md`
- `skills/hooks-integration/SKILL.md`

**Estimated duration:** 1.75 hr
**Note:** `agent-creator` contains the template used to generate new agent personalities — findings here propagate forward. Audit its template content with extra scrutiny for CAT-7 (maximalist persona language).

- [ ] **Step 1: Open session S10.**

- [ ] **Step 2-9: Audit each SKILL.md.** Findings under `findings/skills/`. Per-file commits.

- [ ] **Step 10: Close session S10.**

---

## Task 14: Audit Session S11 — Engineering Division Agents (9 files)

**Target files:**
- `agents/engineering-ai-engineer.md`
- `agents/engineering-backend-architect.md`
- `agents/engineering-frontend-developer.md`
- `agents/engineering-infrastructure-devops.md`
- `agents/engineering-laravel-specialist.md`
- `agents/engineering-mobile-app-builder.md`
- `agents/engineering-rapid-prototyper.md`
- `agents/engineering-security-engineer.md`
- `agents/engineering-senior-developer.md`

**Estimated duration:** 1 hr
**Note:** Agent persona files. Expected categories: CAT-7 (maximalist persona language) dominant. CAT-10 (authority) in senior-developer and security-engineer.

- [ ] **Step 1: Open session S11.**

- [ ] **Step 2-10: Audit each agent file.** Findings under `findings/agents/`. Per-file commits.

- [ ] **Step 11: Close session S11.**

---

## Task 15: Audit Session S12 — Design + Marketing Agents (10 files)

**Target files:**
- `agents/design-brand-guardian.md`
- `agents/design-ui-designer.md`
- `agents/design-ux-architect.md`
- `agents/design-ux-researcher.md`
- `agents/design-visual-storyteller.md`
- `agents/design-whimsy-injector.md`
- `agents/marketing-app-store-optimizer.md`
- `agents/marketing-content-social-strategist.md`
- `agents/marketing-growth-hacker.md`
- `agents/marketing-social-platform-specialist.md`

**Estimated duration:** 1 hr

- [ ] **Step 1: Open session S12.**

- [ ] **Step 2-11: Audit each agent file.** Findings under `findings/agents/`. Per-file commits.

- [ ] **Step 12: Close session S12.**

---

## Task 16: Audit Session S13 — Product + Testing Agents (10 files)

**Target files:**
- `agents/product-feedback-synthesizer.md`
- `agents/product-sprint-prioritizer.md`
- `agents/product-technical-writer.md`
- `agents/product-trend-researcher.md`
- `agents/testing-api-tester.md`
- `agents/testing-performance-benchmarker.md`
- `agents/testing-qa-verification-specialist.md`
- `agents/testing-test-results-analyzer.md`
- `agents/testing-tool-evaluator.md`
- `agents/testing-workflow-optimizer.md`

**Estimated duration:** 1 hr
**Note:** `testing-qa-verification-specialist` has orchestration authority (participates in the S17.5 review gate) — audit with extra scrutiny. Expected to have CAT-7 elevated to P1 per RUBRIC.md guidance.

- [ ] **Step 1: Open session S13.**

- [ ] **Step 2-11: Audit each agent file.** Findings under `findings/agents/`. Per-file commits.

- [ ] **Step 12: Close session S13.**

---

## Task 17: Audit Session S14 — Project Management + Support Agents (9 files)

**Target files:**
- `agents/project-management-experiment-tracker.md`
- `agents/project-management-project-shepherd.md`
- `agents/project-management-studio-operations.md`
- `agents/project-management-studio-producer.md`
- `agents/project-manager-senior.md`
- `agents/support-executive-summary-generator.md`
- `agents/support-finance-tracker.md`
- `agents/support-legal-compliance-checker.md`
- `agents/support-support-responder.md`

**Estimated duration:** 1 hr

- [ ] **Step 1: Open session S14.**

- [ ] **Step 2-10: Audit each agent file.** Findings under `findings/agents/`. Per-file commits.

- [ ] **Step 11: Close session S14.**

---

## Task 18: Audit Session S15 — Spatial + Specialized Agents (10 files)

**Target files:**
- `agents/macos-spatial-metal-engineer.md`
- `agents/terminal-integration-specialist.md`
- `agents/visionos-spatial-engineer.md`
- `agents/xr-cockpit-interaction-specialist.md`
- `agents/xr-immersive-developer.md`
- `agents/xr-interface-architect.md`
- `agents/agents-orchestrator.md`
- `agents/data-analytics-engineer.md`
- `agents/lsp-index-engineer.md`
- `agents/polymath.md`

**Estimated duration:** 1 hr
**Note:** `agents-orchestrator` and `polymath` have orchestration authority — audit with extra scrutiny for CAT-7 (elevated to P1 per RUBRIC.md) and CAT-10.

- [ ] **Step 1: Open session S15.**

- [ ] **Step 2-11: Audit each agent file.** Findings under `findings/agents/`. Per-file commits.

- [ ] **Step 12: Close session S15.**

---

## Task 19: Audit Session S16 — CLI Adapters (10 files)

**Target files:**
- `adapters/ADAPTER.md`
- `adapters/aider.md`
- `adapters/claude-code.md`
- `adapters/codex-cli.md`
- `adapters/copilot-cli.md`
- `adapters/cursor.md`
- `adapters/gemini-cli.md`
- `adapters/kiro-cli.md`
- `adapters/opencode.md`
- `adapters/windsurf.md`

**Estimated duration:** 1.5 hr
**Note:** Adapters define CLI-specific dispatch conventions. Expected categories: CAT-3 (dispatch) and CAT-6 (implicit preconditions about CLI capabilities).

- [ ] **Step 1: Open session S16.**

- [ ] **Step 2-11: Audit each adapter file.** Findings under `findings/adapters/`. Per-file commits.

- [ ] **Step 12: Close session S16.**

---

## Task 20: Cross-Cut Review (S17)

**Purpose:** Validate rubric consistency across the full audit. Rescore stratified samples from early/mid/late sessions against the final rubric state. Decide whether a RUBRIC v1.1 bump is needed.

**Expected duration:** 1 hr

- [ ] **Step 1: Open session S17 in SESSIONS.md.**

- [ ] **Step 2: Re-read the final state of findings across the audit**

```bash
bash scripts/audit/update-index.sh
bash scripts/audit/validate-findings-db.sh
cat docs/audits/2026-04-16-legion-4-7/INDEX.md
```

Note any category with unexpectedly high or low counts.

- [ ] **Step 3: Pick a stratified sample of 5 files for rescoring**

Criteria:
1. One file from the earliest audit session (S02a, S02b, or S02c)
2. One from the middle skills sessions (S06–S09)
3. One from the agent sessions (S11–S15)
4. One from adapters (S16)
5. One that currently has zero findings (to check for false negatives)

Record the 5 file paths in a new section of SESSIONS.md under `## Session S17 — Cross-Cut Review`.

- [ ] **Step 4: Rescore each sampled file against the current rubric**

For each of the 5 files:
1. Re-read the file and the corresponding findings file.
2. Re-apply the rubric from scratch (don't anchor on existing findings).
3. Compare your fresh assessment to the committed findings.
4. Note any mismatches: missed findings, false positives, severity misalignments.

- [ ] **Step 5: Decide on rubric versioning**

Decision rule (document the decision in SESSIONS.md S17 section):
- If rescoring produces <5% change in findings across the sample: **rubric stays at v1.0**, audit proceeds to S17.5.
- If rescoring produces 5-15% change: **bump rubric to v1.1**, re-audit the 4 sampled non-zero files + all files that showed mismatches.
- If rescoring produces >15% change: **bump rubric to v1.1** AND re-audit the full category where mismatch concentrated.

- [ ] **Step 6: Close session S17**

If re-auditing is required, treat it as a separate session (e.g., S17a) before proceeding to S17.5.

```bash
bash scripts/audit/update-index.sh
bash scripts/audit/validate-findings-db.sh
git add docs/audits/2026-04-16-legion-4-7/SESSIONS.md docs/audits/2026-04-16-legion-4-7/INDEX.md
git commit -m "audit: close session S17 — cross-cut review, rubric <v1.0|v1.1> confirmed"
```

---

## Task 21: Review Gate via `/legion:advise` (S17.5)

**Purpose:** Formal second-party review of audit deliverables before REMEDIATION.md is generated. Two agents review a stratified sample.

**Expected duration:** 1 hr

- [ ] **Step 1: Open session S17.5 in SESSIONS.md.**

- [ ] **Step 2: Prepare the stratified review sample (5 files)**

Select 5 findings files that represent:
- 1 P0-heavy file
- 1 P1-heavy file
- 1 P2-only file
- 1 zero-findings file
- 1 agent persona file

Record the sample selection in SESSIONS.md under `## Session S17.5 — Review Gate`.

- [ ] **Step 3: Dispatch QA-verification review**

Run:
```
/legion:advise

Agent: testing-qa-verification-specialist

Request: review the audit findings at the following 5 files against the audit rubric 
at docs/audits/2026-04-16-legion-4-7/RUBRIC.md and methodology at 
docs/audits/2026-04-16-legion-4-7/METHODOLOGY.md. For each file, answer:
1. Are findings real (not invented)?
2. Are severities calibrated consistently?
3. Are remediation sketches actionable (specific enough to implement)?
Produce a single verdict: ship-audit | adjust-rubric | re-audit-sample.

Files:
<list the 5 files>
```

Record verdict in SESSIONS.md.

- [ ] **Step 4: Dispatch Technical Writer review**

Run:
```
/legion:advise

Agent: product-technical-writer

Request: review the audit outputs at docs/audits/2026-04-16-legion-4-7/ for clarity, 
consistency, and execution readiness. Answer:
1. Could a fresh reader execute REMEDIATION.md (once generated) without prior context?
2. Are finding IDs, cluster names, and severity labels used consistently?
3. Is the per-file findings template followed faithfully?
Produce a single verdict: publish-ready | revise.
```

Record verdict in SESSIONS.md.

- [ ] **Step 5: Apply gate logic**

- If **both** verdicts are `ship-audit` + `publish-ready` → proceed to Task 22.
- If **either** verdict is negative:
  - For `adjust-rubric`: bump rubric to v1.1 with specific adjustments, re-audit affected files, re-run the gate.
  - For `re-audit-sample`: re-audit the flagged sample with the current rubric, re-run the gate.
  - For `revise`: apply clarity/consistency fixes to findings files, update INDEX.md, re-run the gate (writer review only).

- [ ] **Step 6: Close session S17.5**

```bash
git add docs/audits/2026-04-16-legion-4-7/SESSIONS.md
git commit -m "audit: close session S17.5 — review gate (<qa-verdict>, <writer-verdict>)"
```

---

## Task 22: Generate REMEDIATION.md (S18)

**Purpose:** Transform the findings database into a thematically-clustered remediation roadmap that can be fed directly into `/legion:plan N` for v7.4.0 hardening phases.

**Expected duration:** 2 hr

**Files:**
- Create: `docs/audits/2026-04-16-legion-4-7/REMEDIATION.md`

- [ ] **Step 1: Open session S18 in SESSIONS.md.**

- [ ] **Step 2: Extract cluster groupings from FINDINGS-DB.jsonl**

```bash
# Enumerate all distinct clusters and their finding counts
python3 <<'EOF'
import json
from collections import defaultdict

clusters = defaultdict(list)
with open("docs/audits/2026-04-16-legion-4-7/FINDINGS-DB.jsonl") as f:
    for line in f:
        if not line.strip(): continue
        d = json.loads(line)
        clusters[d["remediation_cluster"]].append(d)

for name, findings in sorted(clusters.items(), key=lambda kv: -len(kv[1])):
    severities = [f["severity"] for f in findings]
    files = set(f["file"] for f in findings)
    p0 = severities.count("P0")
    p1 = severities.count("P1")
    p2 = severities.count("P2")
    p3 = severities.count("P3")
    print(f"{name}: {len(findings)} findings, {len(files)} files, {p0}/P0 {p1}/P1 {p2}/P2 {p3}/P3")
EOF
```

- [ ] **Step 3: Order clusters into phases**

Apply ordering rules (from spec Section 7.2):
1. P0-heavy clusters first.
2. Among equal-severity clusters, sort by leverage (more files → earlier).
3. Among equal severity and leverage, sort by effort (smaller → earlier).

- [ ] **Step 4: Write REMEDIATION.md**

Template:
```markdown
# REMEDIATION.md — Legion v7.4.0 Audit Remediation Roadmap

**Audit version:** 1.0 (or v1.1 if rescoring occurred)
**Total findings:** <N>
**Phases:** <M>
**Baseline tag:** audit-v47-baseline

This document groups audit findings into thematic clusters. Each cluster maps to 
a Legion phase that can be executed via `/legion:plan <phase-number>` and 
`/legion:build` against the current HEAD.

## Release Mapping

- **must-ship-v7.4.0:** Phases 1–<X>
- **recommended-v7.4.0:** Phases <X+1>–<Y>
- **defer-v7.5.0:** Phases <Y+1>–<M>

---

## Phase 1 — <Cluster Name>

**Cluster slug:** `<cluster-slug>`
**Target:** <one-sentence target>
**Finding IDs:** LEGION-47-<NNN>, LEGION-47-<NNN>, ... (full list)
**Finding count:** <N> (<P0>/P0, <P1>/P1, <P2>/P2, <P3>/P3)
**Files modified:** <count> files
  - <file 1>
  - <file 2>
  - ...
**Severity mix:** <description>
**Effort estimate:** small | medium | large (<day estimate>)
**Pattern:** <the remediation approach, referencing the CAT remediation pattern in RUBRIC.md>
**Verification:** <exact commands or workflows to confirm remediation success>
**Release label:** must-ship-v7.4.0 | recommended-v7.4.0 | defer-v7.5.0

---

[Repeat for each cluster/phase]

---

## Orphan / Unclustered Findings

[List any findings that don't fit a cluster — ideally zero. If non-zero, explain.]
```

Populate every field from the findings database. No placeholder text — if effort estimate is uncertain, pick the more conservative (larger) option and note the uncertainty.

- [ ] **Step 5: Rebase line_ranges against current HEAD**

Since the audit ran against `audit-v47-baseline`, files may have shifted. Run:

```bash
# Identify any line_range drifts between baseline and HEAD
python3 <<'EOF'
import json, subprocess
with open("docs/audits/2026-04-16-legion-4-7/FINDINGS-DB.jsonl") as f:
    for line in f:
        if not line.strip(): continue
        d = json.loads(line)
        # Check if excerpt still appears at expected line range in HEAD
        # Output drift warnings only — don't auto-modify DB
        result = subprocess.run(
            ["git", "diff", "audit-v47-baseline", "HEAD", "--", d["file"]],
            capture_output=True, text=True
        )
        if result.stdout:
            print(f"DRIFT-CANDIDATE: {d['id']} {d['file']} (file changed since baseline)")
EOF
```

For any flagged findings, manually verify the `excerpt` still matches a line range in HEAD, and update `line_range` in both the findings file and FINDINGS-DB.jsonl. Do NOT modify `excerpt` — that's the authoritative identifier.

- [ ] **Step 6: Validate REMEDIATION.md against FINDINGS-DB**

Run a consistency check:
```bash
python3 <<'EOF'
import json
ids_in_db = set()
with open("docs/audits/2026-04-16-legion-4-7/FINDINGS-DB.jsonl") as f:
    for line in f:
        if not line.strip(): continue
        ids_in_db.add(json.loads(line)["id"])

with open("docs/audits/2026-04-16-legion-4-7/REMEDIATION.md") as f:
    content = f.read()

import re
ids_in_remediation = set(re.findall(r"LEGION-47-\d{3}", content))

missing = ids_in_db - ids_in_remediation
extra = ids_in_remediation - ids_in_db
print(f"Findings in DB: {len(ids_in_db)}")
print(f"Findings referenced in REMEDIATION.md: {len(ids_in_remediation)}")
if missing:
    print(f"MISSING from REMEDIATION.md ({len(missing)}): {sorted(missing)[:10]}...")
if extra:
    print(f"EXTRA in REMEDIATION.md ({len(extra)}): {sorted(extra)[:10]}...")
if not missing and not extra:
    print("OK: all findings accounted for in REMEDIATION.md")
EOF
```

Expected: `OK: all findings accounted for in REMEDIATION.md`

If findings are missing from REMEDIATION.md, they MUST be added to an existing phase OR listed under "Orphan / Unclustered Findings" with a reason.

- [ ] **Step 7: Commit REMEDIATION.md**

```bash
git add docs/audits/2026-04-16-legion-4-7/REMEDIATION.md \
        docs/audits/2026-04-16-legion-4-7/FINDINGS-DB.jsonl \
        docs/audits/2026-04-16-legion-4-7/findings/
git commit -m "$(cat <<'EOF'
audit: generate REMEDIATION.md — <M> phases, <N> findings clustered

Thematic clusters ordered by severity, leverage, effort. Phase release labels
assign must-ship-v7.4.0, recommended-v7.4.0, defer-v7.5.0. All findings from
FINDINGS-DB.jsonl accounted for (verified). Line ranges rebased against current
HEAD; excerpts unchanged.
EOF
)"
```

- [ ] **Step 8: Close session S18**

Update SESSIONS.md S18 section with end time, phase count, release mapping breakdown, and any drift warnings resolved. Commit: `audit: close session S18 — REMEDIATION.md generated, <M> phases`

---

## Task 23: Final Sign-Off (S19)

**Purpose:** Verify all 8 success criteria from the design spec. Produce an audit completion summary. Tag final audit state.

**Expected duration:** 1 hr

- [ ] **Step 1: Open session S19 in SESSIONS.md.**

- [ ] **Step 2: Verify success criterion 1 — all 125 files audited**

```bash
find docs/audits/2026-04-16-legion-4-7/findings -name "*.md" -not -name ".gitkeep" | wc -l
```
Expected: `125`

If not 125, identify missing files:
```bash
# Enumerate all expected files and diff against findings
python3 <<'EOF'
import os, glob

expected = []
expected.extend(glob.glob("commands/*.md"))
expected.extend(glob.glob("skills/*/SKILL.md"))
expected.extend(glob.glob("agents/*.md"))
expected.extend(glob.glob("adapters/*.md"))
expected.extend(["CLAUDE.md", "AGENTS.md", "README.md",
                 "docs/control-modes.md", "docs/runtime-audit.md",
                 "docs/runtime-certification-checklists.md",
                 "docs/security/install-integrity.md",
                 "docs/settings.schema.json"])
expected.extend(glob.glob(".planning/config/*.yaml"))
expected.extend(glob.glob("docs/schemas/*.json"))

print(f"Expected: {len(expected)} files")
# (Also enumerate findings/ directory and report missing)
EOF
```

- [ ] **Step 3: Verify success criterion 2 — INDEX.md shows 125/125**

```bash
bash scripts/audit/update-index.sh
grep "125 / 125" docs/audits/2026-04-16-legion-4-7/INDEX.md || grep "125 audited" docs/audits/2026-04-16-legion-4-7/INDEX.md
```

If not matching, debug the update script.

- [ ] **Step 4: Verify success criterion 3 — FINDINGS-DB.jsonl integrity**

```bash
bash scripts/audit/validate-findings-db.sh
```
Expected: `OK: <N> findings, all valid`

- [ ] **Step 5: Verify success criterion 4 — all findings in REMEDIATION.md**

Re-run the cross-reference check from Task 22 Step 6. Expected: `OK: all findings accounted for in REMEDIATION.md`

- [ ] **Step 6: Verify success criterion 5 — S17.5 verdicts recorded**

```bash
grep -E "ship-audit|publish-ready" docs/audits/2026-04-16-legion-4-7/SESSIONS.md
```
Expected: both verdicts present, confirming the review gate passed.

- [ ] **Step 7: Verify success criterion 6 — SESSIONS.md completeness**

```bash
grep -c "^## Session" docs/audits/2026-04-16-legion-4-7/SESSIONS.md
```
Expected: >=23 (at least one section per session S01 through S19; count may be higher if S17a rescoring was needed or gate re-runs occurred).

Each session section must contain start time, end time, files audited count, findings count, status `completed`.

- [ ] **Step 8: Verify success criterion 7 — git history traceable**

```bash
git log --oneline audit-v47-baseline..HEAD -- docs/audits/2026-04-16-legion-4-7/ | wc -l
```
Expected: >=140 (one baseline commit + ~125 per-file audit commits + ~20 session-close commits + REMEDIATION + sign-off). Exact number varies but should be clearly >125.

- [ ] **Step 9: Verify success criterion 8 — baseline tag exists**

```bash
git rev-parse audit-v47-baseline
```
Expected: a valid SHA.

- [ ] **Step 10: Write the audit completion summary**

Append to SESSIONS.md:

```markdown
## Session S19 — Final Sign-Off

**Started:** <timestamp>
**Ended:** <timestamp>

### Success Criteria Verification

- [x] 1. All 125 in-scope files audited
- [x] 2. INDEX.md shows 125/125 audited
- [x] 3. FINDINGS-DB.jsonl validated (N findings)
- [x] 4. REMEDIATION.md groups all findings (M phases)
- [x] 5. S17.5 gate verdicts recorded: ship-audit + publish-ready
- [x] 6. SESSIONS.md documents all sessions (K entries)
- [x] 7. Git history traceable (L commits since baseline)
- [x] 8. audit-v47-baseline tag exists

### Audit Totals

- Files audited: 125
- Total findings: <N>
  - P0: <count>
  - P1: <count>
  - P2: <count>
  - P3: <count>
- Remediation phases: <M>
- Rubric version at close: <v1.0 | v1.1>

### Rubric Drift

<summary of any rubric changes and rescoring performed>

### Handoff

The REMEDIATION.md roadmap is ready for execution via Legion's own workflow:
`/legion:plan <N>` for each phase, followed by `/legion:build`, `/legion:review`, 
`/legion:ship`. Phase ordering and release labels are fixed in REMEDIATION.md.

**Status:** audit complete
```

- [ ] **Step 11: Tag audit completion**

```bash
git add docs/audits/2026-04-16-legion-4-7/SESSIONS.md
git commit -m "$(cat <<'EOF'
audit: sign off Legion v7.4.0 literalism audit

- 125 files audited across 23 sessions (S01-S19)
- All 8 success criteria from design spec verified
- REMEDIATION.md ready for /legion:plan ingestion
- Baseline: audit-v47-baseline
EOF
)"

git tag -a audit-v47-complete -m "Legion v7.4.0 4.7 literalism audit complete. See docs/audits/2026-04-16-legion-4-7/SESSIONS.md and REMEDIATION.md."
```

- [ ] **Step 12: Final verification**

```bash
git tag | grep audit-v47
# Expected: both audit-v47-baseline and audit-v47-complete

git log audit-v47-baseline..audit-v47-complete --oneline | head -20
# Spot-check: recent commits are all audit-related

bash scripts/audit/validate-findings-db.sh
# Expected: OK
```

**Audit complete.** Hand off to remediation via `/legion:plan` for each phase in REMEDIATION.md.

---

## Self-Review

Ran through the plan against the design spec:

**Spec coverage check:**
- Spec Section 4 (rubric) → Task 1 Step 2 (RUBRIC.md frozen)
- Spec Section 5 (severity scale) → Task 1 Step 2 (included in RUBRIC.md)
- Spec Section 6 (per-file workflow) → Task 1 Step 3 (METHODOLOGY.md frozen, referenced in all audit tasks)
- Spec Section 6.1 (finding schema) → Task 1 Step 3 (JSONL schema in METHODOLOGY.md)
- Spec Section 6.2 (per-file template) → Task 1 Step 3 (template in METHODOLOGY.md)
- Spec Section 7 (remediation) → Task 22
- Spec Section 8 (schedule) → Tasks 2-19 (one task per session S02a-S16)
- Spec Section 8.1 (session rules) → METHODOLOGY.md heavy-file rule + validators
- Spec Section 8.2 (baseline/rebase) → Task 1 Step 10 (tag) + Task 22 Step 5 (rebase)
- Spec Section 9 (review gate) → Task 21
- Spec Section 10 (risks) → mitigated via validators (Risk 5), per-session commits (Risk 7), S17 (Risk 1), S17.5 (Risk 8)
- Spec Section 12 (success criteria) → Task 23 (all 8 verified)

**Placeholder scan:** no TBD/TODO markers. The `<N>`, `<NN-MM>`, `<severity summary>` patterns in commit messages and templates are runtime substitutions (not plan-time gaps) — the engineer fills them from actual audit results.

**Type consistency:**
- Finding ID pattern `LEGION-47-NNN` consistent throughout
- Category IDs `CAT-1` through `CAT-10` consistent
- Cluster slug convention (kebab-case) stated in METHODOLOGY.md and used in examples
- Session ID pattern `SNN` / `SNNa` consistent
- File layer slugs consistent between METHODOLOGY.md and subsequent task file lists

**Scope reconciliation:** spec said 124 files; actual and this plan use 125 (agent count 48 not 47). Flagged in plan header.

Plan is complete and self-consistent.
